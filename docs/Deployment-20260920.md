# 线上测试部署记录（2026-09-20）

- 游戏服务及命令行 GM 后端构建自 `c54b6e1b7b05451d6232802b95f71fc9c9f8b5d0`，目标 Linux amd64。全量 Go 测试通过。
- 更新前确认没有游戏连接，备份旧程序、服务配置和生产数据库；数据库压缩备份通过完整性检查。未执行账号清空、奖励补发或密码重置。
- 同步服务器与线上登录器的客户端资源校验值，保留服务器其余配置、线上玩家和 GM 配置。
- 服务重启后健康检查通过；实际运行进程映像与已上传二进制 SHA256 一致。域名 WebSocket、内层证书校验及服务端加密响应连续三次通过。
- Windows 线上登录器重新构建内嵌 Go 桥接与 C++ 登录组件，自带 .NET 运行时；自检与线上健康检查通过。现有 `线上登录器.lnk` 指向 `dist/online-launcher/功夫小子线上登录器.exe`。

服务端 SHA256：`4b1e576f4ab1aa98094a4e6b135f83fd099ac0d167e1a217e16d66709fe2ccc3`。
GM 后端 SHA256：`bb44143fdde4bbd0961ef1181ace11bed15eaa128dd9a9332f88ae36438df381`。
线上登录器 SHA256：`1c1673890091d0ffdc0fc3c010af92ece9318c8fdb543ca0fbc7b1ea0e2781b1`。

这些验证证明程序部署及传输链路可用，不能替代真实客户端登录、房间、战斗与奖励画面验收。模式10准入仍关闭；新手武器选择需要在 GM 新手奖励中配置候选，旧已领奖角色不会重复补发。旧版本绑定的关卡/任务等客户端配置仍保留其版本校验，不自动重写为新资源版本。

部署连接信息、凭据、备份和 EXE 均不进入源码仓库。
