# 服饰菜单与套装回归修复（2026-09-22）

上一版直接穿戴 kind18 并发送1531的方案已撤回，不能继续作为有效实现说明。原生实测出现临时重复仓库条目。

普通衣服kind12..17继续修复count字段。kind18恢复为一次性礼包：旧版错误穿戴的礼包在读取时退回未装备，count恢复1；未消费/未过期实例才修复，不复活删除项。

原生btnItemUse→8A8460打开确认窗口并发送1500；确认8C2820发送1420/173字节。现使用该确认消息，核对实例归属、双方UID及操作105，单事务退役礼包、按服务端suit_bundles发放并装备真实部件。保留原绝对到期时间；永久礼包部件仍永久；重复请求不再次发放。没有配置的礼包不消耗。当前仅男女新春兔装具有核对后的部件配置。

不再发送1531，不再将套装本体放入服装槽；真实部件各发送一次2160，后续通过2090刷新，礼包通过2162移除。测试包含数据库持久化、重复请求和回包无重复新增。

随机断线：本地日志19:21:58选择253003，19:22:02已发送8070/8090并收到心跳；客户端19:22:03记录服务器强制断开。检查时游戏服务器和本机MySQL均已停止，无panic日志；无法确定进程停止来源，不能断言随机协议已修复。补充正常shutdown和monitor_stdin关闭日志。需要恢复线下服务后重测。

原生游戏使用确认界面、仓库及外观仍需实测，自动测试不能替代。

## 21:15 实测修正

test001 21:15:55/59 对实例1048590（180021）发送2080/16字节，上一版错误拦截。现kind18的2080入口与1420确认共用消耗事务，2080不伪造1430确认。新购买的count0模板在事务内兼容，回放真实2080路径验证四个部件只新增一次、源礼包消耗且重放不重复发放。
