# OpenKFO-0920 功能合并记录

来源：`E:\功夫小子\OpenKFO-0920`。目标：当前主项目；逐项移植，不覆盖旧版完整文件。

## 已合入

- 幻影卡 77 → 槽 29、个性图标 79 → 槽 40、装饰称号 31 → 槽 42：显式装备与默认槽位共用现有装备事务。
- GM 目录支持上述三种装备，沿用现有发放/上架入口、数量与期限配置，不使用旧脚本统一写死的期限。
- 连接读失败、非法 JSON、处理请求失败的诊断日志。
- 默认槽位以及 GM 发放/上架记录的回归测试。
- 商城读取兼容：252/25、253/25 复用普通武器，10/67 复用宝石/称号，67/67 按药水/幻影卡/称号优先分组，19/19 复用头饰/背饰，10/30 保留原法宝列表并追加未重复的饰品。
- 上述兼容入口复用原商品编号及记录，不写入数据库；已有明确货架优先，即使该货架全部下架也不会启用后备列表。法宝页已有饰品记录（包括下架记录）不会再次通过别的货架引入。
- 255 推荐入口继续只读取原推荐标记；普通分类、全商品目录、购买、GM 编辑及期限配置继续使用原数据。

## 保留主项目实现

- 装备所有权校验、数据库错误区分、装备有效期激活、换装即时刷新。
- 法宝耐久度、推荐商品、价格和服务端期限配置。
- 登录凭据配置、连接凭据绑定及登录错误诊断。
- 2310 沿用现有 MsgItemUnequipped；不引入无调用的 2591 常量或重复的 21411 常量。

## 未并入的运行数据与脚本

- `inject-items.ps1` / `shop-stock.ps1`：已有 GM 发放和上架入口。旧版硬编码本地数据库、统一设置 5256000 分钟/已激活状态，且会影响当前法宝和有效期语义，不作为现有入口的替代品。
- `shop-shelves.sql`：展示需求已转为只读货架映射，不执行旧版删除、复制、强制上架及商品重新编号。67/67 先各取至多 16 个药水/幻影卡/称号，再返回剩余商品。商品不足 16 个时不补造占位商品；当前客户端是否按固定位置分配页签、溢出商品是否可见仍需实机确认，不能据单元测试声称画面已验证。
- `5-恢复初始背包.cmd`：按固定实例编号删除背包，属于数据恢复操作，不属于源码功能合并。
- 旧 EXE、catalog.json、bridge.json、login-autofill.ps1 和库存备份：属于旧运行环境或数据，不覆盖主项目。

本次不修改数据库，不替换运行中程序，不部署。编译和单元测试不能替代客户端商城展示、装备及实网验证。

## 验证

使用独立 MySQL 实例（127.0.0.1:33079）及独立测试库，未连接正式数据库。验证兼容货架保持记录不变、已有/下架货架优先、推荐筛选、无额外销售行，以及沿原编号购买的扣款、限时期限、永久期限、请求重试和价格篡改拒绝。测试实例使用后关闭。
