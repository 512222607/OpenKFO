# 对战、团战、BUFF 二次核对（2026-09-22）

## 本轮修复状态（代码与候选构建，未部署）

以下状态覆盖本文早期核对快照中的“待修复/未实现”。本轮没有修改数据库、线上配置或替换运行中的服务。

| 项目 | 已落地处理 | 验证与边界 |
|---|---|---|
| 开战网络探测 | 4150/4140只等待参战者；超时记录未回包玩家、账号、房间和耗时 | 保留10秒超时及加载屏障；不宣称解决了现场所有迟到回包 |
| 8150 BUFF | 不再把未确认的+67字段强解为浮点；清除仅检查已知字段；操作限定0/1；相同序号不能换内容重放 | 保留266等既有BUFF编号和跨已知角色兼容；无效游戏包仍走审计丢弃、保留连接 |
| 8126效果重复 | 同一发送者/实体/事件的相同序号不能换内容重复生效 | 未交换+47/+55字段；未照搬参考的命中关联队列，消费者证据存在差异 |
| UDP旁路观察 | 增加宠物/法宝、PVE对象、方块、位置、结束、Foster移动/伤害和团队回合观察 | 只在转发至少成功入队给一个接收者后观察；入队不等于客户端已收到。不再追加一份相同TCP广播；碎片/超限数据保持不观察 |
| PVE伤害去重 | 同一发送者/目标的伤害序号在TCP与UDP观察间共享去重；场次与对象重建时清理 | 避免重复更新服务端HP投影，不宣称已替代客户端全部伤害计算 |
| 宠物/法宝消耗 | 记录UDP已入队对象；4201成功扣费后的事件不再重复发给这些对象 | 原库存事务和幂等扣费保留；本轮未做真实数据库扣费验收 |
| 团队多回合 | 增加8294–8297状态、TCP/UDP观察、4111校验及4112结果；服务端重写身份/名字、奖励为零 | 实验开关默认关闭，最多6人、每队最多3人；成员中途离开中止系列；普通团战继续沿用原退出规则。原生结果界面/观战仍待实机验证 |
| 等待房退出/装备刷新 | 保留其余成员准备状态；装备使用3105刷新，不再伪装3090新进房 | 一并包含在候选构建中；新成员真正加入仍按原流程处理 |

配置 `team_series_rounds` 位于游戏 Config，允许 `0/1/3/5/7`，`0`表示关闭，其他值为总局数。没有自动开启，也不改变普通团队生存默认行为；无需数据库迁移。

主要代码：`internal/game/battle.go`、`network_delay.go`、`team_series.go`、`udp_observation.go`、`session.go`、`talisman.go`、`pve_actors.go`、`pve_blocks.go`、`pve_finish_event.go`、`rooms.go`。

验证：Go `test ./...`通过，候选服务器构建成功；针对性测试覆盖BUFF不透明字段/重复序号、观战探测、UDP/PVE去重、队列失败、多回合顺序及畸形结算。需要数据库环境的可选测试不能视作本轮数据库集成验证；尚无客户端实机验收。

候选文件：`E:\功夫小子\server\dist\local-server\kungfu-server-battle-review.exe`。
测试日志：`E:\功夫小子\server\runtime-local\battle-fix-tests.log`。

仍待证据：双武器不能防御的根因；8126完整命中关联；BUFF活动效果账本；8142/8276/8282/8288/8290未知对象生命周期。参考中仅有解码器的编号没有伪造成功回包。未部署、未重启、未提交推送。

## 早期核对快照：范围与证据边界

参考仓库：`E:\功夫小子\protocol-reference-kungfukid-20260922`，提交 `e2c61c4c64000d2a2ac8746233e13fa59d354d3f`。本次读取本地固定提交，未拉取远端新提交。
当前源码：`E:\功夫小子\server\server\go-server`。下表对比源码，不意味着当前运行 EXE 已包含全部工作区改动。16:47 构建的 room-ready-fix 仍未替换运行服务。
以下为实施前的只读核对快照；后续代码修复见文首状态表。参考实现也包含实验策略，不能直接作为原版协议规范。

## 对照表

参考路径相对 `server/kk_local`；当前路径相对 Go 模块。

| 项目 | 参考实现 | 当前实现 | 结论及影响 |
|---|---|---|---|
| 普通团队生存/团队死亡竞赛 | room_flow.py、lab_settlement.py、battle_results.py | internal/game/rooms.go、battle_outcomes.go、settlement.go | 已有准备、加载、按队分组计分和结算，不能说普通团战整个缺失。全员报告不一致时仍会判 unconfirmed，需区分同步异常与结算规则 |
| 团队多回合8294–8297、4111/4112 | team_series.py；room_requests.py按开关创建SeriesProgress；sdp_peer.py观察UDP回合事件 | 无对应完整状态机/结果处理 | 明确缺项。涉及回合间隔/继续/结束、309B结果、最多六名参战者的界面约束，参考也是可选实验且不发经济奖励。不是普通单局团战的同义词 |
| 团队有人退出 | room_lifecycle.py | settlement.go:teamBattleMemberLeft、DepartedSlots | 有意保留本项目行为：双方仍有人继续，一方空了进入报告/结算；不恢复整房中止 |
| 房间退出、换武器提示进房 | 参考complete join清准备，equipment_changed使用3090 | rooms.go本轮修复等待房离开保留Ready；broadcastEquipment改3105 | 已改源码并构建，尚未部署；3105来自当前gfld反汇编，而非照搬参考 |
| 开战4150→4140 | room_flow.py仅向fighters探测、只等待fighters，10秒限制 | network_delay.go对全部Members包括观战者建立pending | 明确差异：观战可能额外阻塞开战。不能将它当作16:53现场超时的已证根因；现场是不同客户端回包缺失/迟到，是否观战还需对应身份记录 |
| 加载/观战 | 参战和观战角色分开，视角屏障 | spectators.go；全部视角加载，参战者初始化 | 已有链路；观战目前限Mode1等待房进入，不支持中途加入战斗 |
| 8120/8121/8122、8127、8140 | battle_relay.py、layouts.py | battle.go、battle_clock.go | 已有移动、伤害、状态、MP、动作转发。8090周期时钟已存在；不等于被动怒气所有客户端都验收通过 |
| 8150 BUFF添加/取消 | layouts.py分解code/有符号parameter/event/opaque；battle_relay.py限制op0/1并维护active_states | battle.go精确87B、上下文/实体/序号校验后转发；没有同等active_states账本 | 不是漏接8150。当前+67按float检查，但本地BuffProtocol也未确认该字段语义、参考保留原始字节，存在误拒风险；清除要求+59..75全零也比参考严格。需用当前客户端消费者/实包决定，不能盲删所有校验 |
| BUFF跨角色 | 参考大多限制自身目标，房主清除需已记录效果 | 已知同房实体跨角色BUFF允许转发并审计；未知实体仍拒绝，保留连接 | 按用户此前要求保留兼容策略；“不掉线”不等于“任何包都转发”。不应照搬参考恢复限制 |
| BUFF操作号/类型边界 | apply1/cancel0、code<=255 | 当前通用分支没有同等明确检查或效果账本 | 差异需补证/测试；应保持透传字段，不制造BUFF时长或重复执行效果 |
| 8126效果回执/破防关联 | battle_receipts.py结合8121、技能目录、一次性命中见证，定向发送；combat_catalog.py记录破防2→4结果 | battle.go通用技能效果转发，没有同等关联队列 | 明确业务策略差异，有重复副作用风险需排查。当前gfld已有+47来源/+55目标证据，与参考命名冲突；不能直接交换字段或照搬定向规则。尚不能证明是双武器不能防御的原因 |
| 武器切换卡4082/4083 | handlers/single_room.py、features/inventory.py | weapon_switch.go；背包主副武器/卡检查、失败也回包释放等待 | 已实现，不是缺少切换成功回包。test002在16:49:06.396发4082，16:49:06.410收到成功4083、剩余97张。双武器不能防御尚未定位 |
| 4201、8291/8292 | talisman.py及sdp_peer.py | talisman.go等 | 宠物/法宝相关使用链；旧protocol-audit文档将其列为武器切换不准确，武器切换应看4082/4083 |
| UDP旁路观察 | sdp_peer.py除8143，还观察8291/8292、20400/1/3/4/5/7、Foster8120/8121、8294–8297 | session.go:relayDatagram转发后仅observePeerProbe及observeRelayedPairSelection | 明确缺少对应UDP旁路状态观察。不能简单重复转发一次TCP；否则可能重复扣费/生成/伤害。需按实际成功投递对象和场次做幂等观察 |
| 9000–9002、9500–9502拾取 | battle_receipts.py预约/房主回执 | reliable_event.go | 已有预约、房主定向回执和释放处理 |
| 8278/8286死亡通知、8143/8144配对、8287掉落、8400–8404投射物 | battle_receipts.py、battle_pairs.py、battle_relay.py | battle_death.go、battle_extended.go | 已有对应处理；枚举识别和完整玩法仍需区分 |
| 8142/8276/8282/8288/8290等 | 部分仅layouts解码，无普通relay放行 | 无完整对象生命周期 | 不是参考里出现编号就能复制接通；维持待证据状态 |

## 优先顺序

1. 补齐诊断：恢复服务器逐包日志，并覆盖UDP动作投递结果；定位test002防御失效的具体动作/阶段，不用切换成功包替代防御证据。
2. 核对并修复BUFF未确认字段误校验、清除及8126副作用；保持不因游戏包验证告警断开玩家的约定。
3. 对齐参战者网络探测范围；保留全视角加载屏障，不以跳过超时直接强行开战。
4. 补齐UDP旁路状态观察，先宠物与PVE，再独立实现团队多回合；避免在单局团队模式中混入实验多回合规则。

## 现场记录

桥接日志 `client/launcher-components/shared/online-client.log`：16:53:00四客户端收到4150，test003/test001/test004及时回4140，test002到16:53:21才回；16:53:26再次探测，test002/test004/test001及时回，十秒内未见test003回4140。因此确认是等待4140超时，不能直接归因为互联网网络延迟。服务端protocol-20260922-145612.log当前为空；现有桥接记录未包含逐包UDP动作，16:49:23的防御失败仍不能下根因结论。

## 重新进房取消准备修复
现场日志17:33:42：test002请求3070后，服务端向三人下发4070。参考room_lifecycle.py同样清空原成员准备状态。本轮completeRoomJoin改为只取消旧网络探测/换位请求，保留原成员Ready，并在3090后广播4050同步已准备成员；新进入者仍未准备。Go全套测试及候选构建通过，候选kungfu-server-rejoin-fix.exe尚未替换运行服务。此修复覆盖前述真正加入仍清准备的旧行为。
