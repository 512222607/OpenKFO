# 2026-09-21 房间断线与宠物显示

## 已修复：房主报告其他玩家的场景交互被误踢

线上 11:37:26，UID 10001 发送 8071/8140，103 字节；信封发送者为 10001，+39 的目标是同房 UID 10011，+47=10、+51=4、+55=4002，+95/+99 为当前房间与战局。旧代码将 +39 一律限制为发送者控制的角色，返回 `battle actor id=8140 actor=10011 expected=10001`，随后 `session_rejected`、断线。不是网络超时。

当前客户端 SHA256 `98c43be72ac7600b368d4e185d75205376f79e938ea42e4b16ce8f8c4bae827b` 的 `98AFF0` 先通过 `44B3F0` 检查本地控制者，再遍历八个角色。`98B103/98B20D` 构造类型 10、操作 3/4 的场景物件交互，目标 +39 来自被遍历角色。`82BB10` 消费者按 +39 找角色、+55 找物件，并执行相应交互。证据在 `protocol/nixiang/talisman/room-pet-and-action-evidence.asm`。

修复只允许当前房主报告这两个操作，目标必须仍是同房成员；保留信封身份、房间/战局、包长、有限浮点和去重校验，其他跨玩家动作规则不放宽。非控制者的这类消息忽略且不转发。这仍是原生场景控制者的认证转发，不是新增服务器物件物理模拟。

验证：game/protocol 包测试通过；新增测试覆盖两类交互原样转发、收件人隔离、重复、非控制者、伪造发送者、房外目标、旧战局和无关动作拒绝。Linux 服务已于 11:52 部署，运行文件 SHA256 `2189439ffd59703fb1405a0d974111603eec8d90a9fb23239f4ea30046549bc1`；备份 `/opt/kungfu-go/before-room-action-20260921-115223`。配置和证书校验不变，三次加密探测成功。真实对局仍需复测。

## 已部署：饰品1隐藏卸下的宠物（待游戏复测）

用户截图确认选择窗口只有卸下按钮，无宠物名字和图标。PID 76540 的 profile 名称为 qqq；只读库存和 type30/slot37 UI 候选数组均包含实例1048588、物品303118，slot0、state1、quota10000。候选名称为年兽宝宝、图标路径为 Picture\ItemIcon\303118.png，page1。服务器此前同时投影 duration525600（+13）和 quota10000（+23）。

原生 8AB7D5..8AB827 将候选 state(+28)、duration(+32)、quota(+20) 交给 A1EC20。A1EC5B..A1EDA8 在 duration 和 quota 同时非零时生成空字符串；8AB905 检查空字符串，8AB983/8ABA11 隐藏图标和名字。因此候选虽在本地数组，也显示为空。

修复 projectItemMinutes：type30 且 quota>0 时仅投影 quota，将显示时间归零；真实 inventory_expirations 不变，到期判断仍在投影前执行，过期仍改 state2 并卸下。普通武器保留永久/限时显示。持久化、game、protocol 测试通过，覆盖卸下、限时到期和普通武器兼容。未执行数据库清空或批量更新。

11:56:08 的未处理21311已通过7F0240发送、A28F80接收21312、7EFF78的DailyActivity.sui/frmDailyActivity确认属于每日活跃界面，不是饰品列表接口。

线上已于12:04:48部署，运行文件 SHA256 `9b6f6a53c2e47b4af260a22446d28acf96366d151d9f91df381569de5f263db3`，PID60710；健康检查、运行文件、配置和证书哈希校验通过。备份 `/opt/kungfu-go/before-pet-list-20260921-120448`。尚需重新登录后的实际饰品窗口复测。

用户另报离线后房间出现两个qqq；截图不足以判断服务端残留还是客户端模型残留，尚未修改恢复房间逻辑。

## 未修复：房间宠物预览

服务端 `fighter` 包含装备槽37/38，3090向本人及其他房间成员广播。当前宠物外观由战斗装备状态的 Fairy 特效生成，房间预览与战斗初始化不同，既有证据见 `protocol/PetProtocol.md`。`99AE70` 还依赖战斗世界/状态对象，且本人路径发送8291及4201费用请求，因此不能直接调用它作为房间预览修复。尚未确认能单独创建、更新和销毁房间宠物特效的安全客户端入口；本次没有部署猜测性的客户端补丁。

## 期限显示修正（取代12:04的服务器规避方案）

用户实测12:04版本使用宠物后显示10000个。原因是将duration清零虽然避免空列表，却进入A1EC20的数量显示分支；该值是100点真实耐久，不能改成1或清零。

恢复projectItemMinutes的原期限投影，保留+23耐久。登录器内嵌桥接程序在创建已验证客户端的挂起进程后，将A1EC5F的6字节 `0F 85 20 01 00 00` 替换为NOP；这是跳过“有quota就不看duration”的分支。后续A1EC65的duration==0判断保持原字节，仍将纯数量物品送入原数量分支。两字段都有时优先显示期限；原小时/分钟、365+阈值及过期分支不变。不会写库存、修改战斗耐久消费者或改写磁盘gfld.dat。

内存写入使用现有精确原字节校验、页保护恢复和FlushInstructionCache。原字节不符会中止该新建客户端，避免对未知版本盲写。范围为当前shared_client启动路径，需完全退出游戏并更新登录器后重新启动；已运行的旧客户端不会被热补丁修改。

验证：bridge/persistence/game/protocol测试通过；补丁错误原字节拒绝及正确写入测试通过；原PE分支字节核验；永久/有限期限及到期仍保留真实quota10000的测试通过；.NET8登录器发布构建成功。没有自动化游戏窗口验收，仍需用户实际复测。

已发布登录器2026.09.21-pet-duration-1，公网HTTPS清单与本地manifest一致（curl Mozilla UA；Python默认UA被403拒绝）。服务12:36:57部署，PID61119，SHA256 dc3cd12e8ab9fd99a89380861279044cc446f6c877a80f8b84922ae7437f022b，备份/opt/kungfu-go/before-pet-duration-20260921-123657；健康、运行文件、配置证书校验通过。
