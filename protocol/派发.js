# 全谱字典 · 派发

> **本册是《全谱字典》的分册之一**，总目与检索入口见 `索引.js`。
> 范围：opcode 40000 及派发层阶段路由白名单（跨册通则）
> 维护：册内按 opcode 升序；软上限 60KB / 硬上限 100KB，超限按子簇或 opcode 半段拆 `-a` / `-b`（只改索引一行，其余册不动）。

---

## 十、其他（40000）

| opcode | handler    | RVA        | 方向       | 状态 | 阶段放行  | 字段布局 | 触发条件                        | 读全局 | 写全局 | 关键子调用 | 关联包   | 冲突 | 说明 |
| ------ | ---------- | ---------- | -------- | -- | ----- | ---- | --------------------------- | --- | --- | ----- | ----- | -- | -- |
| 40000  | 0x00a2db00 | 0x0062db00 | 下行(recv) |✅| 房间/战斗 | 0x8:D | dword_17C8714!=0;dword_17C8708!=0 | dword_17C8708,dword_17C8714 | dword_17C8714=;dword_17C8964=(经sub_A55FB0);dword_17C89AC=(经sub_990070);dword_17C89E4=(经sub_94BF10);dword_17C8A38=(经sub_404810);dword_17C8A90=(经sub_8C5FD0);…共12项 | sub_81D5C0;sub_912700 | ↔3201(共享符号 unk_568F10等5,唯一候选) | ⚠️写写 dword_17C8964↔3115,4125; ⚠️写写 dword_17C8A38↔1030,1230; 另4项 | 写 dword_17C8714；弹文案窗：“临时网络故障，请稍候再试！”；深层线索：“rbpReady”、“rbpStart”、“\data\config\mapmgr.xml”。〔深读：手读 h_A2DB00.c 核证——`__thiscall(this,a1,a2)`；`v6=(*a1>0) && dword_17C8714!=0`；`if(dword_17C8708!=0) sub_81D5C0(dword_17C8708)`（含 rbpReady/rbpStart 控件重置），`(*vtable+8)(dword_17C8714)`+`unk_63E840/unk_63E780` 收尾后 `dword_17C8714=0`（清零房间/战斗上下文），弹 frmMessageBox(unk_B9D11C="临时网络故障，请稍候再试！")，回调 `sub_912700`（`sub_404810` 懒加载 dword_17C8A38、`sub_8C5FD0` 懒加载 dword_17C8A90 等做房间/战斗失败清理）。注：sub_81D5C0 头部有 "bad sp value" 编译器警告，按上下文（rbp 重置/会话清理）解读，不依赖精确栈帧。〕|

---


## 十一、派发层阶段路由白名单（✅ 已完成）

**派发点**：`sub_98FD60` 内部 `0x0098FDB6`（ebp+0xC=msgId, ebp+0x10=buf, ebp+0x14=size）

**已完成**：白名单数据已逆向，来源 `dispatch_probe_run20e.log` + `msgmap_dump_run21b.log`，由 `out_C/S7_stage_fill.py` 一次性回填至主表 §一~§十 每行的「阶段放行」列。

**阶段放行取值含义**（见主表「阶段放行」列）：
- `登录` / `选角` / `大厅` / `房间` / `战斗` / `角色` / `战斗/世界` — 单阶段放行
- `房间/战斗` / `共享(非登录)` — 多阶段放行
- 空 — 未分析

**关键发现**：
- 2591 阶段放行=`登录` → 房间阶段被拦截（印证 Run241b 注释铁证）
- 1151 阶段放行=`登录/选角` → 登录/选角阶段放行（纠偏：B3_model_flow_full 坐实 sub_823420 只注册登录/选角表 0xbcb1a0 idx14；房间表 0xbcb694(23条) 无 1151→房间阶段经 sub_81F690→sub_81DF80 派发链被静默丢弃，sub_823420 从不被调。「到达派发链」≠「被 handler 处理」。主表 1151 行阶段放行已同步修正为登录/选角。）

---
