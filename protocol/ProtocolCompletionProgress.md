## 2026-09-20 踢人拒绝反馈与统一日志

3140/9B新增具名RoomKickRequest（TargetUID、ClientFlag）。当前PE的8062F0从本玩家481080读取对象+107C；41B670写该字节，822E30从37B数据+32写入，A27330在匹配本玩家UID及若干状态值后置1。证据见nixiang/room-kick-flag.asm。该链不证明非零值是操作枚举或服务器权限，不将1当成移交房主；相关数据处理器的线上包号不在本次重新映射范围。

补齐已实现踢人的拒绝交互：错误长度、非房主、非等待阶段、目标离开、踢自己、过期会话及不支持的非零标志返回20150明确原因，不移除玩家且不因这些拒绝断线。成功仍按已验证0路径广播3150并移除唯一目标。服务器日志统一增加请求目标/原始标志及自身215B通知的中文content；C#原有接收器直接展示，HEX保持完整，无额外网络接口。

Go全量测试通过，包括既有成功踢人及新增拒绝隔离、短包/长包、保留原始标志、中文日志与畸形通知不解码测试。此项不是主动移交、锁位或观战的实现；这些请求仍需进一步证据。

## 2026-09-20 分步事件边界与GM诊断复测

当前PE核实96A1A0→4C52D0→45C6B0为本地事件入队，保留0–6/21–24/10/27等内部事件范围，不将其作为网络包号。完整有界提取及结论写入new-player-guide/README.md和step-event-queue.asm；全体虚函数及其他封装未覆盖，不宣称所有步骤均无上行。整体4124完成处理保持已有实现，没有猜测分步完成协议。

此前未完成的Flutter用例，本次未改动代码、按文件加诊断输出复测：training_config_test.dart 2项通过，talisman_config_test.dart 3项通过，均退出0。日志保存在本机dist/training-test-diagnostic.log和dist/talisman-test-diagnostic.log。只能证明这次独立运行通过，不将间歇性未完成归因于未经验证的原因，也不宣称全量稳定性已解决。

## 2026-09-20 新手引导与扩展任务联动

依据 nixiang/new-player-guide 的4124完成入口与 nixiang/tasks 的141B列表/条件槽及状态通知，新增显式配置事件 `tutorial_complete`。这是模拟器的服务器事件绑定，不把客户端条件编号0直接解释为完成引导，也不接收客户端自行提交的完成状态作为依据。

首次有效引导完成在奖励、角色进度、完成回执的同一事务中推进已接取且版本匹配的任务；复用战斗任务的条件累计与完成判断。未接取任务不自动接取，旧完成账号不追溯计数，不修改现有任务配置。GM事件选项明确“首次完成新手引导（需提前接取）”。若初始流程未提前接取该任务，本绑定不会自动生效；逐步走/跳/攻击教学事件仍未恢复，不能把整体完成当作这些步骤的替代。

返回大厅后复用6041/6042完整列表及6031/6032状态通知；任务奖励继续经6311/6312的资格校验和幂等领取，不随事件直接发奖，不发送已发现原生循环缺陷的20530。

独立数据库TestTutorialBundleLocalDatabase通过8.20s：已接任务达到条件、未达目标保持接取态、未接取保持不变、重复完成不累计、任务版本错误时奖励回滚。TestTutorialCompletionLocalDatabase通过8.18s；Go全量测试通过。GM两项扩展任务测试通过，Windows Release构建成功并替换原dist/GM管理器；Go服务器重新构建并替换本地运行文件。本次没有重置玩家，也未修改线上配置。

## 2026-09-20 构建与验证收尾

.NET 自包含 WinForms EXE 已部署到 dist/local-server/功夫小子本地服务器.exe，包含运行时；Go 游戏服务独立运行。原生控件、文件追加/轮转、UTF-8半行、详情/原始数据共12项自测通过；实际独立测试服启动、重复启动保护、健康检查、正常停止通过。重新构建的Go EXE与部署文件哈希相同。

Go `test ./...` 通过；新手引导两项独立数据库测试（包含原生选择与重登恢复）再次通过。GM奖励选择、批量价格、VIP和主界面分别运行通过；全量Flutter测试仍有若干 `did not complete`，其中法宝配置与训练配置单独运行也出现测试未完成，没有断言失败详情，不能记录为全量通过。原生游戏中新手奖励图片及选择仍待实测；协议自动测试不替代画面验证。

## 2026-09-20 关卡变更主动同步

21370成功后，当前会话订阅自身关卡投影。复用15秒安全阶段刷新，在大厅或等待房更新21372→21373，未变化不重复发，战斗/加载/结算期间不推送，重登与频道切换清理订阅。GM撤销后下发既有`0,`标记清空客户端可选项，不使用未完整命名的21374额外提示。

StagePlayerView同时返回已授权地图与强制开放集合，取自同一个REPEATABLE READ事务；移除游戏层第二次读取策略，避免混合两个版本。独立库TestStagePlayerUnlocksLocalDatabase通过7.976s；真实TLS TestStageGateTLS通过18.016s，直接调用与定时器相同的刷新入口，未上行新21370就收到开放/撤销；重复刷新不发相同名单。原生PVE通关不在此验证中。

## 关卡目录先发与授权撤销自动回归

当前服务器对已配置的21370查询先发送21372目录记录，再发送21373可选名单。当前PE的RoomSet在7F876D找不到目标地图记录时会提前返回，跳过后面的按钮禁用，因此目录包含未授权地图，而不只包含允许地图。21372中间四字段仍未解，保持零；末字段使用本服明确的无匹配前置地图标记0xffffffff，避免0误匹配真实随机地图。此策略不代表恢复了原版前置条件或通关规则，条件标签和原生界面仍需验收。

TestStageGateTLS独立debug库实际通过35.015s：两账号目录先于名单、授权隔离、错长提示、已准备玩家撤销授权后名单清除与房主开战拒绝、恢复授权后双方收到4080。临时规则与账号自动清理。测试器补充21370当前链路及前置地图标记说明；普通game/protocol/protocol-tester测试通过。候选服务器与测试器构建，未替换运行服；不代表完整PVE玩法已实现。

## 21370查询／21373名单网络接入

追控件输入链确认+56参与事件与交互门，新增专用21373空名单标记`0,`以进入原生逐项不匹配分支；普通StageProgress仍拒绝0地图，标记不进入数据库，混合标记拒绝。404B首字段采用本服保留0，不宣称恢复原版含义。详细当前PE证据与局限见room-management/README.md。

游戏大厅/房间21370空请求读取StagePlayerView，并与已实现模式/容量的地图池相交后发21373；未配置/读取失败/错长仅提示，不靠未知相邻包占位。TestStageGateTLS实际通过32.612s，新增双账号不同名单、空清除标记、错长不掉线、授权后重新查询，保留原有称号/关闭/全员开战检查；临时数据清理，辅助SSH关闭。普通protocol/game/protocol-tester通过，候选服务器及测试器Core已构建。没有替换运行服务器，原生画面和自动通关仍未完成。

## 玩家PVE准入名单一致性查询

新增StagePlayerView，在显式REPEATABLE READ只读事务中读取真实账号资料、当前目录和本版本个人授权；区分未配置与已配置空名单，返回目录/授权版本。只遍历PVE目录，复用AllowsPlayer合并称号、关闭地图、条件总开关及个人授权规则；房间准入也调用该规则。查询结果是策略允许集合，仍需游戏层与已实现地图池相交，不能因此开放尚未实现的PVE玩法。

独立debug临时表TestStagePlayerUnlocksLocalDatabase含新增投影验证实际通过12.355s：未授权空名单、授权后地图及版本、错误客户端指纹拒绝、撤销后更新为空；既有事务回滚及隔离继续通过。TestStageGateLocalDatabase通过10.764s，纯规则交集测试通过，候选服务器已构建；辅助SSH关闭，没有改运行规则或替换运行服。21373首字段及原生空列表行为仍未作为已解决事项，查询尚未接入下行。

## PVE目录随服务器关卡配置持久化

StageAccess新增pve_maps，绑定既有客户端指纹，要求全部存在于地图条件目录且编号唯一，并通过404字节进度编码容量检查；旧GM省略该字段时保留同版本原目录，显式空数组可清空。保留后再次验证，避免删掉所引用地图时产生悬空配置。沿用配置版本比较及同事务审计。

GM导入读取附加PVE选择，按MapID去重排序，保留原称号/个人条件，仅生成待保存草稿，不自动启用。总开关改名“启用地图准入条件”，明确同时控制称号和个人解锁。四项页面测试通过，普通persistence/game测试通过；独立debug临时表TestStageAccessLocalDatabase实际通过10.979s，验证持久化、旧GM保留、显式清空以及既有冲突/回滚。辅助SSH关闭，候选服务器及GM后端已构建。尚未下发个人进度或开放自动通关。

## PVE与地图条件目录交叉核验

真实客户端50个PVE选择均能按MapID关联96条MapInfo目录，但前者MapType10/21、后者110，并非同一枚举。初次相等假设失败后，按实际资源保留两种类型，禁止机械转换；当前固定客户端测试验证该关联和差异。

新增ReadStageCatalogue单次读归档并解析两份目录，返回统一指纹，拒绝PVE引用缺失地图；GM stage_requirements保持原maps字段并附加pve_maps，为后续正确生成进度列表提供数据。真实归档TestStage系列通过0.155s，文件哈希不变；未加入额外轮询通知，通用提示不能替代原生列表同步。个人通关/初始解锁规则与首字段仍待确认。

## 个人关卡授权真实TLS及旧GM保存兼容

StageTitleRequirement.unlock_required改为可选布尔；保存相同目录时，缺字段/null保留原条件，显式false才关闭，避免旧GM无意移除已开启的个人准入要求。与原版本比较、配置保存和审计在同一事务处理。TestStageAccessLocalDatabase实际通过8.423s，验证旧字段省略保留true、显式false可关闭，并回归配置冲突与审计回滚。

TestStageGateTLS实际通过29.753s：两临时账号完整TLS登录/建房/进房/准备，既有称号与全局关闭检查之后，通过Store.Admin真实读写个人授权；只授权房主时4030拒绝且双方均无4080，补齐同房成员授权后双方收到4080。关卡配置、个人授权及审计均连接级临时表，测试账号清理，辅助SSH关闭。此测试不执行原生加载/战斗画面，不证明个人进度UI已经同步。

普通persistence/game/protocol-tester测试通过，候选服务器和GM Go后端重新构建，运行服未替换。个人自动通关和进度报文仍未完成。

## 个人关卡解锁管理接口与GM页面

stage_unlocks_get/save接通存储管理、桌面本地/线上转发及已有HTTPS鉴权。读取确认账号存在并使用当前目录指纹；保存要求外层UID与授权集合UID一致，缺载荷拒绝，继续复用版本校验、目录检查与审计事务。HTTP401/错误环境403/合法请求字段保留测试及持久层错账号拒绝测试通过，desktop/adminhttp/persistence普通回归通过。

GM新增选中账号的“个人关卡解锁”页面，加载目录与授权时核对UID/指纹/版本/地图集合；读失败禁保存，列表虚拟滚动，保存冲突保留草稿和原版本，成功更新版本。地图条件列表增加“要求个人解锁”选项，仍由原关卡条件总开关控制。页面明确这是进入授权，不是通关，不发奖励，尚不更新原生解锁显示。

个人解锁2项及原关卡4项Flutter测试全部通过，三文件静态分析无问题。候选服务器与GM Go后端构建成功；运行服未替换，dist Flutter界面仍未覆盖。自动通关授权与21373/21374完整同步仍待接入，不能把管理授权等同完成PVE系统。

## 个人关卡解锁存储与服务端准入

新增stage_player_unlocks，按UID及客户端目录指纹保存版本化MapID集合，并新增审计表。SaveStagePlayerUnlocks锁账号和关卡目录，只接受当前目录中的地图，版本冲突拒绝，解锁替换与审计同事务；它表示显式授权，不伪装通关记录。关卡规则新增unlock_required，仅随现有关卡条件总开关启用，默认无影响。

stageGate在已有称号与地图开关之外检查所有房间玩家的独立授权，任意成员缺失即拒绝；继续复用建房、加入、随机地图、改设置和开战的现有准入入口。没有把客户端按钮当授权，没有发送未知21373首字段，也没有修改当前运行规则。

独立debug连接级临时表实测：TestStagePlayerUnlocksLocalDatabase通过7.582s，验证持久化、重复旧版本、跨账号/版本隔离、未知地图及账号拒绝、审计失败回滚、撤销；TestStageGateLocalDatabase通过9.512s，包含原有房间路径与新增个人解锁/房主不可代替成员/撤销后立即拒绝检查。普通persistence/game测试通过。辅助SSH关闭；管理API、GM个人编辑、自动通关授权及客户端进度下发仍待接入，不宣称关卡系统完成。

## 关卡难度列表键来源闭合

新增当前PE完整A54390及字符串证据pve-entry-loader.asm，确认PVE配置MapID→记录+0C→9290F3列表匹配，SelectDifficulty→+8→默认选择；Group、难度和地图ID不能互换。同步协议结构注释及房间逆向文档。首DWORD仍未知，区域扫描不作为全程序未读取的证明；个人通关/解锁业务没有开放，也没有把客户端展示奖励直接作为服务器发奖规则。

## 关卡进度客户端副作用继续核实

只读提取当前PE的8个控件函数至room-management/difficulty-controls.asm，核对21373地图匹配经7603D0写控件+56、显示路径7611C0写+54，首次匹配更新默认难度。空列表会跳过前者，不能当作可靠撤销解锁；21374匹配地图会反复提示开启，不能用作静默完整状态同步。首DWORD与个人通关规则未确认，不开放猜测进度业务。

独立测试器加入空21373及21374通知副作用诊断，定向边界/终止符/兑换分类/副作用测试通过0.145s，Core已构建。原生点击/渲染语义仍待深入，未改客户端或运行服务器。

## VIP赠送真实TLS及普通赠送回归

新增TestVIPGiftTLS复用完整双账号赠送/邮件测试，独立debug数据库临时VIP规则与测试账号实际通过27.365s：有效黄金会员77点券商品按80比例扣61；在线收件人收到1310，查询详情后2171领取与1340删除连续请求正常，重复附件领取不再发物品。会员期限设为过去后主动执行现有库存到期刷新，验证2161/2121/1038清身份及折扣；旧61报价赠送只返回20150提示，余额维持139、赠送回执仍恰好一条且ping存活。此为协议自动验证，不是原生画面或自然等待期限测试。

既有TestGiftTLS离线/在线两场景实际回归通过37.743s；统一使用连接级临时VIP规则，避免受操作者已保存配置影响。临时账号及商品依原夹具清理，辅助SSH已停止。同步VIP逆向文档顶部当前状态，保留旧研究阶段文字并标注历史。未修改运行配置、未替换服务器，本轮测试代码无需重复构建未改动的游戏服务器。

## VIP非零折扣购买真实TLS验证

TestVIPTLS与TestVIPShopTLS合并实际通过42.773s，使用独立debug数据库临时商城/折扣/购买表及本次创建的临时账号。验证登录1038比例70、101点券商品按70扣款，改为50后旧报价拒绝且同步1038、新报价扣50；到期通知2161/2121/1038降至非会员及比例0，旧折扣价拒绝，原价101成功。重连核对非会员身份、库存数量与三条购买回执，最终余额779。测试账号清理完成，辅助SSH已停止。

首轮断言误把异步到期通知算入购买应答而失败；修正为先完整验证到期通知流，再验证购买流后通过，不屏蔽未知包。此测试未执行原生游戏画面，也未验证赠送的非零折扣完整TLS；赠送事务已有独立库验证。GM发卡说明改为折扣由独立配置决定，发卡不会自动开启折扣；运行服及dist界面未替换。

GM会员与折扣页五项Flutter测试通过，Windows Release重新构建通过24.8s，包含最新发卡与折扣说明；完整运行目录位于E:/kk-item-manager-build-20260917/build/windows/x64/runner/Release。本轮没有覆盖dist，不能将该构建当作运行中GM已更新。

## VIP购买／赠送权威定价接入

既有TestVIPTLS身份/到期真实网络回归通过15.819s；该场景为零额外折扣，不替代非零折扣购买TLS。辅助SSH已关闭，测试临时账号已清理。

新增vipShopPercentTx：购买/赠送锁账号后读取并锁定折扣规则，按数据库会员卡及UTC期限确定档位；不信任请求自报会员身份。Purchase及Gift在各自交易事务内重算应付金额，校验请求报价，旧价格或过期会员折扣拒绝，不扣费。已完成同操作回执先于重新定价读取，改价/过期后的重传不二次收费。购买同时补商品键、发货类型/道具ID及未装备槽一致性校验。

登录VIP账号将支付比例写入1038+20；商城列表、购买、赠送操作前刷新比例，有变化才发1038；库存投影切换会员档时先清旧比例，下一商城操作重新获取。未知权益仍零。折扣配置默认缺失/关闭时保持原价，不启用推荐优惠叠加。当前管理源码说明已更新，dist界面未覆盖。

TestVIPShopPurchaseLocalDatabase在独立debug连接级临时表实际通过32.471s：非会员0比例，VIP八折77→61、改五折77→38、旧报价拒绝、旧成功回执重放不扣款，过期恢复77，赠送61/过期77及重放、金币100/八折80。临时DDL首轮LIKE同名表失败、第二轮夹具商品键不匹配，修正后最终通过；未触及现有玩家/商城规则。普通persistence/game/protocol-tester及1038字段/到期清比例测试通过，候选服务器已构建，运行服未替换。非零折扣完整TLS、GM改价时客户端刷新和高价商品原生显示仍需继续验证。

## GM增加VIP商城折扣页

Windows Release构建已通过（24.4s），输出E:/kk-item-manager-build-20260917/build/windows/x64/runner/Release。此为构建结果，不是dist交付或游戏折扣生效证明。

同一GM侧栏新增VIP商城折扣，使用当前本地/线上环境API，不要求选择玩家；明确作用于该环境全部VIP。编辑三档支付比例0至100及总开关，0无额外折扣、80八折，关闭保留数值。读取异常或非法服务端配置禁保存，保存携带revision，冲突保留草稿与旧版本，成功更新版本。页面明确当前只保存配置，游戏显示/扣款尚未接入，未宣称生效。

三项Flutter测试实际通过：环境标题/正确API及完整版本字段，非法101拒绝且不调用保存，0合法、关闭保留其他档，成功后版本推进；保存冲突两次保留草稿/原版本；失败及非法读取禁保存。定向静态分析通过。未保存任何运行规则。dist界面仍未覆盖（此前目录复制被自动审批拒绝）；Windows构建结果按本轮工具输出核对。

## VIP商城折扣配置基础及管理API

已核对普通购买发送器对金币/点券分别乘1038+20后整除100；支付比例0无额外折扣、80为八折。新增VIPShopSettings分白银/黄金/铂金配置0至100，开关关闭保留数值；版本校验、保存和审计同事务。vip_shop_settings_get/save贯通桌面环境转发和HTTP鉴权，未开放匿名访问。纯价格函数拒绝原生有符号DWORD乘法溢出及付费商品折后为0，尚未用于游戏收费。

独立debug临时表TestVIPShopSettingsLocalDatabase实际通过4.175s；默认关闭、版本冲突、审计失败回滚、关闭保留、损坏配置拒绝。价格边界、桌面字段传递、HTTP未授权401/错误环境403/成功200及缺失配置拒绝通过。候选服务器、GM后端构建成功，辅助SSH已关闭，未改运行规则或替换服务器。

下一步仍需GM可视编辑、游戏1038比例同步及购买/赠送事务内权威定价、会员到期/改价后的旧报价拒绝和完整TLS。当前配置API不等于折扣已经生效；1038仍为零额外折扣。

## 基础、每日、新手任务完整双账号对战TLS验证

TestTasksTLS扩展并实际通过64.446s：临时两账号经证书校验TLS登录/P2P、接取基础/每日/新手任务、建入房/准备/资源加载/开始、两局相反胜负的4110结算、3550回房、领奖、重复领取及注销重连。每日/新手条件为两场对战且一胜，第一局状态2，第二局状态4；未接任务的另一账号始终状态1。任务进度没有SQL置完成，由实际结算事务累计。重复战报不加计数。

每名玩家每次回房均收到6041/6042完整列表；仅接取者第二局在列表之后收到6031/6032各一次。随后查询不重复提示。基础奖励仍按原链发放，每日/新手各通过6311/6312领取经验5、金币7、一件限时商品，验证4300/1240/2160/6301或6302；重复请求只提示。重登后余额正确、每日已领3、新手空列表，库存相较初始恰好新增3件（基础/每日/新手各一件），基础回执只有一条。临时账号及关联数据清理、临时规则/结算表随连接关闭，辅助SSH已停止。

GM说明纠正为新版服务器支持事件计数/领奖，明确UTC每日周期、接取快照、关闭行为、商品规格及客户端奖励文字差异；基础任务开关更名避免误认成三类总开关。两个页面测试文件通过，此轮UI说明仅更新源码，未更新dist界面。测试改动不需要重复构建未改变的服务器。该完整TLS测试不执行原生游戏画面，也不代表训练/PVE等未绑定条件已完成。

## 每日／新手任务物品事务与完成通知

扩展TestExtendedTaskActionsTLS实际通过54.186s：两类状态4查询均验证6041/6042完整列表先于6031/6032，重复查询只通知一次，随后领奖/重复拒领/余额/隐藏逻辑继续通过。完成状态由测试夹具写入，不冒充自然战斗触发；两条回房调用路径由静态调用链及普通回归覆盖，完整TLS回房通知还待验证。辅助SSH已关闭，临时账号清理完成。

新增TestExtendedTaskItemClaimLocalDatabase，独立debug连接级临时表实际通过11.492s：接取后修改GM奖励仍按冻结规则发放；关闭商品拒领；库存插入后期限表主键冲突时库存、经验、金币、任务状态完整回滚；重新领取成功发1件限时物品、经验50、金币500；期限从领取时计算，重复领取及删除物品后再领均不恢复道具，另一账号不变。

依据extended-task-actions.asm中6031/6032消费者接入任务完成通知，先发两个完整6041/6042列表，再发WORD任务键+BYTE状态4。按登录会话内任务键、客户端指纹、周期去重；重登清通知缓存，新每日周期允许再次通知。3550原地回房及3110兼容回房均在安全房间阶段刷新任务，错误只提示，不因此断开。未绑定客户端版本的服务器不发布模板任务键。纯函数测试覆盖两类通知字段、未完成/已领取过滤、重复记录/重复刷新去重及新周期；既有回房库存测试回归通过。

候选服务器与测试器Core已构建，运行服未替换。此次物品测试是存储事务验证，完成通知的原生视觉尚未验证，完整战斗TLS任务链仍待补。

## 每日／新手任务领奖事务与网络入口

新增ClaimExtendedTask，按认证UID、服务器客户端指纹、当前周期和已启用任务读取冻结规则。仅状态4且全部非空条件已有事件绑定并达到计数才可领取；经验/金币/商品物品、进度状态3同事务提交。原进度主键(uid,task_key,cycle)及状态3作为持久领取凭据，重复请求不再次发奖。取消、接取仍不能重置已完成任务。原生请求上下文和尾部不作为认证、奖励数值或操作ID。

6311/6312通过既有19B解析接入游戏，成功下发4300、1240、可选2160，再用已确认消费者的3B最小字段发送6301/6302状态3。失败发提示保留连接。独立测试器增加每日/新手接取、放弃、领奖六个入口；未采用风险20530路径。原生面板奖励文字仍来自客户端模板，GM自定义数值的显示一致性尚未解决。

TestTaskCountersSettlementLocalDatabase实际通过38.218s：自然结算累计至完成、未达成/错误版本/类别/账号/键拒绝、金币溢出保留状态4、经验50金币500单次发放、重复领取不变、新手已领取隐藏。首轮夹具遗漏Normalized成长表导致失败，修正后才通过。TestExtendedTaskActionsTLS实际通过44.380s：真实认证/P2P/TLS、两类未完成拒领、成功三包及精确通知字段、重复拒领、余额仅增加40、每日状态3/新手移除及ping存活。TLS夹具直接设置完成进度，不能当作完整TLS战斗达成链证明；自然结算由独立存储测试覆盖。

临时表/账号清理，辅助SSH关闭。普通persistence/game/protocol-tester测试通过；候选服务器及测试器Core构建成功。运行服务器未替换，自动完成通知、物品发放专门回归、原生界面及完整战斗TLS仍待继续。

## 每日／新手任务战斗计数接入结算

Windows GM构建成功，产物位于E:/kk-item-manager-build-20260917/build/windows/x64/runner/Release。将完整运行目录复制到dist/GM管理器的操作被自动审批以blocked by policy拒绝，未执行，因此dist内GM界面仍是旧版；该目录的Go管理后端已单独构建成功。不可把界面构建完成当作dist交付完成。

当前结算将服务器配置的客户端指纹传入任务查询，复用当前周期、启用规则及接取时冻结的事件绑定；仅已接任务累计有效基础模式的场次/胜场，达到全部条件进入状态4。条件编号不是全局事件编号，未绑定条件不自动完成。GM每日／新手编辑支持逐条件选择服务器事件，重新导入相同条件保留绑定，编辑草稿不修改原配置。

任务计数与战斗奖励使用同一事务及已有结算回执；重复结算不再次累计。先按UID锁所有参战账号，再访问共享任务规则，避免后续参战账号的接取任务操作导致锁顺序反转。TestTaskCountersSettlementLocalDatabase和TestExtendedTaskTransitionsLocalDatabase在独立debug连接级临时表实际通过（合计47.679s）：重复结算、未确认结果、训练模式不计数、达到要求状态4、后续玩家奖励无效时计数回滚及列表/接取回归。辅助SSH已关闭，没有修改现有玩家。

GM两项页面测试复跑通过，验证导入保留绑定、修改事件及草稿隔离；首轮进程未完成无断言输出，不能算通过。定向分析通过。Go persistence/game/protocol-tester测试通过，候选服务器已编译，运行服未替换。任务仍在6000查询时刷新列表；即时完成通知、6311/6312领奖和完整TLS真实战斗任务链仍待补，不宣称整个任务系统完成。

## 每日／新手列表网络接入

6000基础任务查询后，在已配置extended且匹配客户端版本时下发6041/6042两个完整列表。先构造校验全部两类列表，再发送；每类不分包（原生每次接收清容器），关闭后空列表用于清掉旧项，未配置不影响原基础任务链。编码141B已恢复的key+12/state+16/条件槽+53起，显示模式选普通计数0；未知头尾字段采用本服零值策略，不宣称恢复原服务器完整结构，也不利用20530。

TestExtendedTaskActionsTLS扩展真实查询→接取→再查询→重复→取消→再查询，实际通过39.084s：初始两类各一项、状态2同步、合法条件key0计数1、重复不重置、取消后计数0、关闭每日后6041空且6042保留。原有错长/伪造上下文/状态/禁用/版本和ping保持验证继续通过。使用独立debug临时账号及连接级临时表，已清理并关闭辅助SSH。协议编码固定偏移/重复键/错类别/状态/溢出/空列表测试通过，候选LocalServer构建。

运行服未替换，真实原生面板尚未验收；本地模板奖励文字与GM自定义奖励的一致性、完成事件和领奖仍待实现。

## 每日／新手当前列表存储查询

ExtendedTasks按认证账号锁及一致事务读取当前配置/周期/进度；未接任务返回状态1，已接任务使用冻结快照，关闭任务隐藏，新手已领取状态3隐藏，每日仅列UTC当天，旧周期记录保留。未配置返回nil、已配置但无可见任务返回空列表，方便后续网络层区分无需干预与清空面板；指纹不符、损坏或重复跨周期状态拒绝。

扩展TestExtendedTaskTransitionsLocalDatabase在独立debug连接级临时表实际通过16.336s，覆盖初始列表、冻结条件/奖励/计数、跨账号、昨日记录排除、新手已领取隐藏、关闭后空列表、未配置及错误指纹。辅助SSH关闭，无现有玩家变更。此批尚未接网络列表下发，不能宣称原生任务面板可用。

同时纠正新手容器判断：加载器将完整模板保存到manager+20，6042写141B到manager+0接收记录，二者独立；之前“覆盖客户端模板”的说法不成立。奖励面板仍从本地模板取值，服务器自定义奖励的原生显示一致性仍需处理。

## 每日／新手接取放弃真实TLS验证

TestExtendedTaskActionsTLS实际通过16.968s：独立debug库临时账号、连接级临时任务规则/进度表，校验证书的TLS完整登录/P2P流程。两类任务分别接取收到6061/6062精确3B，取消收到6091/6092精确3B；重复接取保留数据库计数，重复放弃不重发清进度通知；错长、伪造state4、关闭任务及指纹不符均返回notice且ping/pong证明连接存活。请求+3至尾部全部填255仍只操作认证账号，数据库验证无其他UID进度。

临时账号数据清理、临时表随连接销毁，辅助SSH已关闭。此批新增网络测试，没有修改运行服/现有配置；没有模拟原生面板，完整列表、完成判定、领奖仍未完成。

## 每日／新手接取与放弃持久化

新增extended_task_progress迁移，按账号/任务/周期唯一。ExtendedTaskTransition锁认证账号及任务规则，匹配服务器ConfigHash、类别、目录与启用状态；接取时冻结完整规则、条件目录、版本和客户端指纹。重复接取保留计数/旧奖励；放弃清计数，重复放弃不写；已领奖/可领奖状态拒绝接取或放弃。每日周期采用数据库UTC日期（明确的本服策略，不代表原版），新手使用永久周期，历史每日记录保留。

TestExtendedTaskTransitionsLocalDatabase在独立debug库全部使用连接级临时表（包括accounts）实际通过11.637s：接取/重复/取消/再接、GM改奖励后冻结、跨账号隔离、错误指纹/类别/不存在账号/未知任务/领奖请求拒绝、完成状态取消拒绝、昨日已领奖与新周期隔离、关闭规则拒绝。辅助SSH已关闭，无现有玩家数据变化。

游戏入口接6051/6052、6081/6082，认证会话UID取代所有客户端上下文；状态改变后发6061/6062、6091/6092的3B最小已读字段，重复操作不重发会清进度的通知。game/persistence/protocol-tester测试通过，候选LocalServer构建。此轮真实数据库验证存储路径，不是新网络入口的TLS或原生UI验收；完整列表下发、完成事件和领奖仍未接入，运行服未替换。

## GM每日／新手任务编辑页面

任务配置新增每日／新手入口，两类虚拟列表、目录导入、开关与经验/金币/商品奖励编辑；条件保持客户端目录值。导入校验指纹/类别范围/三条件及重复编号，不同版本拒绝并保留草稿，同版本保留已有奖励，新项默认关闭和零奖励。页面返回深拷贝草稿，再由原任务配置统一按revision保存，取消不修改父级。线上/本地沿用当前环境，缺本地资源的平台隐藏导入但可编辑已保存目录。名称增加持久化及128字符校验。

页面明确提示游戏进度/每日重置/领奖未接通，不将保存规则等同已可玩。新增两项widget测试通过（第一次与基础页并跑时基础5项通过，新页进程提前结束；单独详细日志复跑2项通过），覆盖保留奖励、负金额拒绝、分组切换、父级隔离、版本不匹配保护；定向analyze无问题，persistence/desktop测试通过。候选服务器构建，运行服未替换。

## 每日／新手配置持久化基础

TaskRules新增独立extended目录与规则，绑定自身客户端SHA256；每日编号2000–3000、新手编号>3000，三组条件保留编号0的合法要求，拒绝重复有效条件、重复任务、越界奖励及未匹配目录的规则。经验/金币/商品奖励为本服显式策略，不冒充原版。复用task_rules的版本比较和审计事务；已有extended不能由缺字段的旧管理器清除。基础任务GM读取/导入/保存保留extended，不会在编辑旧任务时丢掉新类别。

TestTaskSettingsLocalDatabase实际在独立debug库连接级临时表通过（5.027s），覆盖读写往返、旧版本、审计失败回滚、旧GM清字段拒绝；辅助SSH已关闭。persistence/game/desktop/adminhttp普通测试通过。Flutter初两次进程未完成无断言信息；单项详细日志通过后，全页五项详细日志复跑通过，定向analyze无问题。

仍只是可保存配置及防丢字段基础，每日/新手编辑页、完成事件、周期和领奖事务尚未接入；没有保存运行配置或替换运行服。

## 任务附加记录20530诊断及原生风险

从当前PE注册表定位20530→A2BEE0，确认22B记录、+12任务键和两类附加容器，尚非已证明的条件计数更新。发现循环增加载荷长度而不是局部索引，并持续推进指针；非空载荷存在静态可见越界风险。没有对游戏实发验证。

共享协议与测试器新增只读诊断/短尾校验，测试通过；没有新增服务器发送、编码器或客户端修改。后续任务实现不能走这个未修复的原生入口；完整任务业务仍未完成。

## 每日／新手条件计数布局

恢复七组12B条件槽（141B记录+53）、当前计数（+57）及模板三组条件映射。目录接口提供描述/编号/要求，协议测试器显示实际条件计数与未知第三DWORD，保留全部原字节。记录+14仅命名显示模式，不把UI比较当作服务端完成授权；新手训练条件编号0有效。

首末槽及边界哨兵、真实目录与条件读取、既有协议/管理/测试器测试通过，客户端资源未修改。状态达成事件、周期、领奖持久化及GM编辑页面仍待接入，运行服未替换。

## 每日／新手操作结构及状态解析

从当前PE恢复六个19B请求6051/6052、6081/6082、6311/6312及八个通知消费者；确认141B记录状态偏移16。共享解析器与独立测试器接入字段诊断和边界验证，不将进程上下文/未初始化尾部作为身份或幂等键。protocol与protocol-tester测试通过；具体消费者和副作用见tasks/README.md、extended-task-actions.asm。

进度达成、周期重置、持久化领奖和GM编辑仍未接入；运行服未替换，未操作数据库。

## 每日／新手任务目录读取基础

确认6041对应BaseDailyQuest、6042对应BaseNewBieQuest，修正把已接收容器查询称作配置查询的旧描述。桌面管理task_extended_templates从同一客户端包读取27列目录并返回SHA256；保留未知字段，不混入基础任务41列模型，不访问数据库。真实资源验证42条每日、15条新手，资源哈希不变。目录解析边界及协议测试器分类/截断测试通过。

此批是新增任务类别的配置读取基础，尚无每日/新手状态持久化、重置、领奖或GM编辑页面，不能宣称这两类任务已可玩。运行服务器及配置没有替换。修改前Git检查点HEAD与origin/main均为b30c24c759624933975de1c9893a938c6e9ab16a，本轮修改尚未提交。

# 服务器协议完善执行记录


## 6040后续任务通知与6030完整TLS测试

静态追踪确认6040创建记录不查重，新增后续通知选择与6020排除策略：仅新完成父任务、客户端指纹匹配、模板及服务端后续一致、目标在当前列表且state1才发6040。同次6020不包含该目标；后续完整查询仍包含，状态2不因通知重置。纯函数测试覆盖字段、去重、版本及后续不匹配、已接任务保护。

TestTasksTLS实际通过（43.660s）：两账号真实对战结算累计两场，首次6000完成任务发4300/1240/6020/2160/6030/6040/提示，验证6030身份和键、6040目标与6020不重复；重复查询及显式注销重连后完整列表/余额/一条奖励回执/一件新增库存均正确。使用独立debug临时规则表与临时账号，清理完成，辅助SSH关闭。原生界面视觉未验证；6041/6042及连击、歼敌等条件仍未完成。候选服务器与测试器Core构建，测试器菜单更新；运行服务器未替换。


## 6030完成通知接入奖励提交后的回包链

TaskAwards保留完成事务读取的目录规则；6000先提交奖励，返回4300/1240/6020和新增物品，再为新完成且当前列表state3的任务发6030。通知仅在客户端指纹一致、本地模板启用及模板称号不高于持久profile+123时发送；无绑定、自定义ID、模板关闭/版本不匹配/称号超前继续用真实列表与已有文字通知，不猜测客户端模板。

6030使用14B（当前完整消费者只读+12 WORD）；前12B采用现有任务应答的认证UID/profile上下文策略，不宣称恢复了原服务器未知前缀。重复Keys去重；重复查询没有新奖励Keys，不重发完成通知，也不再次发奖。6040后续任务通知及6041/6042其他任务业务仍待补。

TestTaskCompletionMessages覆盖64位UID、字段、去重、模板和称号门控；独立debug临时表TestTaskSettingsLocalDatabase通过（4.001s），验证目录往返/旧GM丢字段拒绝/审计回滚；TestTaskProtocolLocalDatabase通过（12.635s），验证接取→完成→6030→重复查询不通知/不发奖。辅助SSH关闭。尚非完整TLS及原生任务面板视觉验收，未保存运行配置或替换运行服务器。


## 任务完成通知目录绑定基础

readBaseQuestCatalogue从同次loadArchive数据计算SHA256，task_templates返回client_hash；模板新增title_level，兼容保留unknown_rank原字段，两者取原第38列。TaskRules加入可选客户端目录（ID/后续键/称号等级/启用）及指纹；校验格式、重复、BYTE称号范围、后续引用。已有绑定不能由旧管理器缺失字段清除。CanNotifyCompletion仅接受匹配版本、存在且启用、模板称号不超过持久角色称号的任务，避免6030无保护读取或客户端单独升称号。

GM导入校验指纹/字段，保存保留目录，重复导入保留自定义任务条件/奖励。真实config.spf2读取50条任务及同字节SHA256测试通过；Go desktop/persistence/game/protocol-tester通过。Flutter五项测试复跑通过（首轮最后两项进程未完成，无断言原因），定向分析无问题。管理后端及候选服务器构建。此批尚未调用6030/6040网络通知，配置CAS目录保护的真实数据库验证及完成链仍需继续；未保存运行配置或替换服务器。


## 在线邮件提示真实TLS验证

TestGiftTLS增加offline/online两个子场景，独立debug临时账号和临时商品、证书校验TLS，实际通过（37.277s）。online收件人在赠送前完成登录/P2P，不先发1300即收到1310/339B、+327未读标记0；随后正常查询详情，2171/1340背靠背领取删除成功，重复领取只收到心跳，无重复物品。offline链继续覆盖离线投递后上线读取。此网络测试不模拟原生anmMessageTip动画；战斗延迟通知由上一轮Hub独立库三场景测试覆盖。

临时数据清理，辅助SSH关闭。此批仅新增测试，运行服务器未替换。下一缺口为任务完成通知，必须绑定真实BaseQuest模板与客户端版本，避免6030按不存在的模板读取或客户端称号超前。


## 在线赠送邮件提示刷新

根据当前客户端1310→8BCC80→8BC790→anmMessageTip证据接入真实邮箱刷新。GiftResult区分新投递/幂等重放，新投递才通知；发送者成功不依赖收件人刷新成功。在线大厅/等待房立即1310，战斗/加载推迟，回到安全阶段后现有15秒ticker刷新并清标记，失败保留重试；主动1300读取也清标记。未新增未经证实的通知包号。

TestGiftProtocolLocalDatabase新增offline/online/battle子场景实际通过（46.879s），验证新未读列表、重复操作不重复通知/收费、战斗期间不发列表、回大厅仅补发一次及原有领取删除幂等。临时数据清理，辅助SSH关闭。普通persistence/game/protocol-tester测试通过。本通知尚未完整TLS及原生动画验收，运行服务未替换。


## 法宝材料修理完整TLS验证

新增TestTalismanRepairTLS，独立debug临时账号、连接级临时规则表、校验证书TLS及正常登录/P2P初始化。实际通过（25.668s）：无报价/不存在实例拒绝；4203精确32B字段；材料数量由2改4时旧报价拒绝且库存不动；刷新报价4材料不足不扣；改3后重新报价，伪造材料拒绝，合法操作发2161法宝额度10000、2162删除耗尽材料、4205成功。再次点击和关闭规则均不再扣费，金币保持1000，仅一条修理回执。每次响应通过ping/pong确认连接仍活跃。

临时账号/库存/回执清理完成、辅助SSH关闭；没有更新现有运行规则或替换服务器。该测试检查真实网络与数据库，不代替原生修理面板视觉验收。GM页面说明更新为需新版服务器及修理重新报价；页面三项测试、定向静态检查与构建结果见本轮产物。当前待办转向其他协议缺口，法宝原生技能/MP全部约束仍不能宣称完整权威模拟。


## 法宝改价重放与动态规则TLS验证

UseTalisman回执身份比较保留实例/物品/事件种类/槽位，先校验完整16字节，再比较前14字节；最后2字节为服务器策略费用，不再因GM改价拒绝已完成事件。重放仍检查本人装备、状态及期限，只返回当前额度，不再扣费、不恢复历史额度。改变事件种类仍拒绝，客户端4201自报价格仍不参与收费。

TestTalismanTLS真实双账号、证书校验TLS及独立debug临时规则表通过（30.520s）：200费用接受后改250，重复pending仍扣200；完成后改价重放仍返回300额度且不重复效果；配置关闭阻止新事件，重新开启后主动/被动、被动每局一次收费、额度不足及可靠事件测试继续通过。初次测试因夹具对二进制BLOB使用JSON_SET报3144，修为json.Marshal参数写入后通过，不是游戏业务故障。

TestTalismanEquipmentLocalDatabase实际复测通过（33.858s），覆盖改价重放不扣费及事件种类替换拒绝、既有装备/修理/期限/跨账号事务。临时账号清理，辅助SSH关闭。候选LocalServer与测试器Core构建，完整协议测试器EXE重新发布，菜单说明改为GM动态规则及修理前必读报价。未替换运行服，原生UI及修理全TLS链尚待验证，GM说明尚待随最终交付更新。


## 法宝动态配置及修理报价保护

服务器启动SeedTalismanSettings只初始化不存在的规则；游戏使用和修理读取当前管理规则，revision非零时关闭不回退静态配置。4202绑定实例、完整材料规则及版本，4204要求一致，存储事务锁规则并重新检查版本/启用/完整规则后才扣材料。重登清报价。已接受8071到4201之间费用保存在5秒pending中，相同重传不刷新费用与到期时间。

TestTalismanSettingsLocalDatabase独立debug临时表实际通过（3.398s）；TestTalismanRepairProtocolLocalDatabase改用临时规则表，通过（35.302s）：无报价、旧报价、事务中旧版本和篡改数量拒绝且库存不变；重新报价后修理成功、材料删除同步、重复修理不扣/不补，以及后续主动/被动消耗和重复事件。临时账号数据清理，辅助SSH关闭。普通game/persistence/protocol-tester通过，候选服务器构建完成，未替换运行实例。

仍需覆盖配置变更期间pending费用、已完成使用事件的重放、关闭及完整TLS动态规则链；当前UseTalisman回执比较包含旧费用，改价后旧事件重放会被拒绝（不会二次扣费），此恢复语义需继续完善。GM现有构建仍保留动态读取尚未接入提示，待验证完整后更新提示及交付。


## GM宠物／法宝规则编辑页面

同一GM新增宠物／法宝页，分别编辑使用消耗和材料修理列表，按当前环境API读写版本化配置。列表按需构建，支持新增、删除及逐项编辑；校验非零物品/材料、重复物品、uint16额度范围、自身作修理材料、启用空规则。关闭保留两类列表；读取失败禁止保存，保存冲突保留草稿。界面明确当前仅配置保存，运行服动态读取仍待接入，未声称修改已作用于游戏。

三项Flutter页面测试通过：修理字段边界/自身材料拒绝、关闭与版本及另一列表保留、保存冲突草稿保留、读取失败、重复规则和空启用拒绝。定向静态分析无问题。未保存任何运行配置或修改原客户端资源。


## 法宝配置持久化及管理接口

新增TalismanSettings，独立总开关、使用消耗列表和材料修理列表；额度沿用库存百分之一单位。拒绝启用空规则、超过4096条、重复物品、无效材料数量/容量和自身修理；零主动/被动消耗保持既有免费规则语义。关闭保留数值。配置版本比较、更新及审计同事务；旧规则初始化使用INSERT IGNORE，不覆盖已保存配置。

talisman_settings_get/save贯通本地桌面及线上鉴权管理接口，缺失请求在数据库访问前拒绝。共享使用规则类型与原游戏配置兼容，未修改现有使用/修理运行路径。普通persistence/game/desktop/adminhttp测试通过，覆盖字段传递、未授权/错误环境拒绝和规则边界。独立数据库测试已新增但本轮没有提供DSN，未执行，不能视为真实数据库验证。

GM可视页面、运行规则动态读取、修理报价版本绑定及真实TLS验证仍待接入。未保存任何运行配置、未替换运行服务器。

## 武器升级真实TLS验证

TestWeaponUpgradeTLS独立debug库实际通过（17.712s），临时账号经证书校验TLS完整登录/P2P后验证：未读表拒绝；21411精确63B三行及保留字段；GM由70改80后旧报价拒绝且金币1000不变；刷新收到80，错误实例拒绝，正确升级后金币920/熟练度400/等级1；下一次0%失败金币830/熟练度200/等级仍1；关闭后拒绝且仅两条收费回执。每步ping/pong证明连接存活。配置为连接级TEMPORARY表，临时账号/库存/回执清理完成，辅助SSH关闭。

测试器菜单同步要求先读报价及改价后刷新。该测试执行真实协议和数据库事务，不执行原生UI或伤害算法；运行服务器未替换，完整项目仍有其他协议缺口。

## 武器升级动态规则与报价版本保护

新版服务器启动时INSERT IGNORE导入原武器等级表/启用状态，不覆盖已有GM配置。21410读取管理表并绑定会话WeaponRevision；21412使用该版本，在扣费事务内锁weapon_rules重新核对启用状态、完整规则和版本，费用/成功率取数据库。未读表、旧版本或关闭不扣费，提示重新打开面板；重登清除报价版本。已提交操作的存储层重放继续恢复原库存与余额，避免配置变化后二次扣款。

独立debug库临时规则表验证实际通过：TestWeaponUpgradeConfiguredLocalDatabase（10.797s）覆盖无报价/旧版/未来版、合法收费、关闭及旧成功回执；TestWeaponUpgradeProtocolLocalDatabase（11.613s）覆盖21410读取、改版拒绝、刷新后升级成功/失败及重复回执。临时账号清理、辅助SSH关闭。普通game/persistence/protocol-tester测试及候选服务器构建通过；尚非完整TLS或原生升级面板验证，未替换运行服务器。GM说明已更新，需部署新版服务器后生效。

## GM武器升级等级表编辑页

同一GM新增武器升级入口，沿当前本地/线上环境读写weapon_settings。支持逐级编辑熟练度、金币、成功率、攻击加成显示原值，从0连续新增/删除最高级，虚拟列表展示。启用需至少两级，最高行为满级；关闭保留数值，未知字节16只保留不提供猜测性编辑。读取失败禁保存、版本随请求传递、保存失败保留草稿。页面明确当前仅保存，游戏动态读取尚未接入，不能将保存当作已生效。

两项Flutter测试通过：非法成功率拒绝、修改和关闭保留、未知字段/版本/环境及读取失败；定向静态检查无问题。Windows构建及dist运行目录更新见本轮产物；未修改运行服配置。

## 武器升级管理接口

weapon_settings_get/save接入persistence.Admin、桌面环境转发和线上HTTP鉴权白名单，参数weapon_settings包含revision/rules，不开匿名保存入口。缺失配置在访问数据库前拒绝。桌面传递、HTTP无凭据401/错误环境403/合法请求200与字段保留测试通过，persistence/desktop/adminhttp回归通过。GM后端组件及候选服务器重新构建；当前GM可视页面及游戏动态读取还未接入，配置保存本身不改变升级行为。未替换运行服务器或保存本地/线上规则。

## 武器升级配置持久层

新增WeaponSettings（开关、完整等级表、revision），沿既有配置方式保存weapon_rules及weapon_rules_audit，版本冲突拒绝、审计与配置同事务提交。关闭保留数值，启用需2至256条从0连续等级，熟练度非零、成功率0至100，未知字节16仍原样保留。game.WeaponLevel改用共享字段定义，旧运行配置验证复用同一方法；尚未切换游戏读取来源，不改变当前升级行为。

独立debug库连接级临时表TestWeaponSettingsLocalDatabase实际通过（3.953s）：默认关闭、保存/版本冲突、审计失败回滚、关闭保留和损坏数据拒绝。game/persistence普通测试通过，辅助SSH已关闭。管理接口、GM页面和游戏动态读取仍待接入；未保存运行服配置或替换运行服务器。

## 21372目标地图键闭合

通过A963B0 mapselect.xml Id读取、A56560扩展记录复制、7F8B47/481580面板写入，确认+5D8为目标地图ID，继而闭合21372首DWORD容器键。共享结构区分MapID与RequiredMapID，测试器分别展示目标地图和条件地图，固定字节往返测试通过。证据map-selection-key.asm绑定当前客户端哈希；中间四DWORD及解锁规则继续待恢复。protocol/protocol-tester测试通过（后者0.136s）。未改变运行服或个人进度数据。

## 21372条件地图字段恢复

完整提取A553D0及A56560，闭合MapInfo.MapId写入配置首DWORD、地图列表复制及7F8854匹配链。21372的+20现命名MapID（条件地图ID），共享编解码及测试器诊断同步修改，固定字节布局测试保持通过。记录Key及四个中间DWORD继续未解，不据此伪造通关结果。protocol及protocol-tester测试通过；地图加载器完整证据替代旧文件中两段非指令边界提取。此批推进字段恢复，个人解锁业务仍未接通。

## 21371消费者归类纠正

继续静态追踪发现21371经7ED340进入7EBE00的物品兑换控件，并与7ECF30的库存选择面板关联。相邻包号不构成21370应答族证据。已保存当前PE哈希绑定的exchange-21371.asm，修正测试器诊断和21370菜单期待值，防止把兑换通知当关卡初始化成功。第二DWORD仅确认整数除100用于动画参数，不定义金币/经验；第一DWORD继续保留未知。相关边界与归类回归通过（0.133s），测试器Go核心及Windows EXE重新构建。此次没有伪造关卡回复或修改运行服务器；个人进度和兑换业务仍未完成。

## 关卡准入完整TLS自动场景

独立测试库TestStageGateTLS实际通过（21.024s）。测试器以两个新临时账号经过证书校验TLS、登录及P2P注册，验证低称号建房3030、进房3080且无3100；关闭称号条件后同连接进房成功；重新启用条件后房主不能带低称号成员开战；关闭称号条件但全局关图仍拒绝开战；恢复开放后双方收到4080。每步通过ping/pong读取应答并检查连接存活，拒绝后不重登即可恢复操作。

规则使用单连接TEMPORARY stage_access表，没有保存运行服配置；临时账号和库存清理完成，辅助SSH已关闭。日志runtime-backups/stage-tls-test.log。测试止于双方进入加载，未执行原生渲染，不代表21370个人进度、通关奖励已完成；此批新增自动测试，不需要重新替换服务器程序。

## GM地图称号条件编辑与构建

关卡页支持读取本机客户端地图目录、保留已有条件并合并缺项、逐图编辑最低称号及独立启停。导入不自动保存或启用；随机选择项0不当具体地图；版本不匹配拒绝导入。保存保留关闭地图列表及版本，纯在线模式隐藏本机读取入口。修复编辑弹窗关闭时提前释放TextEditingController的问题，改由TextFormField管理输入生命周期。

四项页面测试最终完整通过，覆盖重复导入、范围校验、关闭保留编辑值、环境及版本保留、读取失败和目录不匹配。中间两次测试进程未完成，没有断言原因输出；单项及完整详细日志复跑通过，记录runtime-backups/stage-ui-all.log。本页及测试定向静态检查无问题；全项目检查仍有六处既有测试样式提示。Windows release构建通过，完整运行目录更新至dist/GM管理器，app.so哈希与构建产物一致。未保存运行配置、未重启服务器，不代替原生游戏或完整TLS关卡准入验证。个人21370进度业务仍待补。

## 房间操作接入关卡称号准入

新增共享stageGate：每次操作读取当前地图规则，启用条件时读取认证账号的服务器profile称号，多人使用最低称号检查整组；不采用客户端申报的等级。建房及随机/分组选图使用同一过滤条件，不能先选到不允许地图再无故失败。普通进房拒绝返回3080及说明，快速加入跳过不满足条件的房间；换图与开战检查所有房内成员，条件失败前不改变房间、准备或玩家归属。

独立debug临时表TestStageGateLocalDatabase通过（5.386s），验证随机跳过高门槛地图、单人达标/同房他人未达标、建房/普通进房/快速加入/换图/开战拒绝、版本不匹配、关闭条件保留及全局关图仍生效。首次测试因夹具没有有效P2P期限在旧校验处失败，补齐真实前置条件后复测通过。普通game/persistence/protocol-tester测试与候选服务器构建；尚未完整TLS或原生UI验证。

进行中的战斗不因配置修改被强制终止，下一次操作检查当前规则。GM条件导入编辑界面仍待接；21370个人通关/难度进度协议未完成。未替换运行实例或修改运行配置。

## 关卡称号准入规则持久化

StageAccess新增独立requirements_enabled、客户端配置包SHA256和具体地图最低称号列表。关闭条件保留目录，启用时要求匹配版本、具体非零地图和有效名称；未知地图不满足启用目录准入。AllowsTitle合并全局地图关闭与称号判断，不会用称号绕过关闭地图。

读取兼容旧JSON关闭数组；新增字段用对象存储，revision始终来自数据库而非JSON。原配置已有目录时，旧GM提交缺失目录的请求被拒绝，防止无意擦除准入条件。现有关卡GM保存保留服务端返回的完整字段，重新读取失败禁止保存。尚未添加条件编辑/导入界面，也未调用房间准入检查，不能称游戏限制已生效。

独立debug临时表TestStageAccessLocalDatabase通过（6.524s）：旧格式兼容、审计回滚、配置版本、目录往返、旧客户端丢字段拒绝和关闭保留。普通Go测试及Flutter2项页面测试、静态检查通过；服务器/管理组件构建。后续继续房间建入/换图/开战入口接入和GM目录编辑，运行配置未改动。

## 关卡准入目录来源恢复

静态追踪确认21374调用mapmgr配置及“关卡已开启！”提示；提取MapId、MapType、NeedTitleLevel加载证据到nixiang/room-management/map-requirements.asm。新增ReadStageRequirements及桌面stage_requirements读取同包SHA256、地图名称/类型/称号要求，区别于现有PVE难度与奖励展示表。

真实资源测试纠正两个假设：ID0为合法随机地图目录项；非注释MapConfig共96条而非旧笔记97条。解析保留ID0供目录展示，具体关卡业务不能据此发通关奖励。desktop完整测试（包含真实资源及读取前后哈希校验）通过（0.177s），管理组件构建；未修改原包。下一步接地图准入规则持久化/GM和服务器建房、进房、开战校验；21370个人进度完整响应仍未完成。

## 称号目录随GM配置生效及客户端版本绑定

title_catalog现在从同一次读取的config.spf2字节计算SHA256并返回client_hash；GM把目录等级/名称及指纹随规则保存，重新读取仍保留，纯在线端编辑不会丢失已保存目录。服务器以登录校验所用ConfigHash比对，只接受相同配置包的目录；已有绑定但不匹配时不退回旧静态白名单。目录无绑定的旧配置仍可沿原title_levels方式运行，正常GM导入路径不再需要另改该文件字段。

规则校验拒绝无效/大写/缺失指纹、空/超长目录、重复/越界等级、空名称及目录外启用规则。自动授予事务内重新读取目录绑定，防止使用配置变更前的目录授予新称号。此为可信GM导入的版本匹配，不是客户端上传任意目录；客户端游戏协议不能修改配置。

TestTitlesTLS去除静态TitleLevels，改由数据库保存的匹配目录驱动，真实独立debug网络验证通过（33.682s）。Go普通测试及真实客户端同字节指纹校验通过，Flutter5项页面测试和静态检查通过。服务器及GM管理组件重新构建；运行服务器、规则和线上数据未替换。

## GM称号目录导入

称号页接title_catalog读取当前本机客户端roletitle.xml目录并显示名称，按等级合并缺项：0级初始称号不生成奖励规则，新增行关闭、条件为0、奖励为空，已有条件和候选不覆盖。重复导入不重复新增，不自动保存或打开总开关。错误/重复目录保留草稿；目录已加载时，不允许保存目录外的启用称号。纯在线模式隐藏本机读取按钮，未向HTTP管理接口开放服务器文件读取。

5项Flutter页面测试通过，包括原保存保护及重复导入、已有值/版本保留、新规则默认关闭、坏目录拒绝和在线入口隐藏。静态分析通过；实际客户端资源读取17条测试通过（0.131s），原包哈希未变。Windows构建成功，完整GM目录更新并核对app.so哈希。此导入仅辅助管理编辑，不替代服务器title_levels目录门控，未修改运行规则或线上数据。

## 称号完整TLS测试与测试器入口

新增TestTitlesTLS，独立debug库临时账号经校验证书的TLS登录/P2P初始化，6000触发第一个称号4125，重复刷新恢复候选；4126未公告、短包和名单外商品均拒绝，合法领取2160，重复领取2161同实例。同会话刷新不提前发下一称号，显式注销重连后第二称号可领取；再次重登没有待领通知，库存恰好增加两件且领取记录两条。任务/称号/商品配置均为连接级临时表，临时账号及数据清理，不修改已有玩家和运行配置。

首轮实际测试发现NewAccount等级字段0与成长系统视为1级的约定不一致，修正titleCountersMet复用ProfileLevel，同时仍拒绝原值超过150。新增零等级与越界单测；真实TLS复测通过（32.603s），普通persistence/game/protocol-tester测试通过。该验证覆盖网络和数据库，不代表原生奖励面板视觉验收。

独立EXE测试器新增“称号 / 查询待领奖励”“称号 / 领取候选奖励”，使用6000及4126/149、+145商品键，提示首次2160/重试2161和库存变更；同时纠正任务物品奖励的旧说明。Core及Windows测试器构建发布到dist/协议测试器。候选服务器更新但运行实例未替换。

## GM称号规则页面与Windows交付

同一GM新增称号规则入口，沿当前环境API读取/保存titles配置，标题和状态显示本地/线上环境及版本。支持总开关、逐项启用、新增/编辑/删除、角色等级/完成任务/参赛/胜场条件和最多7个候选商品；列表按需构建。编辑与保存分开，读取失败禁止保存，服务端版本冲突保留草稿，关闭不清数值。页面提示称号需匹配客户端目录与服务器title_levels，尚未自动导入/校验目录，不宣称配置即能在所有客户端生效。

3项Flutter页面测试通过：读取失败、禁用规则编辑后字段及revision保留、重复候选拒绝、必须显式保存、保存失败草稿保留。静态分析无问题；Windows Release构建成功，完整运行目录复制到dist/GM管理器，data/app.so哈希与构建目录一致。没有修改运行配置或线上数据，未替换游戏服务器；完整称号TLS链及原生面板仍待验证。

## 称号自动达成与资格事务

新增AdvanceTitle：锁定认证账号和称号配置，读取服务器profile等级、四种已支持对战模式累计参赛/胜场及本人task_progress完成状态。全部非零条件同时满足才发资格；先选择高于当前称号的最低启用规则，不因该规则未达标或不在匹配客户端目录而跳到更高规则。异常角色等级、短profile、胜场大于场次或计数越界不满足条件。

资格发放复用grantTitleChoices同一事务，候选商品关闭/不存在则回滚，不先改称号。已有待领资格时不创建下一份、不覆盖候选；配置修改仅影响未来资格。announceTitleReward在会话尚未绑定时调用自动判定，再读取待领奖励；已绑定会话保留原重试语义。仍需配置客户端允许的title_levels，运行配置保持关闭。

独立debug库TestTitleProgressLocalDatabase与原TitleReward存储回归实际通过（persistence合计33.361s），覆盖跨账号任务、多个条件、低级称号未达标不跳级、目录限制、配置关闭、坏商品回滚、重复判定、配置修改不覆盖待领候选、领完后下一资格。TitleClaimProtocolLocalDatabase复测通过（14.029s）。普通game/persistence测试、候选服务器构建通过；未替换运行服务。GM可视页面及完整TLS链仍待补，不等同原生界面验收。

## 称号规则持久化与GM管理通道

新增TitleSettings：总开关、逐称号开关、称号等级、最低角色等级、已完成任务ID、参赛数、胜场数和最多7个候选商品。非零条件约定同时满足；等级/计数范围、重复称号、重复或零商品、无条件启用及空候选启用均拒绝。关闭保留原值。此为模拟器规则定义，不冒充原版称号获取规则；客户端称号目录/商品存在性及达成判定尚待业务层接入。

title_rules和title_rules_audit沿现有配置模式同事务保存，revision冲突拒绝。titles_get/titles_save贯通desktop、本地管理及线上HTTPS鉴权接口；未授权401、错误环境403，合法字段与版本传递测试通过。独立debug库连接级临时表TestTitleSettingsLocalDatabase实际通过（4.526s），覆盖保存、旧版本拒绝、审计失败整笔回滚、关闭保留和损坏数据拒绝。persistence/desktop/adminhttp普通测试通过。

GM可视页面、自动条件判定/发资格仍未接入，配置保存本身不会自动授予称号。未保存运行环境的规则或修改现有玩家。

## 称号候选通知及刷新恢复

补充EncodeTitleAward：64B，首字节称号等级，+8起最多7组候选商品键，未使用槽及未解字段置零（显式模拟器编码策略，不声称恢复原版字段值）。原生957973调用目录查询，95797A失败跳957E02并在957E61以0调用控件显示函数；已确认缺失候选隐藏选择按钮，见title-reward-selection.asm。候选商品仍需在客户端商品目录中存在，真实面板验收未完成。

6000任务查询成功后接announceTitleReward；仅对配置title_levels允许的等级通知，默认空列表关闭。通知读取当前账号待领资格并绑定TitleOffer；刷新可重发同一待领资格，领取后同会话不切到下一份，重登清除此绑定且不影响其他账号。title_levels须来自匹配客户端的roletitle.xml；目前尚未接GM自动目录校验及规则配置，不能任意填等级后宣称安全兼容所有客户端。

独立debug库TestTitleClaimProtocolLocalDatabase扩展通过（12.737s）：默认关闭、目录未允许等级不通知、重复公告同一64B候选、通知后领取/重试及后续资格隔离。普通game/protocol测试通过；数据库验证直接调用公告方法及正常领取route，尚非完整TLS/原生UI测试。称号获得条件、GM管理、完整网络自动测试继续待补，运行配置未开启。

## 称号领取网络入口与会话重试隔离

4126/149已接游戏大厅/等待房分发，商品键只取+145，身份只取认证会话。Session.TitleOffer必须由服务端已通知资格绑定；领取成功后保留，防止旧请求消费下一称号的相同商品资格，原生会话重置时清零。当前还没有自动通知调用方，因此未公告资格的游戏请求只提示，不会发奖。

领奖复用现有事务；新增库存发2160，已有实例发2161，仅更新对应缓存而不清除其他物品；删除后的已领取物品不重建。4127会打开另一选择面板，不作为成功ACK；本入口使用库存通知和普通提示。重试发现库存记录短于68B或实例不符时拒绝，避免发送损坏原生包。

新增PendingTitleReward按账号读取未领候选，对零等级、空/重复候选、损坏JSON及多份未领资格拒绝。TestTitleClaimProtocolLocalDatabase使用独立debug库连接级临时表实际通过（11.717s），覆盖未通知拒绝、伪造身份前缀、重复领取更新、旧请求不消费新资格、其他库存保留、坏库存拒绝、删除不复活和候选数据损坏。普通game/persistence/protocol测试通过，候选LocalServer构建通过；尚非TLS端到端或原生面板验证。

4125通知触发、候选包其余字段及客户端目录核对、GM获取条件和候选配置仍未完成。未替换运行服务器，未修改线上配置或现有玩家数据。

## 称号候选资格与一次性领奖事务

新增title_rewards保存(uid,title_level)唯一资格、候选商品及已领取商品/实例；GrantTitleChoices只供可信奖励策略/管理入口使用，禁止零等级、重复/空/超7项候选、不开放商品、等级倒退和重叠未领取资格。同一次相同候选重试不重置资格，不同候选拒绝；称号profile+123更新与资格写入同事务。

ClaimTitleReward使用认证UID及服务端绑定的announcedLevel，仅允许资格内商品。复用独立catalog_award发放组件，与任务奖励共用记录、实例和期限校验；领奖标记与库存/期限同事务。重试同选项返回现有实例，换选项拒绝，已删除/消耗的实例不重建，旧称号重试不消费新称号资格。原生4126没有称号字段，未来网络入口必须使用会话中已通知的等级，不能仅凭当前profile推断。

独立debug库连接级临时表TestTitleRewardLocalDatabase实际通过（15.915s），覆盖重复发资格、候选篡改、跨账号/错误称号/名单外商品、期限写入失败整笔回滚、重复领奖、第二选项拒绝、删除后不复活以及后续称号隔离。persistence/game/desktop/adminhttp测试通过，候选服务器构建通过；辅助SSH已关闭。网络通知/领取入口、成功响应及GM配置仍未开放，可信调用方尚需核对称号目录，不把此存储事务等同完整称号玩法。

## 称号奖励选择4126入口恢复

当前PE进一步确认4125中+8起是7组8B候选，首DWORD用于查商品目录，第二DWORD未解。选择回调957280发送4126/149，所选商品键在+145；6220/13仅为状态4下额外通知，不能误当领奖请求。新增共享候选/领取解析与测试器诊断，固定字段和错长测试通过，汇编保存至nixiang/tasks/title-reward-selection.asm。尚未开放领取业务，下一步接候选资格与一次性领奖事务及GM配置，而不是复用普通购买绕过余额检查。

## 称号字段及4125安全边界恢复

当前客户端确认profile+123为称号等级；4125/829B50读取首字节写入，并由957670复制整个64字节至奖励面板。旧“无载荷”说法有越界风险，已在nixiang/tasks README明确纠正并保存title-consumers.asm。新增4125共享解析与测试器诊断，拒绝0–63B并保留未知字段；不将读取下界冒充原版完整结构，不盲发零填充称号奖励。

新增ReadRoleTitles和桌面title_catalog只读接口，实际从roletitle.xml读取17条等级/名称/图标路径。真实资源验证及重复/范围/缺字段测试通过，原资源哈希未变；protocol/desktop/protocol-tester测试通过。称号获得条件、奖励包其余字段、GM称号发放及6030安全模板匹配仍需继续；此批只提供目录及协议诊断，未宣称称号业务完成。

## 任务完成通知安全核对及测试器

只读当前客户端确认6030/6040完整处理器。6030除了state3，还按本地模板修改profile+123并无保护读取配置对象；GM自定义任务编号不能未经匹配就发送。界面收尾有world+78==5、面板可见和+614等门控，并非无条件切换房间。详细汇编与结论见nixiang/tasks/completion-notifications.asm及README。

测试器新增6030/6040/6060/6090实际诊断分发，共享解析保留未知数据、短包拒绝、区分已证实的读取下界与未知完整包长。protocol及protocol-tester测试通过，包含固定字节/截断/输入隔离/诊断入口。游戏服务器仍不发送未完成模板匹配的6030/6040，继续用已实现列表及奖励链；下一步需恢复该角色字段和客户端模板校验，再完成安全的原生通知链。

## 任务物品奖励与库存通知

TaskRule新增可选reward_catalog，由GM明确填写商城商品编号（0关闭）。服务器使用接取时冻结的商品编号，按领取时服务器offers规格及offer_lifetimes有效期发放一份；不接受玩家自报物品记录，不将BaseQuest未知奖励字段自动解释为物品。校验商品开放、108/68B长度、配置键/类型一致、未装备且可用、期限范围和实例溢出。物品及期限写入、余额、任务状态和唯一回执同一事务，任一失败全部回滚。成功6000应答在列表后发送2160并更新会话库存缓存；重复查询不重复发奖，重登走现有库存恢复。

TestTaskCompletionLocalDatabase独立临时表实际通过（11.557s）：冻结商品键、关闭商品拒绝、回执冲突后库存/期限/余额/状态回滚、一天有效期、重复完成仅一份。TestTasksTLS真实双人两局完整网络场景扩展后通过（41.425s），收到2160正确物品，重复查询及重连后库存仅比初始增加一件。临时商品/期限配置与原服务器隔离，未修改现有玩家。Go game/persistence普通测试及GM任务页5项测试、静态检查通过。此实现为可配置奖励，非原版奖励规则恢复；连击/击杀等条件、6030弹窗、等级即时显示等仍待补。

## GM基础任务模板导入

任务页接入已有task_templates只读接口，按ID合并客户端BaseQuest缺项，显示名称与说明。已有配置不覆盖，新行默认关闭、经验金币为0；模板的未知奖励/物品字段不写入服务器规则。模板须满足29统计项、数值边界、唯一非零键及无环后续关系，错误时保留全部草稿。导入不自动保存，继续使用当前环境与revision版本保护。纯在线移动端隐藏本机资源读取入口。

Flutter任务页5项测试通过，覆盖重复导入、已有配置与版本保留、不自动保存、不导入未知奖励、损坏模板保留草稿，以及原有编辑/保存保护；静态检查通过。真实资源TestBaseQuestNativeArchive再次读取50项且SHA256未变，解析校验测试通过。Windows Release构建成功，完整运行文件已更新到dist/GM管理器，app.so构建与交付哈希一致；未操作线上配置，未替换游戏服务器，不将构建视作原生游戏任务界面验收。

## 双人对战驱动任务完成的TLS验收

TestTasksTLS已替换直接SQL增长统计的步骤，独立debug库完整执行通过（46.895s）。两个临时账号经真实TLS登录/P2P，建房、加入、准备、加载、进入战斗，提交4110战报、接收4120并以3550返回房间；连续两局互换胜负。第一局重复战报后任务仍为进行中且未发奖，第二局后双方统计各增加两次参赛、一次胜利，查询任务才领取40经验30金币、完成1001并解锁1002。重复查询、注销重连后余额及任务状态不变，奖励回执仍只有一条。

任务、成长、地图开关、荣誉规则及结算回执采用连接级临时表隔离，保留正常服务器结算事务和序号分配，未写现有玩家统计或运行配置。初次运行在临时表CREATE LIKE同名限制处失败，改为显式临时表结构后上述完整测试通过；普通测试器Go测试亦通过。此次只有测试与说明变更，无需替换运行服务器或重构建EXE。自动协议验收不代替原生任务弹窗、等级即时显示，也不代表击杀/连击、任务物品奖励或其他任务类别已完成。

## 基础任务真实TLS验收

新增TestTasksTLS，独立debug库实际通过（20.094s）。临时账号经验证证书的本机TLS完成SDK/原生登录/P2P，查询任务、使用伪造前缀接取本人任务、重复接取不重置；仅对新账号模拟服务器统计增加，刷新获得40经验30金币与状态3、解锁后续任务；重复查询及显式注销后重新认证均恢复相同余额和任务状态，发奖回执仅1条。

task_rules/battle_reward_rules使用连接级临时表，未修改运行服配置。临时账号及任务进度、奖励回执在结束清理，辅助SSH关闭。初次测试在正常logged_out确认处因交互读取器的终止信号失败，修正测试断言后完整复测通过，未修改注销协议。

此用例模拟统计增长，不替代双人真实战斗到任务的端到端验收；6030原生弹窗、等级即时显示、其他统计、物品奖励等缺口继续开放。当前仅新增测试，不替换运行服务。

## 游戏任务查询触发完成与余额同步

6000查询现在调用CompleteTasks，以服务器统计自动完成达标参赛/胜场任务，再读取最新列表。每次成功查询发送4300绝对经验、1240绝对金币、6020状态；新发奖额外提示。重复查询仍同步当前余额，处理上次事务已提交但响应丢失的情况，不重复奖励。该自动查询发奖为模拟器策略，未声称已恢复全部原版领奖流程。

扩展TestTaskProtocolLocalDatabase独立临时表实际通过（12.298s）：接取/取消/再接取、仅测试账号模拟统计增长、查询触发100经验30金币及状态3、再次查询余额不变且回执仅1条、伪造前缀及账号隔离。普通game/persistence测试通过。尚非真实TLS及原生UI；大厅经验升级的等级即时显示、6030完成弹窗、物品奖励和更多统计来源继续待补。运行服务和线上配置未替换。

## 基础任务完成发奖事务

新增CompleteTasks：锁定账号与当前任务开关，读取状态2记录的接取时规则和进度快照，以服务器profile统计差值判断条件。总参赛与四模式参赛/胜场可判定；连击、歼敌及其余未有可靠累计来源的非零条件暂不允许完成。计数倒退、越界和空条件不能被当成达标。

达标任务经验金币、角色成长、状态3及task_rewards唯一回执同事务。采用接取时奖励，GM后续编辑不追溯已接任务；当前总开关/逐任务关闭阻止发奖。重复检查状态3不会再次入账，后续任务可沿已有列表逻辑解锁。物品奖励未接入。

TestTaskCompletionLocalDatabase在独立debug临时表实际通过（7.122s），覆盖未达标、冻结奖励、回执冲突回滚钱包与状态、正确发奖、重复无奖励、账号隔离和未知账号拒绝；条件单元测试覆盖多模式累计、胜场组合、基线差值、倒退和未支持统计。初次测试因空成长表被正确拒绝，已使用Normalized完整150级测试配置复测通过。game/persistence测试通过。

当前完成事务尚未接入结算或游戏查询触发，原生完成通知与资料同步继续待接；不能称游戏内领奖已开放。未替换运行服务、未修改线上及现有玩家数据。

## 任务参赛与胜场统计接入结算

BattleReward携带服务器房间模式；SettleBattle在已持久结算回执查询之后、同一事务内更新profile+133/+137（模式0）、+141/+145（1）、+149/+153（2）、+157/+161（3）。参赛只统计至少两人的已确认胜/负/平，胜场只计win；练习/其他模式及unconfirmed不计，达到INT32上限不回绕。无BattleMode的旧内部调用维持原行为。该字段来自房间，不直接采纳客户端声明。

位置依据BaseQuest A48AA0与统计文案表，模式对应见规范摘要的mapselect矩阵。统计更新随原结算profile进入现有结果回包；本轮未声称歼敌数、连击和其他奖章计数已可靠恢复，也未实现任务完成发奖。运行服务器未替换。

TestTaskCountersSettlementLocalDatabase在独立debug库临时表实际通过（9.032s），覆盖两账号胜负计数、重复serial、unconfirmed、练习、团队歼敌平局及后续账号失败时整局回滚。映射/上限/单人排除单元测试通过，game与persistence测试通过。辅助SSH结束；没有更新运行玩家或配置。

## 基础任务游戏收发接通

6000/4查询认证账号后返回6020/123B记录，空列表沿已确认7B空包。6050/14和6080/14调用TaskTransition，只在状态实际变化时返回6060/6090；重复操作提示刷新，不重复6060重置客户端快照。客户端925987和833D33在发送6000前调用A47C70清列表，A48FC0收到6020只追加，所以操作成功后不擅自推整表。非法包仅提示，战斗阶段沿既有阶段门忽略，前缀UID与上下文不用于选择账号。

TestTaskProtocolLocalDatabase在独立debug连接级临时表实际通过（6.115s），覆盖查询、接取、重复、持久快照、取消、错长、战斗限制、伪造前缀及收件人隔离。测试器新增三项入口，尚非真实TLS或原生UI验收；统计累计与完成领奖尚未接通，未启用运行配置。

## 基础任务状态持久化

新增Store.Tasks及task_progress：事务锁定认证账号，按配置生成可接根任务及已完成前置解锁的后续任务；6050接取保存profile+129..244的116B快照和规则版本/JSON，重复接取不重置。6080取消清快照与规则，重新接取才取新基线。多个前置指向同一后续时，任一已完成即可解锁；已接取任务继续保留状态。总开关或逐任务关闭不删除历史状态。

该接口不接受客户端完成状态、不凭请求前8B决定账号、不发放奖励。TestTaskLifecycleLocalDatabase在独立debug库连接级临时表实际通过（10.463s）：列表、未解锁拒绝、接取与重复、快照与规则冻结、取消再接、账号隔离、模拟已完成后续解锁、已完成不可取消、未知账号和非法操作拒绝、关闭保留状态。数据库连接与辅助SSH已关闭，未改变运行账号或配置。

尚未接入游戏6000/6050/6080分发和成功回包；任务统计累计、完成判定、领奖事务与真实TLS完整链继续开放。测试里的状态3只在临时表模拟，不代表已经实现完成任务业务。

## GM基础任务编辑页

同一GM增加任务配置入口，继承本地/线上环境通道。列表按需构建，支持新增/修改/删除、总开关/逐任务开关、后续链、参赛/连击/29项统计条件和显式经验金币。读取失败禁止保存，版本随配置提交；关闭保留数值，循环或缺失后续ID阻止提交。页面明确游戏任务业务尚未接通，不将配置保存描述为已开放接取或领奖。

新增3项Flutter测试覆盖加载失败、编辑后版本与统计值保留、错误后续链不发送保存。默认测试渲染器的flutter_tester出现0xc0000005；加--no-enable-impeller后3项实际通过。task_config.dart及main.dart静态分析通过。此为控件测试，非原生游戏验收；物品奖励和客户端任务条件匹配仍未完成。

## 基础任务配置持久层与管理通道

新增TaskSettings：总开关、逐任务开关、ID/后续任务、参赛/连击/29项统计条件、显式经验和金币奖励。默认关闭；不会把BaseQuest未知奖励币种自动转换为服务器奖励。校验1–512条启用配置、非零唯一ID、整数范围、29项条件、启用任务不可无条件、后续引用及循环；关闭保留规则。

task_rules及task_rules_audit使用版本检查和同事务保存。tasks_get/tasks_save已贯通desktop、本地管理与既有HTTPS鉴权，未认证401、错误环境403及合法字段保留测试通过。独立openkfo_debug库临时表TestTaskSettingsLocalDatabase实际通过（4.286s），覆盖旧版本拒绝、审计冲突回滚、关闭保留、损坏配置拒绝；辅助SSH已结束，未修改运行配置或玩家数据。

GM编辑页面、游戏任务列表/接取/取消/完成领奖事务仍未接入，因此保存配置暂不会让游戏任务生效。物品奖励、特殊任务类型另需继续恢复，不能将当前经验/金币配置视为全部奖励完成。

## 基础任务配置来源恢复

确认BaseQuest.txt才是当前60xx基础任务配置，与titlemission.xml训练关卡独立。恢复41列和29项统计条件映射；新增ReadBaseQuests及GM只读task_templates接口。TestBaseQuestNativeArchive实际读取50条并检查资源哈希不变；非法字段、宽度、重复键、后续引用测试通过（0.133s）。奖励币种、连击达成判断以及接取/完成/领奖事务继续开放；未启动或替换运行服务器。

## 任务简表与操作请求

当前PE复核6010简表展开、6050/6080两条请求路径、6060/6090/6030状态变化，证据见nixiang/tasks/state-actions.asm。发现6050的831400发送路径未初始化前8字节，新增ParseTaskAction保留原值并要求未来业务以认证会话鉴权，避免错误身份判断。测试器增加6010/6050/6080诊断；protocol和protocol-tester测试通过（0.116s/0.121s）。仍未开放任务领奖业务，未改运行服务器或线上数据。

## 基础训练目录接入管理接口与测试器

desktop增加training_missions只读操作，使用已有client-path配置读取titlemission.xml，返回source=client_titlemission及任务目录；不访问账号数据库，不将目录ID解释为60xx任务键。真实资源测试覆盖local/online环境选择、禁止远程调用及读取前后哈希一致。desktop与protocol-tester测试通过（0.168s/0.139s）。测试器核心新增--training-catalog，真实命令读取28项，Windows界面增加“读取基础训练任务”按钮。尚未增加GM任务编辑页面，未实现60xx接取/完成/领奖业务；未替换运行服务器或修改线上数据。

## 客户端基础训练目录

新增ReadTrainingMissions从titlemission.xml读取3组28条基础训练关卡，保留0起任务ID、地图、时限与前置任务。真实config.spf2读取验证通过（0.190s）且文件哈希不变；另识别quest.xml为问答任务，不能混同60xx任务模板或佩戴称号。目录校验测试覆盖依赖和非法字段；服务器任务进度、GM任务配置及其他全量缺口继续开放。

## 任务列表字段恢复

只读核对当前6020/6041/6042处理函数，纠正旧资料6020首DWORD为key：实际A48FC0读+4 uint16，123B每条；6041/6042为141B每条，配置键+12 uint16。证据nixiang/tasks。新增共享严格列表解析与测试器诊断；TestTaskRecordNativeLayout通过（0.130s）。任务进度/领奖字段和服务器业务仍待接入，不发全零假任务表。

## 名侠真实TLS验收

新增TestTrainingTLS，独立debug库实际通过（17.768s）。临时账号经带证书验证的loopback TLS完成SDK/原生登录/P2P；读取服务器名侠等级2和每小时120经验配置；验证开始、重复开始不重置、过早领取拒绝。仅把临时账号起点移到两小时前，再查询120分钟、领取4300/21007并核对240经验落库，重复点击不重复入账，21002开启下一轮。

奖励配置使用连接级临时training_rules/battle_reward_rules表，不修改运行服配置。账号、训练、等级、回执及库存按本次UID清理，辅助SSH关闭。普通测试器测试通过。原生角色等级即时显示、名侠晋级/宝箱仍未完成，未替换运行服务，本轮只增加自动测试无需重建未变的程序。

## 名侠查询奖励预览接入

21000/21002查询现读取服务器名侠等级与GM配置，回填56B中的等级、每小时经验、上限；关闭时奖励字段为0。客户端整小时乘法采用signed int32，配置拒绝不可安全封顶的组合；极长计时在报文中饱和以避免乘法溢出，持久起点和实际领奖不改。TestTrainingPreviewBounds覆盖边界并对比真实奖励计算。

game/persistence测试通过；扩展TestTrainingClaimProtocolLocalDatabase独立debug临时表实际通过（5.863s），验证查询从配置得到100/250及120分钟，再领取/重试。测试器增加开始/领取菜单，后者注明实际入账及剩余限制。运行服务未替换，等级即时显示、名侠晋级/宝箱仍开放。

## 名侠领取协议入口

21006空载荷现接ClaimTraining，复用会话/通道/消息序号作操作ID；成功发4300绝对经验和56B的21007，后者按原生处理器触发21002。失败仅提示，不发送领取成功；非大厅/等待房阶段沿既有阶段门忽略。

TestTrainingClaimProtocolLocalDatabase独立debug库临时表实际通过（5.074s）：错长拒绝、4300/21007顺序与内容、重复请求单回执、无训练失败、同房其他玩家无私有通知、战斗阶段不处理。普通game测试通过。测试为真实数据库与服务器协议队列，不是TLS或原生界面验收。

角色等级即时同步、训练查询回填GM规则、名侠晋级及宝箱仍待完善；当前入口未构建部署到运行服，GM页面继续提示未完成。完整目标继续开放。

## 名侠等级可信来源与领奖同步边界

ClaimTraining移除传入rank，改为事务内查询training_ranks（缺记录为未评级0，异常大于8拒绝），新增只读TrainingRank账号查询。独立debug库临时表扩展测试通过（13.790s），复验等级读取、非法等级和领取事务。未替换运行服务。

静态确认4300只写经验，不更新角色等级字段；大厅领奖网络入口仍待补完整等级同步，不能以战斗4120替代。名侠晋级及宝箱、任务等全量范围继续开放。

## 名侠领奖存储事务

新增ClaimTraining与training_claims回执表：账号锁、服务器时间、配置校验、整小时封顶经验、现有成长表升级、清空训练起点及领取回执同事务。操作ID重试不重复入账，训练周期唯一约束防重复结算。

普通persistence测试通过；独立debug库临时表TestTrainingClaimLocalDatabase实际通过（12.564s），包括领取记录失败的完整回滚、禁用/未满时长/未来时间拒绝及升级。辅助SSH已关闭，未修改已有数据。尚未接网络应答、服务器名侠等级来源及宝箱业务，未更新运行服务；全量目标继续开放。

## 名侠配置GM与构建

training_get/save接既有本地/线上管理及HTTPS鉴权。GM新增0–8级每小时经验/上限与开关编辑，失败禁止保存、revision防覆盖、关闭保留数值。Go管理传输/鉴权测试、Flutter页面2项测试及静态检查通过。完整Windows Release已更新dist/GM管理器，管理Go组件及候选LocalServer.exe已构建，未替换运行服务。

此前TrainingSettings独立debug库临时表验证通过（3.806s），覆盖审计失败回滚等。21006领取按钮绑定已静态确认，证据在nixiang/mingxia。游戏领奖事务与训练等级/宝箱业务尚未接通；页面明确注明配置尚未生效，不代表名侠或全量协议完成。

## VIP真实TLS自动验收

TestVIPTLS在独立debug库实际通过（12.458s）：创建临时账号，经带证书验证的loopback TLS完成SDK/原生登录/P2P，检查重复管理发卡后1120仅一张、1020+33与1038类型均为4、剩余分钟正确。只修改该临时卡到期时间，调用正常安全阶段库存刷新，网络收到2161→2121→1038降为1，ping保持连接；断开后再次完整登录仍为非VIP、库存状态2。临时账号/库存/发卡回执及辅助SSH连接已清理，未替换运行服务。

测试器connect新增可选消息观察回调，复用原登录链核对被引导阶段消费的消息；正常APP继续使用无观察回调入口。此验收证明基础身份/期限协议链，不代替原生VIP画面、全部权益或整个项目完成。

## VIP管理页构建

主界面选中玩家后可打开VIP管理，显示所选环境/玩家/UID，查询资格并确认新增指定档位、1–3650天或永久卡。失败后保留随机请求ID及完整参数，禁用编辑/返回并允许同请求重试；待确认操作当前只在页面内存保存，关闭进程须按已显示请求ID核查审计，不宣称具备跨进程恢复。

Flutter静态检查通过，界面测试2项通过（模拟超时后完整同请求重试、读取失败禁止发卡）；HTTPS入口无凭据及错误环境拒绝测试通过，相关Go包测试通过。Windows GM完整目录、管理Go组件及候选LocalServer.exe已构建更新，未替换运行服。仅身份/期限基础功能，额外VIP权益、真实TLS/原生客户端验证及其他全量目标继续开放。

## VIP身份登录与库存同步

新增Account.VIPKind从服务器已处理期限的库存选择档位；登录1020+33及1038同步当前类型，库存变化后通过1038更新，过期状态先2161/2121再恢复类型，防止缓存旧VIP。当前只启用身份/期限基础能力：1038商城比例为0，走原生不追加折扣分支，未确认权益字段为0；不宣称已配置VIP折扣或额外奖励。现有商城要求原价/售价相等并按服务器报价扣款，本批不改变经济规则。

持久管理vip_get及vip_grant已接桌面/HTTPS既有鉴权路由，GM页面仍待补。TestVIPIdentityFollowsInventory覆盖2160→1038、重复不发送、2161→2121→1038降档、删除及同房隔离；修正测试的完整消息顺序后通过。未替换运行服务，未做原生游戏或真实TLS VIP验收。

## VIP库存快照剩余分钟

Snapshot在读完并关闭库存rows后，仅对type73/730001–730003/状态1卡读取服务器期限，输出记录+13按剩余秒向上取整为分钟；到期输出状态2，永久输出525600作为365+显示。普通道具与状态0不改动，复制记录后修改，不写回原库存。数据库仍以UTC期限决定资格，重新登录不能恢复初始时长。

TestVIPSnapshotRemainingMinutes覆盖分钟边界、到期、永久与共享记录不变；persistence/game测试通过。扩展TestVIPGrantLocalDatabase通过（8.758s），真实Snapshot读取两分钟而持久记录保持60分钟，同时复验重复发放及事务回滚。临时测试表及辅助连接已清理，运行服未替换。VIP类型/权益同步、GM管理和其他协议范围仍继续开放。

## VIP发卡事务与重复请求保护

新增持久层vip_grant，复用desktop_admin_operations请求哈希、回执与同事务审计，不新建旁路授权接口。账号锁内分配库存实例，构造type73、730001–730003、状态1及分钟展示；服务端UTC到期独立存inventory_expirations，永久不写期限行（365+仅显示）。拒绝不存在账号、非法档位、缺期限、过去时间及超过十年的期限；实例溢出拒绝。一次新请求发一张卡，不隐式延长旧卡。

TestVIPGrantLocalDatabase独立debug库连接级临时表实际通过（7.575s）：重复请求仅一张、参数篡改拒绝、记录格式、无效参数，以及期限写入失败时库存/审计一起回滚。未修改已有玩家库存。管理桌面/HTTPS入口尚未放行该操作，GM页面、登录与在线权益同步仍待接入，运行服务器未替换。

## VIP资格持久层查询

当前客户端config.spf2只读目录测试确认730001–730003均为type73；TestVIPClientCatalogue通过。新增Store.VIPMembership：只接受状态1会员卡，结合服务器UTC deadline排除过期，严格按账号读取；不从客户端剩余分钟推断服务端到期日。多卡最高有效档优先，同档永久优先、否则最晚期限，此为确定性服务器政策，不照搬原生遍历顺序依赖。

普通测试覆盖错类型/状态/卡号、短记录、精确到期、多卡正反序及永久优先。独立debug库TestVIPMembershipLocalDatabase用连接级临时表实际通过（2.198s），验证账号隔离、无卡、永久、到期后降档；不修改真实库存及运行配置。

尚未接登录1038、在线期限同步、商城权益扣款或GM发卡；重复发放幂等仍待发卡事务实现。此批为资格计算基础，不宣称VIP完整可用。

## VIP登录类型与会员卡时间来源

继续核对1020+33 DWORD设置VIP类型、VIP面板86D810及9CE200扫描730001/730002/730003卡记录。类型选择受状态1与遍历顺序影响，天数来自记录+13除60再除24，不来自1038权益块。已有永久记录报告中状态0小时/状态1分钟与该消费一致，不能全局改动普通道具期限。

证据保存nixiang/vip/login-duration.asm及README；测试器补1020类型诊断与相邻字段/短包测试，TestVIP全部通过（0.133s）。此批尚未重新发布测试器EXE，VIP业务、GM资格发放和到期链仍未完成；未改变运行服务。

## VIP刷新字段恢复

只读核对当前客户端1038/28B接收器、VIP类型显示与商城消费，证据保存nixiang/vip。类型2/3/4对应白银/黄金/铂金；+20 DWORD为商城比例计算字段，不是已证明的到期秒数。其余五个DWORD仍保留原偏移，不命名为权益或期限。

新增共享1038解析与测试器诊断，固定HEX样例、错长边界和文字诊断TestVIPNativeLayout通过（0.133s）。未修改服务器VIP资格或扣款，尚需补登录/资料同步、权益、期限和GM管理完整链。名侠训练与任务仍需独立恢复，不能用荣誉规则代替。

## 荣誉GM与运行读取接通

新增honour_get/honour_save，复用本地/线上管理路由及HTTPS鉴权；GM荣誉页可编辑胜负平积分、模式编号、最多10级门槛并追加期次。页面读取失败禁用写入，保留revision，兼容已有byte数组base64读取及数字数组提交。主服务首次启动SeedHonourSettings导入原配置，未初始化时GM拒绝保存，后续启动不覆盖已保存规则。

登录1035、20360资料等级与结算积分现读取保存规则；新增期次的客户端目录须重登刷新。规则在结算时读取，调整门槛会影响历史资料等级展示，不声称历史曲线冻结或完整挑战匹配已完成。

扩展独立库临时表测试实际通过（5.496s）：初始化保护、重复导入不覆盖、历史期保护、审计回滚。Go game/persistence/desktop/adminhttp/protocol-tester通过，额外管理传输测试确认模式数组与revision未丢失。Flutter静态检查无问题；界面测试首次因滚动选择修正，第二次有未完成退出，随后完整重跑2项通过。Windows Release构建成功，完整GM目录与Go管理组件已更新；候选服务器已构建。未替换运行服务，未修改线上配置，无原生UI验收。

## 荣誉管理持久层与历史保护

将已有荣誉规则、校验、积分及等级计算下沉到persistence，game保留兼容包装，原游戏规则不变。新增HonourSettings读取/保存、revision冲突检查、同事务审计；旧期次只能保留原顺序原名称并向后追加，避免数据库按期号保存的战绩被重新解释。存储配置不继承fallback缺省字段，显式关闭模式或等级不会被运行配置重新打开。

TestHonourSettingsLocalDatabase在独立debug库连接级临时表实际通过（5.288s），覆盖首次读取/保存、旧版本拒绝、删改重排期次拒绝、追加期次、审计前后内容、审计失败回滚及关闭配置不继承fallback。game/persistence普通测试通过。未改现有配置或运行服务。

此批尚未把管理接口、GM页面和游戏实时读取接入新表；接入前需从原运行配置安全初始化期次，避免首次GM保存重写已有期号含义。荣誉挑战匹配及其他全量目标继续开放。

## 关卡GM构建交付

关卡页面在ASCII构建路径完成Flutter静态检查（无问题）及两个界面测试，修正保存状态更新的条件语句格式；Windows Release构建成功。完整Release运行文件已复制到dist/GM管理器，保留data、DLL和运行配置，同时重新构建kungfu-desktop-admin.exe。候选dist/protocol-preview/LocalServer.exe已构建，未替换或重启当前本地服务。说明补充环境隔离、地图池限制、生效时机及版本冲突行为；本次不等于原生游戏或线上部署验收。

## GM关卡开关保存与审计验证

关卡开关现有源码提供stages_get/stages_save及GM页面，限制服务器已支持的地图池，不代表21370关卡进度或自动解锁已经实现。保存采用revision冲突检查，配置更新与前后值审计处于同一事务。

新增TestStageAccessLocalDatabase，独立debug库实际执行通过（3.940s）：首次读取、保存、拒绝旧版本、审计内容、审计失败时配置回滚、恢复开放以及损坏配置拒绝。测试仅使用连接级临时表遮蔽同名配置表，不运行迁移、不改变运行服务器配置，连接退出自动释放。新增管理HTTP鉴权及完整revision/地图参数传递测试；persistence/adminhttp/game/desktop测试通过。

本批未重新构建GM或替换运行服务器；关卡进度、任务、称号、VIP及其他完整目标继续开放。

## 关卡目录显示难度与奖励展示项

只读核对原config.spf2的PVEEntryUI记录，扩充ReadStageMaps与测试器展示DisplayDifficulty及RewardItem1/2/3，保留空项为0并拒绝负数/溢出/非数字ID。标记为客户端展示信息，不当作原版掉落概率，不修改发奖或解锁规则。合成字段测试及真实50条资源读取测试通过（0.598s），原文件哈希未变化。

mapmgr.xml是另一张97条地图表，包含称号要求字段；RoomSet的条件文案分支与其有关，仍需核对21372各字段和选择键来源。21370服务器业务、进度持久化与解锁仍开放。

## 21372记录索引及安全编解码

继续静态追RoomSet.sui消费者，闭合21372/24B记录+0索引及+20配置匹配值；证据保存在room-management/stage-records.asm。41CD20返回pair+4数据区，避免误将消费者pair+18当成报文+24越界字段。中间四个DWORD语义未定，保留原值；重复键在客户端覆盖旧记录。

新增protocol.StageRecord编解码：解析保留证据，编码拒绝重复键。测试器显示索引/未解字段/匹配值并提示重复键，最多显示8条，完整HEX保留。TestStageRecords及TestRoomSettings实际通过（0.550s）。21370服务器业务、进度落库及解锁仍未完成，不把编解码算成玩法完成。

## 对象事件真实TLS验证

在现有TestTalismanTLS独立debug库与临时TLS服务器中加入checkReliableTLS，实际通过（27.304s）。两账号走真实认证/P2P/建入房/加载/开战后，分别执行object-events-9000和object-events-9500：申请、接受、拒绝、取消、完成、重复请求/回复/完成、错误对象键、伪造控制者、完成早于接受、取消后迟到回复、控制者本机完成及9000错误战局。逐包比对转发内容，拒绝操作后使用ping屏障确认无误转发且连接存活；之后原法宝使用与离房流程继续完成。

只使用独立测试库临时账号，已清理记录及辅助SSH；正常测试战局序号递增。日志runtime-backups/reliable-tls-test.log。无需用户打开游戏，未改线上或替换运行服务。本轮仅新增测试和说明，无需重建未改变的程序。合成对象键验证协议流程，不等于原生地图物件存在或客户端动画效果正确，其余完整目标仍开放。

## 对象申请事件服务器链接入

9000/9500族接入battleMessage：严格长度及发送身份检查、同房实体限制，9000族额外校验房间/战局；本人申请/取消/完成，统一控制者回复。按玩家与事件族保存当前对象键、待处理/已接受状态和每阶段消息去重；不允许未经接受的普通玩家完成、伪造控制者、回复其他对象或取消后的回复。开局、恢复、离场清理状态，存量最多每玩家两条；相同对象竞争由原生控制者现有对象表裁决，服务器并非权威物件模拟。9500族原包无战局尾部，无法从包自身判定跨局延迟的新申请，不宣称完全消除这种原生边界。

TestReliableBattleExchange覆盖两族申请/接受/拒绝/取消/完成、重复、未知实体、错误对象及9000跨局，game包通过（0.997s）；protocol-tester测试通过（0.564s）。测试器增加对象事件构包入口，保留完整64位实体，诊断同步更新。真实TLS链和原生场景物件消费尚待验证，尚未替换运行服务；后续完整缺口继续开放。

## 4080统一战斗控制者

本轮静态闭合81E1A0→818910→4539E0，证明4080+11选择唯一实体+1B74控制标记，纠正每位接收者填写自己座位的实现。服务器统一选择房主座位；提取battleStartPayload，测试非零座位、房主切换、P2P花名册及上下文，game包测试通过（0.958s）。法宝TLS用例新增双方4080都选择房主的断言；原生效果与非控制端自然到期仍需区分验证，不自动替换运行服务。

独立debug库TestTalismanTLS实际通过（30.830s）：双账号认证/P2P/建入房、两端一致控制者、加载/开战及主动被动额度/重复事件/离房完整链。临时记录与辅助SSH清理完成，运行服务未重启；日志runtime-backups/controller-tls-test.log。候选LocalServer.exe构建及diff检查通过。测试器未执行原生游戏逻辑，不将此称为原生技能/BUFF画面验收；9000/9500对象业务及其他全量缺口仍未完成。

## 战斗对象申请表核验

进一步追A443B0/A444E0入表、A44340/A44470删除及4C5F20比较器，确认9000/9500族+47是无符号对象键，待处理表不是按网络序号或玩家复合键索引；恢复初始/取消/完成生产路径的+51=1/0。证据保存reliable-queue.asm与reliable-producers.asm，说明见ReliableEvents.md。不能直接把本机+1B74门当成服务器房主身份；对象目录/占用及控制者映射未闭合，未开放无校验转发。

共享解析与测试器字段命名已修正，新增未知状态诊断及0/1边界测试，TestReliableEventNativeLayouts通过（0.578s）。服务器业务仍待接入，本批不声称可靠事件已完成。

## 战斗9000/9500族实际分发与解析

静态闭合当前82D0F0到六个消费者及A3FBB0封装，证据见buff-debuff/ReliableEvents.md及完整函数ASM。恢复9000/9001/9002严格63B、9500/9501/9502严格55B、实体+39及前者上下文+55。发送封装重写本机身份，不能要求所有回复发送者都等于被操作实体。消费者有实际实体状态变更及往返发包，尚不能作为普通ACK无条件回声。

新增共享安全解析与测试器诊断，TestReliableEventNativeLayouts实际通过（0.572s），覆盖六命令、64位身份与边界。服务器授权/待处理关联尚未接通，保留为明确缺口；继续静态追A44340/A443B0/A44470/A444E0，不要求用户手工反复复现。

## 荣誉分级交付与真实关卡资源读取复核

当前源码已有honour.level_points分级及20370+24写入；本轮同步测试器说明并增加超出图标0–10、积分/排名有符号范围的诊断。分级是本服可配置策略，统一应用于历史查询，非原版曲线，未启用运行配置。完整挑战匹配仍缺。

新增TestStageCatalogueClientArchive：通过OPENKFO_CLIENT_ARCHIVE指定原客户端config.spf2，真实读取50条地图选择记录，验证组1四难度8110–8113，读取前后SHA256一致；测试实际通过。无需游戏窗口或网络，不分发资源文件。21370服务端业务和进度落库仍未接入，资源读取不等于关卡玩法完成。

本次game测试通过；protocol包测试程序启动时返回Windows Access is denied，未执行，不记为通过。未修改安全设置、重启现有服务或操作线上。

测试器新增诊断后的cmd/protocol-tester测试通过（0.564s），候选LocalServer.exe及Windows协议测试器发布成功，diff检查通过；未替换运行服务。修改前提交b30c24c与远端main再次核对一致，本批后续修改尚未提交。

## 法宝真实TLS完整链验证

TestTalismanTLS在独立debug库实际通过（30.983s）。临时本机game.Server监听、固定证书校验、现有测试器connect双账号认证及P2P注册，随后3010建房→3070加入→双方4030→4080→4160/4180→8040/8070。真实连接连续发送8292/4201，验证4206额度与同房8071转发、重复事件不转发不扣费；8291新序号重复施加但每局只扣一次；耗尽后4207且对方无效果。最后核对库存额度0、协议离房、清理临时账号/道具/回执及SSH连接。仅独立库战局序号正常递增，未触碰线上和运行主服务。

用例位于server/go-server/cmd/protocol-tester/talisman_tls_test.go；需OPENKFO_DEBUG_DSN且库名以openkfo_debug_开头，无DSN明确跳过。普通protocol-tester测试也通过（0.551s）。本批仅测试与记录变化，不需要重打未改变的运行程序。原生客户端动画、状态效果及后续关卡/称号/房间扩展仍未完成。

## 客户端关卡目录读取接入测试器

复用desktop现有SPF2只读解析器和XML读取，新增ReadStageMaps：从pveuiconfig.xml读取LogicType/Group/SelectDifficulty/MapID/MapType，检查缺字段、正整数与重复选择组合。测试器核心新增--stage-catalog入口；Windows界面新增读取目录按钮，无需连接或登录服务器，不改变资源及解锁状态。复用PrepareCore提取组件，避免维护两份提取逻辑。

desktop及protocol-tester测试通过；protocol新增编解码单测仍在执行前被Windows拒绝。Windows测试器构建通过。首次核心命令执行遭PowerShell重定向编码错误，改用标准隐藏Start-Process后返回拒绝访问，实际核心读取本机SPF2的端到端验证未通过；未改变系统安全设置。此前只读Python已能读取该资源，但不替代新增Go程序运行验证。

尚未实现进度落库、自动解锁和21370服务器回复；整体目标继续开放。

## 关卡进度安全编解码

继续核对9290F3列表数字与候选地图键比较，并只读查看pveuiconfig.xml的组/难度/MapID字段。新增protocol.StageProgress固定404B编码与解析，保留首DWORD，限制正整数、重复键和文本溢出，自动尾逗号及NUL；测试器接入地图键诊断。尚未增加自动解锁、奖励或21370服务器回包。

protocol包编译通过；其新增测试两次在启动临时protocol.test.exe之前被Windows拒绝（Access is denied），不能称通过，未修改安全设置。protocol-tester诊断测试实际通过（0.547s）。新增编解码完整单测待后续正常环境执行。全量目标继续开放。

## 21370响应族业务方向与文本边界

进一步追踪21373/21374发现难度单选项、地图名称和关卡开启消费者，纠正其不能直接作为房主/锁位设置的解释。+4起为逗号分隔文本，固定复制的400字节文本内必须NUL终止；原生661A70丢弃无尾逗号的末项。21371第二DWORD整数除100交AnimateDestItem，不猜货币含义。

证据更新nixiang/room-management。测试器新增终止符和尾项检查，protocol-tester测试通过（0.572s）。业务字段、持久化与服务器21370回复尚未完成，不伪造解锁数据。其余全量范围继续开放。

## 房间管理入口静态复核

新增nixiang/room-management/settings-consumers.asm与README，核对3140发送器及21370响应族。3140的第9字节来自玩家对象+107C，不能猜为主动移交操作。21371消费者严格8B，21372按24B记录流读取，21373/21374消费者无长度检查复制404B。因此新增测试器21370请求入口、响应诊断及边界测试，不向原生客户端发送空成功包或未知全零配置。

protocol-tester测试通过（0.581s），独立测试器重新构建。字段含义和实际服务器21370业务仍待补，主动移交/锁位/观战也仍开放；本轮未改变运行服务器。

## 法宝被动扣费与效果重施分离

核对当前82C0D0/82C150收包消费（75B，槽+39、实体+59、上下文+67）及9F94B0多处调用，完整函数保存在nixiang/talisman/use-receivers.asm。纠正上一版将“本局被动已扣费”同时当成禁止再次转发的处理：持久回执保持每局一次扣费，Member独立记录各实例/事件种类的最新序号，新的被动事件可同步，相同与旧序号不重复转发。房间恢复/新局清理该状态。这里只证明存在多处重施调用，未将其直接命名为已确认的复活函数。

UseTalisman重放仍须校验当前装备、状态与期限，避免已过期实例用旧费用回执重新施加效果。独立debug库复测通过：game 31.576s、persistence 37.161s；覆盖新被动序号不扣费但转发、重复及旧序号不转发、过期后重放拒绝。临时数据清理及SSH退出完成。测试器使用入口自动连续发送事件与4201，预览同时列出确认包；Windows构建通过。

运行服务未替换；原生效果与复活场景仍待客户端验收。其余协议范围继续开放。

## 法宝战斗配对与额度回包接通

8291/8292事件与随后4201按本人装备实例配对，待确认最长5秒（服务器保护策略）；校验认证UID、实体UID、房间/战局、装备槽及显式talisman_uses规则。无规则不开放。4201自报费用忽略，持久事务成功后只向同房他人转发事件，再向本人4206同步额度；不足4207，正常装备/期限拒绝走通知。配对状态在重登、切频道离场、离房、战斗恢复与新局开始清理。

当前被动策略按法宝实例每局一次，主动按事件序号幂等；重复被动通知不再转发，复活等需要重新施加被动的客户端语义仍需继续核实。此处不是权威战斗模拟：MP/动画/状态效果由现有客户端事件链消费。

TestTalismanRepairProtocolLocalDatabase扩展后独立debug库通过（27.788s）：修理→装备→战斗，主动扣4000、重放不重复、第二次扣4000、被动扣1000且换序号不重复，额度不足不转发，迟到确认不能跨局。另有无需数据库的伪造身份/房间/槽位及无规则拒绝测试。临时记录和SSH已清理。Go全包测试通过；候选服务与测试器构建，未替换运行服务。尚非TLS或原生效果验收。

配置格式：`"talisman_uses":[{"item":303002,"active_cost":4000,"passive_cost":1000}]`。这些数值仅是测试示例，额度使用百分之一单位，非恢复的原版费用；实际配置应核对talisman.xml消费者再填写，不自动改运行配置。

## 法宝额度消费事务

新增Store.UseTalisman及talisman_uses回执表：仅服务端传入费用，账号行锁内校验本人type30实例、配置ID、37/38装备槽、状态及期限；额度不足拒绝，不做无符号下溢。扣额度与操作回执同事务，重复返回当前额度且不再次扣除，重放更换实例/配置/事件种类/槽/费用拒绝。支持服务端显式零费用被动策略。

独立debug库TestTalismanEquipmentLocalDatabase实际通过（30.108s），覆盖修理与双槽原有链，并新增同事件并发只提交一次、连续消耗、旧事件重放不恢复额度、额度不足、未装备、跨账号、错误槽、篡改重放和零费用被动。临时记录与辅助SSH已清理。persistence、game、protocol、protocol-tester测试通过；候选LocalServer.exe构建成功。运行服务未替换。

此批为持久层实现；尚未连接8291/8292与4201的网络配对，也尚未启用4206/4207战斗回包。自动测试不代表原生客户端效果验收；其余全量协议目标继续开放。

## 法宝使用槽位与安全解析

静态追当前客户端9DE410、9F94B0及9F9560，确认8291/8292的+39为装备槽37/38，保存完整函数到nixiang/talisman/slot-use.asm。新增protocol.ParseTalismanEvent和认证身份/房间上下文Match校验；测试覆盖64位UID、错误槽位（含截断伪装）、错长、他人实体、不同战局和未初始化字节不参与语义。测试器接入解析并纠正历史测试样例槽位2。protocol及protocol-tester测试通过。

收包顺序确定为战斗事件先于4201；实际额度消费与事件配对仍待实现，尚未开放无校验透传。测试器重构建；运行服务器保持不变。

## 法宝修理网络链与测试器接通

当前源码已接4202/4203报价及4204/4205修理；服务器显式规则校验，等待阶段准备后拒绝修理，库存同步先于成功回包。TestTalismanRepairProtocolLocalDatabase在独立debug库实际通过（11.893s）：不存在实例、错误材料、32B报价字段、2161额度更新、2162材料耗尽删除、4205结果、相同请求重放和新请求满额度拒绝。临时数据清理，SSH辅助连接退出。此为真实数据库与Hub分发验证，尚非TLS或原生UI验收。

现有测试器新增报价及确认修理入口，法宝装备可选37/38；4204诊断纠正为实例、材料配置、保留字段。game及protocol-tester普通测试通过。候选构建不自动替换运行服务器。4201及8291/8292主动/被动使用闭环仍未完成；下方较早“修理网络未接入”描述为历史记录。

## 法宝修理事务与双槽数据库验证

从99AA80确认库存+23 uint16为法宝剩余额度百分之一单位，实例键来自库存+0。新增RepairTalisman：服务端显式规则，账号行锁、材料跨堆叠扣除、额度更新、幂等回执同事务；不读取客户端自报费用。独立测试库扩展用例通过（19.743s），也完成之前未验证的双槽37/38共存。临时数据已清理；未替换运行服务器。4202/4204网络入口和自动使用消耗仍待接入。

## 法宝回包字段和第二槽

恢复4203的32B报价消费、4205错误码、4206的12B额度刷新、4207不足通知；修理请求使用材料配置而非金币报价。证据见nixiang/talisman/replies.asm。根据A28290的双法宝37/38分支，服务器允许type30第二槽38；数据库双槽共存用例已扩充，尚未重新执行带DSN测试。测试器回复诊断及普通persistence测试通过，扣除修理材料/额度持久化仍待补。

## 法宝使用/修理消息恢复

确定4201–4207及8291/8292枚举，恢复4201/8、4202/4、4204/12和75B战斗事件的部分字段，原始函数证据见nixiang/talisman/use-repair.asm。发现被动4201的第二DWORD没有明确初始化，不可用作客户端自报消耗扣费。测试器诊断及边界测试通过；实际扣费/额度/修理回包仍需核实。此批没有新增服务器放行规则。

## 宠物法宝装备入口

当前资源与8B3890仓库消费链确认type30、槽37，证据见nixiang/talisman。服务端Slots新增30→37，现有2080/2090和2300/2310事务/库存同步复用；测试器新增固定槽37的宠物法宝装备入口。独立库装备替换/归属/非法槽/卸下测试通过（10.769s）。主动效果、消耗和GM完整配置仍未完成。

同批普通persistence单测通过；game.test.exe有一次Windows Access is denied，未执行该批game回归，不当作逻辑失败或测试通过。未改变安全设置或替换运行服务器。

## 赠送真实TLS验收

TestGiftTLS实际通过（16.115s）：在临时loopback监听器启动真实game.Server，使用现有测试器connect完成证书验证、SDK/角色/频道/P2P认证；通过9090收到1230/9100，原先离线的收件账号连接后1300/1310、1320/1330查询，再连续发送2171与1340，验证2160先于1350、单件库存落库。删除后重复2171不额外发库存，ping仍返回pong。临时账号/商品清理，SSH辅助连接退出。测试仅独立debug库，不替换运行服务，也不证明原生UI动画。

用例位置：server/go-server/cmd/protocol-tester/gift_tls_test.go；需OPENKFO_DEBUG_DSN指向独立debug库，运行go test ./cmd/protocol-tester -run '^TestGiftTLS$' -count=1。数据库名在打开Store之前验证；无DSN时明确跳过，普通go test通过不能冒充该网络验收。

## 赠送网络入口接通

本轮独立debug数据库TestGiftLocalDatabase实际通过（17.160s），先前Windows临时测试程序拒绝执行未再次发生；未修改安全设置。9090现接Store.Gift，提交后1230更新点券、9100发送非空成功通知。货币不支持仍9110/56，其余失败走已知通知，不猜错误索引，也不保证数据库提交不确定错误时未扣款。

TestGiftProtocolLocalDatabase通过（13.923s）：真实数据库经Hub分发，发送人重复同操作只扣77点券；收件人投递时离线，之后登录查询1300/1310、1320/1330、2171/2160、重复领取无额外库存、1340/1350删除，确认余额和库存。临时账号/商品清理，辅助SSH关闭。不是TLS或原生UI验收，运行服务器未替换。

测试器赠送入口改为实际操作，收件UID支持完整64位，留言按服务器200个GBK字节上限，新增9100/9110诊断；新点击仍是新交易。当前只支持已验证的普通点券单件商品；特殊折扣/索取/其他附件奖励和主动新邮件提醒未完成，收件人可查询列表。

## 默认装备槽扩展

复核当前PE的660CE0与660D6C/660DA4跳转表，恢复12→4、13→3、14→7、15→2、16→6、17→5、20→10、21→11、25→8。服务端2080 slot=0现按该已支持类型集合解析默认槽，仍在原归属/状态/期限事务内完成；2300的slot=0卸装语义不变。套装18、消耗品64及未支持类型不自动套用默认槽。TestNativeDefaultEquipmentSlots通过（0.523s）。静态证据保存到all-weapons目录equip/current-default-slots.asm。

同一函数另有31→42、71→32、75→35、76→36；尚未验证这些类型的完整装备/消费链，未加入服务器允许槽。候选商品目录中31标为徽章，30为饰品；没有依据把它们直接命名为宠物/法宝。下一步需追其客户端调用与资源配置。

赠送独立库复测仍在执行前Access is denied（30.786s），未进入测试主体。随后修改默认装备槽并重新编译的persistence单元测试能够执行，不据此宣称赠送数据库测试已通过，也未修改系统安全设置。

## 测试器换座入口

现有Windows测试器新增3260申请、3262同意、3263拒绝；原样回复本人收到的3261，不重建对方UID/座位。3264/3265/3115/2070/3100清理缓存申请。Go诊断器增加双方UID、座位及拒绝/忙碌解析，独立HEX测试覆盖64位无符号身份和错长包。cmd/protocol-tester及internal/game测试通过；完整EXE构建后仍需区分测试器出包与原生画面表现。

## 接续验证：邮件领取与换座状态清理

- 独立debug数据库的TestMailClaimProtocolLocalDatabase已实际通过（44.384s），覆盖领取、重复领取、删除及坏附件领取失败后自动删除保护。临时账号清理和辅助SSH退出已执行；不是原生界面或TLS验收。
- 同批TestGiftLocalDatabase仍在启动persistence.test.exe时被Windows拒绝（Access is denied），测试主体未执行。9090继续保持未开放，不因此启用扣款。
- 3260/3262/3263换座实现补充状态失效处理：离房、换队/设置/装备等清准备路径，以及准备/取消准备时撤销待确认请求，向双方发送3264；迟到的同意请求不能改变座位，也不遗留忙碌状态。3265成功仍交换座位及队伍，不改独立Spawn字段。
- 新增TestSeatExchangeInvalidatedByRoomChange；本轮internal/game全包测试通过（0.952s，独立库测试默认跳过）。未部署运行服务器，赠送和其他全量范围继续开放。

目标：按 protocol/nixiang 和当前客户端证据补全服务器缺失协议，扩充现有独立测试器模拟多账号及异常请求。完成必须包括解析、鉴权、状态/持久化、正确回包、自动验证；不以空包或测试器菜单冒充实现。不修改线上配置或数据，不自动提交推送。

## 全量范围（保持开放）

| 范围 | 当前状态 | 下一步证据/验收 |
| --- | --- | --- |
| 大厅2250/2270玩家列表 | 已接真实在线会话和数据库资料，自动测试通过，未部署主服务器 | 已恢复UID、名称、性别、等级及7/10分页；声望/附加身份未配置，保留零；原生UI待验收 |
| 创建角色1125/1150/1151/1130 | 第一批代码与独立库测试通过，未部署 | 接新建无角色账号入口、完整候选配置、现有测试器创建模式、真实TLS回归；当前仅支持现有单角色账号模型，多角色仍未完成 |
| 道具到期/删除2121/2162 | 2162增量删除已实现并通过TLS；到期业务未完成 | 已确认2120/2121同时登记，均可标记状态2，2121另有提示；继续持久期限、到期调度和卸装 |
| 武器升级21410/21411 | 配置响应及可选升级事务已实现；原生UI待验收 | 等级、熟练度阈值、金币、显示成功率、攻击加成；可选自定义规则、账户行锁、扣费/库存/审计事务已接入；未启用运行配置 |
| 荣誉挑战战绩20360/20370 | 已接自定义规则累计与真实历史查询；分级未完成 | 荣誉排名/值/等级、挑战场次/胜场；目录/按期累计已接；荣誉分级与完整挑战匹配待补 |
| 其他战斗事件8127/8155等 | 8127本人MP与8155计分同步已接入；可靠确认族等仍待实现 | 8155逐槽身份检查、房主授权与测试器诊断；原生画面及多房型验收未完成 |
| 宠物/法宝 | 待恢复/实现 | 商品、库存类型、装备槽、效果完整链 |
| 赠送9090（纠正旧9041标注） | 426B解析和测试器入口已接入，业务未完成 | 邮件附件投递/领取、商品键映射、扣款事务、幂等和通知；当前明确提示未开放，不扣款 |
| 称号 | 待恢复/实现 | 获取/持久化/选择/显示协议；奖励规则可配置 |
| 主动移交房主/锁位/观战 | 待恢复/实现 | 请求字段、权限、容量及房间状态一致性 |
| 多个独立频道 | 身份、隔离、多记录目录及真实TLS切换已实现；未部署 | 原生客户端频道显示/切换画面验收；测试器支持指定频道ID |
| 已有功能回归 | 持续 | 重登、切频道、房间、战斗、BUFF、结算、库存不回退 |
| 推荐优惠 | 仍遵守用户暂缓 | 本轮不擅自恢复该项 |

## 商城购买期限（2026-09-19 接续）

- 新增offer_lifetimes保存显式服务器天数；无策略/0为永久，绝不自动把旧商品显示365天转换成服务器失效期限。
- AdminOffer.server_expiry_days为可选字段：缺省保留旧策略，显式0改永久，最大3650天；随商城保存审计同事务。批量改价、上下架继续保留策略。
- Purchase在商品行锁内读取期限策略，扣款、库存、失效时间和幂等回执同事务提交。重复购买请求使用原实例，不重建/延长期限。
- GM商城增加“购买后按上述天数到期”；仅影响后续购买。堆叠道具暂不新增此入口。实例期限和商品期限是独立设置。
- Go全包测试、Flutter商城保存/永久与限时切换测试及shop_config.dart分析通过；独立测试库TestPurchaseExpiryLocalDatabase通过（16.996s），测试临时账号/商品已清理。原13316隧道返回EOF，测试使用临时13317 SSH隧道并在结束关闭，未重启现有服务；只访问openkfo_debug_库。
- 主运行服务器与线上服务仍未替换；战斗到期策略、原生界面到期展示、武器升级等全量范围继续开放。
- 加入期限行锁后独立库复测通过（17.299s）；GM额外要求expiry_policy_saved确认，避免旧线上接口忽略新字段后误显示期限保存成功。商城单项保存、批量上下架及批量改价共4项Flutter测试通过。

## 第一批：创建角色（2026-09-19）

新增 persistence/character.go：1125选项编码、1150严格解析、服务器选项ID到资源ID映射、七个初始装备生成、CreateCharacter事务。只有全零360字节Profile且无库存的账号可创建；已有角色不可覆盖。character_creations保存原始请求并限制新建角色重名；同一请求重试不重复发装备。

新增 game/character.go：bootstrap的空角色分支下发1125；1150返回1151；第一次3320按已验证创建回执语义返回真实角色ID3330和1130，后续进入现有选角/频道链。

失败应答核对当前gfld.dat 0x822BC0：WORD >= 0x82 走通用失败分支，故回1152/2(82 00)，不猜文案索引，不泄露数据库错误。

验证：
- character_test.go：合法布局、不同服务端/资源ID、非法GBK/昵称/性别/变体/跨槽和未下发选择、重复候选拒绝，通过。
- character_integration_test.go（persistence）：独立openkfo_debug数据库，同账号并发相同请求幂等、重名拒绝、已有角色保护、拒绝后无部分数据，通过；仅使用新生成测试账号，结束清理。
- character_integration_test.go（game）：独立库驱动1125→非法1150/1152→正确1150/1151→重复请求→首次3320/3330/1130→选角3320/3330/1201，通过。
- 以上为解析/数据库/Hub协议分发测试，尚非真实TLS和原生UI验收。新代码没有替换运行EXE。

执行命令：从server/go-server运行go test ./internal/persistence ./internal/game；独立数据库测试需OPENKFO_DEBUG_DSN且数据库名称必须以openkfo_debug_开头。凭据不写入文档或测试日志。

## 本轮接续：修改前提交与测试器创建入口

- 修改前源码检查点已推送main：b30c24c759624933975de1c9893a938c6e9ab16a；保留远端README更新。原始candidate及历史逆向实验目录未纳入该提交。
- 测试器增加可选创建昵称，加密保存到现有设置；仅收到1125时创建，已有角色不会覆盖。
- creationRequest增加原始昵称GBK往返检查，避免无法编码字符被截断后变成其他昵称。测试覆盖七槽选择ID与资源ID不同、缺槽、重复候选、超长、空白控制字符及无法编码字符。
- 本轮Go测试通过；Windows测试器重新构建。尚未重新执行独立数据库TLS测试或原生游戏界面测试，未替换服务器EXE。其余全量范围继续开放。

## 大厅玩家列表（本轮）

- 新增player_directory.go：2250长度8，页号从1开始，容量7或10；只列当前lobby/room会话，按UID稳定排序。越界页收敛到末页，空名单保留1页。每次只查询当前页uid/nickname/profile，不加载库存或凭据。
- 2270按8+68N编码真实UID、GBK昵称、性别和等级；可选声望、会员背景、自定义等级图标仍未实现，不伪造原版值。
- 当前PE静态链：92DBB0请求；92DAE0容量；8243D0接收；549600页数上限；92E6C0→8978F0→896F20绘制。记录+29选Male/FeMale.png，+30读取等级图标；+48为txtReputation；+56自定义等级资源，+64==4显示picVipBG。
- TestPlayerDirectoryPagesAndLifecycle / TestPlayerListRecordNativeFields通过。TestCharacterCreationTLS本轮扩展为双真实TLS账号列表查询、退出后名单更新，独立测试库通过（7.273s）；新临时账号结束清理。打开数据库前新增名称前缀校验。
- 现有测试器增加在线玩家按钮；服务器候选构建到dist/protocol-preview，不替换正在运行的本地服。多频道隔离和全量范围其他项目继续开放。

## 库存增量删除

当前PE确认2162是单个实例DWORD；syncInventory已改为逐实例通知，覆盖删除全部、重复同步及跨玩家隔离。真实TLS临时账号删除库存后查询验证通过（6.938s），测试账号已清理。证据见nixiang/inventory-lifecycle。到期业务仍未完成；未替换运行服务器。

## 到期持久化与查询链（继续实施中）

- 新增inventory_expirations，显式UTC Unix秒，主键(uid,instance)，绑定库存复合外键、删除级联。没有期限行即永久，绝不从8760/365+显示值推导失效日期。
- Snapshot先处理当前账号到期项；账户行锁内标记状态2、清装备槽、记录processed，重复处理幂等。装备和消耗在事务内重新检查到期，拒绝失效物品，防止绕过只读快照。
- syncInventory下发2161后对首次过期的已知实例发送2121(count+实例数组)，重复查询不重复提示。
- 独立库期限测试通过（3.823s），覆盖已到期、未来、永久、装备拒绝和幂等。真实TLS创建/重登/双玩家列表/过期更新/删除完整测试通过（8.417s）。仅新临时账号，结束清理；测试执行了新增表迁移，没有为既有玩家添加期限。
- 未完成：在线主动调度、GM期限配置入口、购买时配置期限、战斗中到期策略及原生模型刷新验收。本轮不能称完整到期业务交付。正在运行的服务器未替换。

## 在线到期主动刷新

- 每个认证连接使用15秒ticker，连接结束自动释放。RefreshExpiredInventory校验当前会话、频道阶段和房间阶段；只在大厅/等待房下发模型变化。查询当前账号明确到期实例，仅有缓存仍有效的实例才读取完整Snapshot。DB错误记录账号UID并留待下一轮，不因此断开玩家。
- 顺序：2310卸装(instance+68B记录)→2161库存状态→2121过期提示。等待房装备改变还会清准备并向其他成员3090同步。加载、战斗、结算阶段暂不主动改模型；事务内到期装备/消耗限制仍立即生效，战斗完整策略尚需完善。
- 实际服务器ticker真实TLS测试通过（22.690s），全程无库存查询触发，验证自动卸装/失效通知，随后删除和重复查询继续有效。阶段跳过单元测试通过。
- GM期限配置入口、商品期限配置和战斗边界回归仍未完成；运行服务器未替换。

## GM实例期限入口

- inventory_expiry经现有认证管理接口/local与online路由。目标为uid+instance，expires_at为绝对UTC Unix秒；0代表未失效实例改永久，非零必须未来且10年以内。账户行锁与desktop_admin_operations审计同事务，同请求号不同参数拒绝，已到期/失效实例不可复活。库存接口返回真实expires_at。
- 同一GM界面增加实例期限弹窗；锁定操作期间环境和账号，支持1–3650天/永久；明确区分服务器期限与客户端原文字。
- 独立库Admin期限/审计/重试/篡改/无所有权/已失效测试通过（6.773s）；管理API认证范围测试通过；Flutter新增期限交互以及既有发放、线上范围、移动布局共5项测试通过，main.dart分析无问题。全目录分析仍有既有online_manager_test.dart第35行单条花括号info，非本次错误。
- Windows GM构建完成，完整运行文件已更新dist/GM管理器，保留原gm-settings.json；线上未部署，现有本地运行服务器仍需替换候选版本后才能自动处理期限。商品购买时期限设置和战斗到期策略仍未完成。

## 结算回房时立即同步失效库存

- 原生3550(0)此前仅清结果图标，没有同步期间到期的库存。现在首次从settlement返回时先取权威Snapshot，成功后切换状态、广播3550，再依次2310卸装、2161更新、2121到期提示；装备改变向同房其他人3090刷新并清准备。
- 3110兼容回房复用已读取的Snapshot，在原房间重建通知后同步库存，避免再等15秒ticker。抽取已有卸装/库存更新逻辑供回房和ticker共用。
- TestExpiredEquipmentSynchronizesAtEachPlayersReturn覆盖两条回房路径、两个玩家分开返回、尚在结算的玩家库存缓存不提前改动、重复3550不重复发失效通知。原有结算状态测试补真实Snapshot测试替身；Go全包测试通过，服务器候选EXE已重建，运行服务未替换。
- 这是服务端边界与协议队列测试，尚非原生游戏模型验收；本局战斗模型仍沿开战快照，加载/战斗中不主动卸装，装备和消耗事务继续拒绝到期项。战斗其他事件/升级等全量范围未完成。

## 武器升级配置响应（2026-09-19）

- Config.weapon_levels 接入21410/0→21411，每条21字节，无数量头。字段来自current-fields.md；未知字节16原值保留。等级要求从0连续排列，熟练度阈值非零，显示概率0..100；256条为实现限制，不代表原版等级上限。启动及查询均验证配置。
- 未配置时只返回20150提示，不发送空21411或合成全零表；未设置任何默认升级数值，也未修改运行配置。21412/4目前明确提示尚未开放，不扣款、不更新库存、不伪造21413。
- game协议测试覆盖两条手写HEX记录（含非对齐无符号字段）、重复请求、大厅/房间、收件人隔离、战斗阶段跳过、非法长度及配置。测试器菜单说明同步。完整升级事务和结果回执仍未完成。

## 升级结果消费复核

完整反汇编证明21413消费者只读取+0结果标志与+9实例ID，860120从库存取68字节覆盖UI缓存，未找到则得到全零记录。由此确认库存更新必须先于结果通知。测试器新增21413诊断和合法/非法长度、多记录、非零成功标志、无符号实例ID测试。服务器扣费升级仍未开放；未更改运行服务。

## 可选武器升级事务

显式weapon_upgrade_mode=consume_score_keep_level启用自定义规则，默认关闭。21412按本人库存实例、类型、状态、期限、等级、熟练度及金币校验；同事务写金币、武器等级/熟练度和抽样审计。持久操作回执防同操作重复扣费；原生请求没有业务幂等号，新消息/新点击是新尝试。独立测试库TestWeaponUpgradeLocalDatabase通过（20.175s），仅临时账户，清理完成。Go测试通过；正在补协议层出包顺序验证，未部署主运行服务。

- TestWeaponUpgradeProtocolLocalDatabase通过（8.078s）：真实独立库经Hub分发验证成功/失败的1240→2161→21413顺序、同消息操作重试及资源不足不扣费。临时账号清理、SSH辅助隧道关闭。全Go包测试通过，服务器候选和测试器EXE重建；这是数据库和协议分发验证，非原生游戏动画或真实TLS升级验收。

## 荣誉挑战战绩字段恢复

当前PE确认20370属于RoleInfo.sui荣誉/挑战战绩，不是全部普通对局统计；五个DWORD字段及说明、图标、胜率算法已恢复。首DWORD=0会清空页面，旧占位英文实际上不显示。测试器支持目标UID与历史页参数、字段解析、空页/错长/字符串结尾/计数风险测试。57字节历史目录来源和挑战业务累计仍待核实；证据见nixiang/honour-profile。

## 荣誉历史目录1035

静态闭合1035→A2B930→41BEB0→世界对象+18C→RoleInfo历史上限。Bootstrap现在发送57字节单期初始化记录，首DWORD=1，其他字段未解保留零，不虚构战绩。测试器识别1035，登录传输测试及独立编码断言覆盖顺序和长度。20370持久查询/挑战累计仍待完成，原生UI未验证。

## 荣誉累计与真实查询

新增honour显式模式/期名/胜负平积分配置；默认不累计。结算同事务写honour_stats，重复serial复用回执，计数/积分封顶INT32_MAX。1035同步当前期，20360按目标与期号返回20370真实荣誉值、排名、挑战场次/胜场；缺失记录清空旧页。荣誉等级尚未分级，不等同全部挑战功能完成。Go全包测试通过；正在核对独立库累计/重试/历史隔离/回滚测试。运行配置未启用新规则。

- 独立库TestHonourSettlementLocalDatabase复测通过（17.168s），覆盖重复结算、双账号排名、历史隔离、不一致结果回滚及有符号上限。首次仅清理阶段被初始库存外键阻止，已按严格测试账号标识清除遗留两账号并修正先删库存的清理顺序；复测无残留。服务器候选与测试器重建，全Go包测试通过。未替换运行服务、未启用现有配置，尚非原生UI验收。

## 8127 MP同步

当前PE AddMP脚本注册证明旧HP标签错误。63B本人MP同步已接服务器白名单，恢复目标/变化/最终值/房间上下文，有限浮点/归属/序号验证与原样同房转发。新增TestMPRelayAndIsolation通过，测试器加入数值输入及重放偏移。8155比分批量尚未开放，原生MP显示未验收。

本轮GM宠物／法宝页面Windows release构建通过，完整运行目录已更新dist/GM管理器，app.so与构建产物哈希一致。运行服务器及配置未修改。
