# 频道切换与账号登出分离

2026-09-19，当前客户端 gfld.dat 静态验证见 native-channel-leave.asm。

- 0x910790 发送 2060/0，然后设置换线状态；0x910C00 的频道连接路径在 0x910E3F 调用该函数。因此 2060 不能独占解释为退出账号。
- 旧实现回2070后删除 Hub.Sessions、设置 LoggedOut，清空 P2P，并在短超时后断开认证隧道；原生切换流程后续无法复用认证连接。
- 新实现继续返回2070/0，但只离开房间、取消 Bound、清理旧游戏通道与游戏缓存。保留认证隧道、SDK、表就绪状态和 P2P 注册。发放两分钟的1010/2010换线授权；旧通道标记closed，迟到数据忽略，旧close不影响新通道。2010成功后消耗换线授权。
- 真正密码再认证沿用Bridge现有 tunnel logout → logged_out，再建立新认证会话；没有修改游戏二进制，不加游戏重启兜底。
- 这次不新增多个频道或虚构频道目录。先修复现有 Local Lobby 频道重新进入的连接生命周期。

测试：channel_switch_test.go验证同隧道2010换线、其他玩家保房、迟到旧数据/close、重新1156绑定和授权过期/重复使用。recovery_test.go及relogin_peer_test.go继续验证真正账号退出与重登。真实TLS测试relogin_live_test.go在initial场景加入直接2010及1010→3320→2010两种切换，再执行立即/延迟65秒/换账号重登。协议自动验证不等于原生画面实测。
