# 商城赠送入口纠偏（2026-09-19）


## 在线邮件提示控件链

当前PE证据见mail-refresh.asm：1310/826C30按339字节建列表后调用8BCC80；后者重建面板列表、更新分页并调用8BC790。8BC831检查拷贝记录+327的DWORD（0为未读），检测到未读后向91C590或7FB310传1，否则传0。两函数读取B99E18/B96F30，均指向BAFE6C字符串anmMessageTip，调用75AA30控制控件。注意该遍历起点取面板当前页，不能据此声称全邮箱未读计数。

服务器采用真实1310列表刷新，不猜测新的通知包号。新赠送事务提交后标记在线收件人的MailDirty，大厅/等待房立即刷新；战斗/加载阶段推迟，回到安全状态后由现有15秒检查补发。刷新失败保留标记，不撤销已提交赠送或断开发件人；同一操作重放Created=false，不重复推送。离线玩家仍由正常1300查询获得邮件。

TestGiftProtocolLocalDatabase离线/在线/战斗三个子场景独立debug库实际通过（46.879s），含重复赠送、延迟刷新、详情领取及删除。新增通知的完整TLS已由TestGiftTLS在线子场景通过（与离线子场景共37.277s）；原生动画显示仍未实机验收。

## 当前实现更新

补充：真实TLS用例TestGiftTLS已通过（16.115s），覆盖离线投递后登录查询、连续领取/删除、重复领取不发物品且保持连接。原生游戏界面仍未验收。

9090已接通扣点券与邮件投递事务，1230余额后9100非空成功通知。独立库并发幂等测试通过（17.160s），Hub完整赠送→离线收件人上线查询→详情→领取→重复领取→删除测试通过（13.923s），临时数据已清理。下方“尚未开放”段落保留为历史过程，不能作为当前源码状态。运行服务器尚未替换；TLS及原生界面仍待验收。仅普通点券单件附件，特殊字段、其他奖励附件与主动邮件提醒仍待补。

当前PE证据：[native-gift.asm](native-gift.asm)。旧清单将9041/169直接标成赠送，不足以作为开发依据。

85C7E0在85CA13发送9090，长度0x1AA=426。包先清零，再写以下字段：

| 偏移 | 字段及证据 |
| --- | --- |
| 0 | DWORD 109，点券货币标记 |
| 54 | qword目标UID提示；名字目录查询998B30未命中时为0，不是认证身份 |
| 83 | 收件人名字，21字节区域；4B1970使用容量21复制 |
| 145 | DWORD商品选择值，来自传入商品记录+9；业务需再核对与数据库catalog_key的映射 |
| 157 | DWORD点券报价，来自商品+38或折扣+42；服务器不能信任客户端价格 |
| 165 | DWORD来自商品+77，业务含义未恢复 |
| 169 | 单字节第四个函数参数，含义未恢复 |
| 170 | 留言文本，256字节区域；4ED4C0使用容量256复制 |

接收9100的A2D550要求非空包，不读取结果字段，刷新三组界面后显示“道具已经成功被赠送出，对方将在邮件中收到您的馈赠！”。9110的A2A120读取WORD：56为只能点券赠送，19为收件玩家不存在。其他错误直接索引文案表，不能套用9060的130通用错误。

9041发送器8406F0在本次观察路径中把两处UID均填本人，名字也取本人；它发送169字节并进入购买成功/买一赠一相关界面路径。因此不能据相邻包号认定它就是指定收件人的赠送入口。它的完整业务用途仍待追踪，不贸然开放扣款。

本批代码：新增9090精确长度与GBK/NUL文本解析，保留未解字段；服务器显式提示邮件投递未开放，错误货币返回已确认的9110/56。没有扣款、发货或伪造9100成功。测试器改为真实9090请求布局，移除错误的9041赠送入口。测试覆盖长度、名字与文本边界、非法GBK、64位UID、零UID目录未命中、阶段隔离及重复请求不访问数据库。

未完成：邮件列表、附件投递/领取请求和回执；名字与UID一致性、商品选择键映射、未知字段165/169、赠送资格、扣款与持久投递事务、领取幂等、在线/离线通知。下一步沿原生邮件界面继续追踪，不能把当前拒绝提示算作赠送功能完成。

## 邮件链路恢复

当前客户端有Mail.sui/frmMail、btnMailCheckout、btnDelMail及分页控件，见BBEFAC、BBEFB8、BBC3C4、BBC3B8字符串。旧增补.js“邮箱系统不存在”不能用于当前PE。只凭赠送成功文案也不足以证明收取流程，以下为实际按钮和接收器证据：[native-mail.asm](native-mail.asm)。

| 消息 | 当前证据 |
| --- | --- |
| 1310下行 | B22F0C注册826C30；按339字节分割数组，送8BCC80重建列表；空数组有效 |
| 1320上行 | MailCheckout按钮经8BAE30→8BA780，12字节：本人UID qword+邮件记录首DWORD；之后打开详情窗口，并非已经领取附件 |
| 1330下行 | B22E14注册826AB0；8BA5D0按+8关联键更新列表状态，89F380至少复制136字节进入详情；完整各字段继续恢复 |
| 1340上行 | 删除确认8B9160→8B8FE0，12字节：本人UID+邮件记录首DWORD |
| 1350下行 | B22F5F注册826CF0；+0字节非零才按+1 DWORD邮件键移除列表项。处理器未验证包长，服务端必须保证至少5字节 |
| 1410下行 | B22EBA注册8269C0；124字节记录数组，+8查库存实例、+12改库存状态，交8BBC20列表。具体列表业务仍需核对，不能等同1310 |
| 2171上行 | 89EE80先发12字节：本人UID+详情对象+580字段，即1330回包+8的关联键；随后1340使用对象+578，即1330回包+0，两个键不可混用 |

领取后紧接删除意味着服务端不能依赖客户端先后两包来保证原子性。后续应在领取事务内完成归属校验、发物品与领取回执，再让重复领取/随后删除不重复发奖。删除未领附件的行为须按真实按钮提示确认，不能无条件自动领取。

本批测试器新增1310/1410数组边界、1330最小安全长度和1350删除键诊断，覆盖空列表、两条记录、非对齐DWORD、截断及无符号键。未新增数据库邮件表，也未开放扣款；下一步继续恢复339字节列表与136字节详情字段及领取应答。

## 列表及附件预览编码器

8B8F30和89EE30均按0x54 DWORD+WORD+BYTE拷贝，证明339字节列表记录没有额外内部填充。89EFB0读取+4标题、+83发送人、+126正文；8B9B80读取+125的剩余时间显示原值（单位未确认），+327为新/已读状态（BBC514/BBC518文字）；+331为关联附件键，+335用于选择普通附件/9091索取详情类型。

1330消费89F380复制136字节，+12→txtGold，+16→txtCoupon，+20→txtExp，+24→txtReputation；+32为物品类型，+33为物品配置ID，+50/+54为期限显示字段，对应28字节头后108字节商品记录的相同偏移。删除键在+0，领取使用+8；两者不能互换。

新增protocol.MailRecord.Encode、MailDetail.Encode、ParseMailAction：339字节列表、136字节附件预览、12字节操作归属校验。未知区域留零；标题和发送人采用服务器保守20个GBK字节上限，不宣称标题原生容量只有20；正文最多200个GBK字节，保证在+327状态前NUL终止。+125用RemainingRaw保留原值，不猜天/秒单位。附件预览不会发物品或改余额。

测试采用独立预期字节，覆盖中文GBK、字段偏移、目录108字节完整复制、缓冲区不共享、超长/NUL/不可编码字符、64位身份及错账号/零键拒绝。测试器显示真实字段；这些编码器仍待接邮件存储、归属查询和领取事务，不能据此称赠送已完成。

## 邮件存储与本人查询已接入

mailbox保存服务端生成的列表/详情快照、收件UID、已读/已删除/已领取标记。1300返回本人未删除339B记录数组；1320校验信封UID及邮件归属，在行锁事务内置已读，回1330预览；1340校验归属并软删除，回1350结果和原邮件键。重复删除本人同一邮件成功，其他账号不能查询或删除。删除保留原记录，后续领取事务必须检查deleted和claimed，不能只凭旧附件键发奖。

列表单次上限2048条（超出报错，不静默截断），是当前服务端保护限制，不是原生容量。数据库快照由服务端构造；未开放客户端上传任意邮件记录入口。未接赠送投递及领取，claimed当前不会由查看/删除修改。

独立测试库TestMailboxLocalDatabase通过（8.672s）：新建临时账号和邮件，验证跨账号隔离、重复查看仅置已读且余额不变、附件键与邮件键不同、重复删除、删除后不可见及保留删除记录；账号与邮件结束清理。新增表仅迁移独立debug库，未改线上。测试器增加列表、详情、删除入口；本次不是原生游戏画面验收。

## 单件附件领取事务

新增mail_attachments保存收件UID、独立附件键、所属邮件、68字节发放快照、领取后生效天数和已领取库存实例。ClaimMailItem先锁账号，再锁附件及所属邮件；校验归属、删除/领取状态、快照长度、未装备槽和期限。库存插入、到期行、附件领取实例、邮件已领取标记在同一事务提交。

并发同附件只产生一个库存实例。重试读取当前库存，不把已删除/已消耗物品重新生成；已领取邮件随后删除仍保留领取回执。未领取邮件若已删除则拒绝领取。当前仅做单件物品附件，金币/经验等不从预览字段直接发放。

独立库扩展测试通过（17.287s）：并发两次领取同实例、仅一条库存、限时期限、跨账号拒绝、领取后删除再重试、库存移除后不复活、先删除未领邮件后拒领。仅临时账号，结束清理；临时数据库隧道已关闭。

尚未接2171网络入口：原生客户端领取后立即1340删除，必须补齐领取失败时的删除保护及正确通知顺序再开放。赠送扣款/附件投递同事务及自动TLS完整场景仍未完成，不将存储单测视为赠送已可用。

## 2171入口及自动删除保护（已编码，运行验证受阻）

新增Session中的当前邮件/附件预览与失败标记。1320成功后记住服务端返回的邮件键和附件键；2171必须对应已查看附件，验证账号后调用领取事务，读取权威库存并以2160/2161增量同步。重复领取不重复添加；重新认证清除预览状态。

领取失败时，随后针对同一邮件的第一次1340返回1350失败，保留邮件；后续用户显式删除仍可进行。数据库或库存同步错误沿原错误通道结束处理，不继续自动删除。未伪造未知的2171专属成功回包。测试器新增独立领取操作，不自动删除邮件。

新增TestMailClaimProtocolLocalDatabase覆盖详情→成功领取→重复领取→删除，以及坏附件领取失败→自动删除被拦→后续显式删除。**此测试本批未实际运行**：两次go test均在启动临时game.test.exe时报Windows Access is denied，尚未执行到测试主体。之前17.287s的存储事务验证不能代替这批新入口验证。候选服务器编译通过，未替换运行中的服务器；临时数据库隧道已关闭。下一步先解决测试执行问题并完成该协议链验证，再开放赠送投递。

## 赠送原子投递事务（未接收费入口）

商品键映射由现有admin.go构造/校验证实：商品记录+9等于offers.catalog_key，对应9090请求+145。Store.Gift按精确昵称查询唯一收件人，拒绝重名、自己及非零UID提示不匹配；按UID顺序锁定双方账号，避免相互赠送锁顺序相反。

服务端重新取已上架商品和实际点券价格，拒绝金币商品、报价不符、余额不足及当前未支持的折扣/特殊字段。邮件、附件、扣点券、gift_receipts操作回执同事务提交。重复同操作同请求返回原邮件与当前余额，不重新投递；同操作换请求拒绝。新消息/新点击仍是新业务操作，不承诺跨登录自动识别用户重复点击。

投递前检查收件箱2048条上限与GBK文本限制，不能先收费再发现邮件无法显示。未解字段165/169非零暂拒绝；当前单件商品附件，邮件内不另外赠金币/经验。gift_receipts保留收件人与原邮件编号；删除邮件后重试原操作仍不补发。

新增TestGiftLocalDatabase只用新临时账号与新临时商品验证并发重复扣款、伪造价格/目标、自赠、未支持参数及超长正文拒绝、领取后的重试。该存储测试与受Windows执行限制的game协议测试分开记录；网络9090仍提示未开放，待领取/删除保护验证通过后再接通。

本次验证结果：普通persistence包测试曾通过（新独立库用例未设置DSN时跳过）；设置独立库后，Windows在启动persistence.test.exe时报Access is denied，测试主体未执行，因此不能报告赠送事务实测通过。数据库连接隧道已关闭，未创建本次临时数据；未绕过或修改系统安全设置。已询问用户是否看到安全软件拦截原因，仍可继续静态检查与其他协议恢复。
