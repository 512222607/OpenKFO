# 2130 丢弃（2026-09-21）

当前 gfld.dat 的 8A3A60 从仓库对象+5B4取实例，8A3A75传长度4，8A3A7D发送2130。线上12:12:13也收到2130/4B，之前未实现。载荷只有 uint32 实例，无UID、商品类型、数量；按整条库存实例丢弃，不能自行解释成减一个堆叠数量。

下行复用已确认的2162：A2C2C0要求4B，调用9CE160删除客户端库存实例并刷新仓库。只在数据库事务提交后发送。失败只提示，不发送删除包，也不相信会话缓存中的归属。

服务端：只接受大厅/未准备房间；账号行锁与现有装备、发货事务共用。按认证UID+实例读取，校验68B记录、记录自身实例、非零物品号、未装备、状态0/1/2和支持的实体类型。会员卡及未确认特殊类型拒绝。基础服饰12–17和武器25保留一件可用同类；这是明确的服务器保护策略，不声称还原所有原版限制。锁定/绑定字段尚未确认，本次没有新增锁定协议或宣称实现原版所有锁定规则。

丢弃写state=0xffffffff，清除该实例到期记录；Snapshot不下发废弃记录。保留行是为了MAX(instance)+1不复用编号，从而避免延迟重复丢弃误删新道具。原来的装备、使用、续费已拒绝该状态。重复请求拒绝，不会重复变更其他道具。

验证：persistence/game/protocol测试；独立MySQL库openkfo_debug_discard_20260921实际验证不存在、跨账号、已装备、异常状态、会员卡、最后一件武器拒绝；并发两次仅一次成功；到期元数据清理、禁止重新装备、重登快照隐藏和最高编号不复用通过。未用自动化点击丢弃正式玩家道具；原生UI由用户复测。
