# 4160 → 4180 加载放行证据

4180的消息表项为0x12ed998，raw handler0x829770，减0x10000为0x819770；这条表项属于联网战斗消息处理器。

0x819770不读取正文：先重置计时器UI（sub_93cc70→mode vtable+20取得时长并除1000），再由sub_443db0获取game+172模式对象并调用其vtable+36，最后sub_4a0fe0(1)将帧更新器+52置1。sub_7d8360将+52作为游戏update分支开关：为1时执行sub_976fe0/sub_979110等游戏更新。

加载函数sub_97c9c0在0x97d6f0压入4160，0x97d6f5调用sub_A2CA00(4160,0)。随后检查离线模式或本地房间player是否存在：离线/无本地player分支0x97d786–0x97d809直接设置更新器+52=1并调用同一game+172模式对象vtable+36；联网分支跳过这些动作。因此4180执行的是客户端离线分支自主执行的开场动作，4160→4180是有静态支持的加载完成放行链。

父agent本轮实机证据：4080/53进入加载UI并自动发4160/0；发送一次4180/0后，客户端继续发8040/14。实机日志/截图由父agent归档，本文件只保存独立静态分析并明确动态结果来自父agent报告。

建议最小实现：对活动会话4160/0回复4180/0，并对重复/乱序请求按会话状态处理，避免提前发4180。后续8040→8070的字段由另一agent继续确认。
