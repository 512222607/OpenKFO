# 自由训练开始恢复证据

范围沿用 ../scope.md；输入/导入证据沿用 ../evidence/E-imports.md。只读当前 IDA 数据库，未切换数据库、未改客户端或服务源码。使用 ida-reverse 工作流；tool-index.md 在技能目录缺失，仅存在 template，本轮直接使用已验证可用的 IDA MCP 工具。

## 结论

客户端房间“开始”发送 4030/0；当前客户端消息表没有 4030 或 4040 的接收处理入口。已确定 4080/53 是进入战斗加载的成功通知，不能用 4030 空回显替代。

4030 发送证据：IDB 0x7f02c0/0x7f2b30 → sub_A2CA00(4030,0)，另有0x815640在创建特殊房间成功时发4030。当前用户点击成功发出4030，故请求前的UI检查已通过。

## 4080 成功体（固定 53 字节，little-endian）

| Offset | 类型 | 证据和含义 | 本地最小候选 |
|---|---|---|---|
| 0 | u32 | 必须等于当前room u16 ID，0x80e1cd–0x80e1d5 | room_id |
| 4 | u8 | 0x80e1a0未读取 | 0 |
| 5 | u32 | 写dword_17c86a0，战斗统一计时基准 | 0（待实机验证） |
| 9 | u16 | 0x80e1a0未读取 | 0 |
| 11 | u16 | sub_808910循环8槽，仅指定槽player+7028置1，其余0；选中/主持槽 | 当前本地slot，通常0 |
| 13 | u32[8] | 每槽延迟，写player+7056；getter sub_5464a0被0x7ebea0按50/100/150/250阈值显示 | 全0 |
| 45 | u64 | 本局校验标识；sub_807b10存room+785/+789；sub_807ac0校验后续战斗消息尾部u64 | 非零match_id，后续复用 |

0x12eeb98表项为4080/kind1/raw handler0x81e1a0；减dump偏移0x10000后为0x80e1a0。长53且房间ID匹配后一定调用this虚表+36。

派生虚表0xbbb664的+36为raw0x820a10→IDB0x810a10；进入sub_7f4400→sub_94a810。sub_94a810调用自身虚表+264；构造函数0x94a310写入raw vtable0xbaae2c，其IDB0xb9ae2c+264为raw0x95b2b0→0x94b2b0。0x94b2b0调用sub_97fde0(9)，打开战斗加载UI并设置游戏加载状态。

4080处理体没有读取地图字段，地图须先经房间信息/地图变更路径设定。请求中的随机map0是否可直接加载需实机判定，推荐使用已有静态记录中有效的训练地图。

## 相关消息

- 4050/8：0x80e100，体为玩家UID；找到玩家后置ready=1、调用虚表+24。可能为4030前置ready通知，可先发送本地UID并再发4080，顺序需实机验证。
- 4070/8：0x80e090，同UID清ready。
- 4081/2：0x80e6d0，u16错误号；清ready并展示错误信息，恢复房间状态7。不可当成功体。
- 4032：0x810d00，UID驱动移出战斗/本地离开处理；不是4030成功回复。
- 4041：0x8109d0，显示确认对话框；不是正常开始成功。
- 4082/0：0x9f6520的角色状态机可发出，涉及角色/技能状态；尚未证实其为加载完成，不建议盲目当加载完成握手。
- 4100：0x81a9b0→0x977e40，属于结束/结算链，不是加载完成。

## 可验证实现边界

先增加4030 handler构造53字节4080，并保留frame序列和wire记录。待实机观察发包/画面确认后，再实现具体的加载完成请求。不能仅凭静态进入状态9声称自由训练已可玩。本报告的偏移、字段宽度、长度校验及虚表链为静态确认；计时基准默认0、可选先发4050、具体地图选择仍需动态验证。

原始反编译见decomp-complete.json；初始和中间快照为decomp-initial.json/decomp-followup.json。
