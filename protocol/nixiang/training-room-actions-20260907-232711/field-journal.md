# 本轮可复用结论

- 表内绝对函数指针与当前IDA镜像地址差0x10000；CALL目的地址已正确。先区分再追，不沿用前轮误映射。
- 4030的成功链为4080→4160→4180→8040→8070。能看到场景不等于角色已经可操作；用户在4180后明确不能动，8070后确认正常。
- 4080+11是主持槽，+13八个DWORD是延迟，+45是本局标识；8070后8字节复用该标识，不能回显UID。
- 4100会触发结算统计4110/696，不可当开始回复。
- 退出既要给客户端3115，也要清服务端房间状态，才不会继续抑制大厅轮询。
- 临时heartbeat推送只证明客户端处理路径；正式自动分派需独立socket完整链和真实重登录验证。
- 本机tool-index.md缺失时可使用refresh-tool-index.ps1显式输出到case目录；无需写入技能包或改全局工具。
