# 新账号引导：模式4，地图1201

当前PE证据见native-entry-exit.asm，文件头绑定gfld.dat SHA256。与titlemission.xml的技能练习、每日/新手任务面板及名侠训练分别处理。

## 已核实入口

8A0F00与921430构造81B的3010：容量+37=1、模式+46=4，两个地图字段从A55170返回对象读取，时间为0。2026-09-19登录断线日志中实际两次请求地图均为1201，房名为新手训练；请求发生在2010/2030后、1156绑定之前。

8253A0要求3020恰83B，复制+2处81B配置，初始化房间号、模式及地图。模式4另调用4F35A0(0)；随后发送3550/12，在普通分支发送3070/14。不能只返回3100而省略此处的模式初始化。

代码当前加入受限入口：仅模式4、容量1、两个地图字段均1201的房间可免P2P前置，仍受地图准入策略控制；房间不出现在公开列表。先3020，收到本人3070后3100/3160，之后才允许开始。未授予P2P身份或放宽普通房间权限。退出仍走3110/3115。

TestNativeTutorialEntryWithoutPeerRegistration使用断线日志原始HEX，覆盖创建、未入房不能提前开始、进入应答、重复进入及退出；TestTutorialAdmissionIsNotMultiplayerBypass覆盖变更人数/模式/地图后不能绕过P2P与地图关闭规则。

## 尚未闭合：开场、完成与持久化

RTTI定位CUserGuideMode虚表BA82AC。96AB90初始化引导状态10。96A2B0根据本玩家对象4536E0返回值是否2，发送4124/0或3110/0；目前没有找到足够的调用者证据，不能将任意4124直接当成合法完成或发奖授权，也未确认持久化字段与后续登录如何不重复引导。

6220发送器953A30与6221消费953910属于skillExercise相关流程；17B结果面板包含经验/战斗值/金币/评价/成功图。它们不能未经证明移植为地图1201的新手完成协议。先前分析曾将两者混淆，此处明确纠正。

本次没有实现最终完成通知、完成标记落库及自动回大厅的完整闭环；入口单元测试不等于完整原生训练验收。候选代码未部署运行服。

## 2026-09-19 实机入口与 NPC 断线修复

localtest1 创建角色后已实际进入引导战斗。20:59:24 收到 8071/8150（87B），目标 +39 为 101、来源 +47 为 0、效果 +55 为 12、房间 1、战斗序号 81。此前仅模式5允许本地 NPC 事件，模式4因此被报 battle unknown actor 并断开。

battleMessage 现将严格匹配 tutorialRoom 的新手房纳入现有本地 NPC 隔离分支：校验长度、认证发送者、房间序号和数值后，本地非玩家目标/来源不进入玩家广播与状态同步。没有将 101 硬编码成玩家，也没有放宽普通房间。TestTutorialNPCEffectCapture 使用本次原始载荷，覆盖新手目标/来源、普通房间、错误地图、伪造发送者及错误战斗上下文。

修复已构建并替换本地运行服；单元回归通过，健康检查通过。完整引导完成、持久化及退出仍待闭合，不能将本次 NPC 兼容修复当作新手引导全部完成。

## 2026-09-19 21:06 终点上行确认

本地 protocol-20260919-210415.log：21:05:53 下发8070/12及8090/4=1；21:06:42.4718361 客户端上行4124/0，phase=battle。当前服务端没有4124处理，故完成后的退出/保存未推进。该实包补足了此前只有静态发送器的证据。

从当前客户端config.spf2只读提取UserGuide.xml（研究副本runtime-backups/UserGuide.xml）：课程依次为介绍、界面介绍、山顶寻师、最后考验（破坏四个箱子）、结束语；该XML没有倒计时配置。原生3010构造器把+47时间字段写为0，不能据此猜一个任意训练限时。

当前PE调用点8A10B0、924010均读取9CD390返回角色记录+0x7B（称号等级），<=1时进入自动新手引导；后者另检查9CCF80()==1。这是完成后不重复进入的关键客户端条件。尚未确认原版4124对应的下行组合与称号推进策略，不能仅存一个客户端不读取的完成布尔值，也不能擅自提高称号冒充闭环已完成。

## 完成处理实现（2026-09-19）

根据实测4124与当前PE/配置恢复模拟器完成策略：只接收已进入battle、本人为唯一成员/房主、输入就绪且严格匹配模式4/地图1201的4124/0。事务锁定本人档案，将称号字节123从0/1推进为2（roletitle.xml“天赋少年Ⅰ”，对应UserGuide结束语），保留更高称号与其他档案内容。以现有称号字段作为持久完成门，不添加客户端无法读取的独立布尔字段。

称号2下发4125/64（无物品选项）后3115/0退出；更高称号只退出，避免覆盖已有领奖UI。957670按七个候选查目录，未找到候选时957E02分支隐藏对应控件；该包只同步称号，不产生领奖资格或物品。不是原版服务端完整奖励策略的恢复。重复4124在lobby无副作用。

独立debug数据库连接级临时表测试通过：0/1升级、2重复、8不降级、其他档案字节不变、非battle/错误地图/多人房/未就绪/非法长度不完成、返回大厅、重复请求无重复应答。普通game/protocol/tester回归通过。已经构建替换本地服务器；实际客户端通知窗口、返回大厅及重登不重复引导仍需实机确认。

计时仍保留原生3010的时间0，8070/8090正常发送。当前只证实课程事件触发完成，未证实需下发另一倒计时；不把任意填写秒数当修复。


## 2026-09-19 23:13 地图不存在误报

实包 `localtest1` 在23:13:32上行3010/81：模式4、容量1、地图与建议地图均1201；服务端下行3030/2，载荷`2c00`（44）。当时GM关卡条件启用，训练山最低称号1，新创建角色称号0；版本指纹一致、地图未关闭。因此实际是普通称号准入拒绝，不是缺少地图或3010字段错误。

严格新手房在创建和开战时豁免称号/个人解锁门槛；保留明确关闭地图和启用条件时的客户端版本校验。普通请求、其他模式/地图/人数不能使用此豁免。TestStageGateLocalDatabase加入称号0的复现及入口、开战、普通请求、关闭和版本不匹配回归。


## 23:20 重复进入的实包闭环

23:20:13.856收到4124，23:20:14.596下行3115，23:20:14.696又收到同一3010。前版自动奖励实现删除4125后，只有数据库称号推进，客户端924010仍读取旧的+123，重新触发新手入口。恢复4125/64在3115之前同步称号；奖励依然自动入库，4125不授予额外选择资格。原生称号/空候选界面的副作用尚未消除，不把缺少独立刷新协议当作省略状态同步的理由。

已完成角色重发3010：查询持久称号>=2后，补发4125、3115和完成说明，不分配房间。数据库测试覆盖首轮完成顺序、不同已有称号、重复4124、连续两个延迟3010与不重复发奖。

## 2026-09-20：接通新手武器选择奖励

此前零候选4125只同步称号，会出现空的原生武器选择界面。现在GM新手奖励中的武器最多7件，作为任选一件候选；非武器、金币、点券仍是完成时必得奖励。完成事务同时记录tutorial_rewards和title_rewards的待领取候选，确认前不发候选武器。

网络顺序：补发1550完整商品目录（保留普通商品，添加奖励专用显示条目）→4125/64（+0称号2，+8起7组候选目录键）→必得奖励余额/库存→3115退房。独立物品定义只有经对应108B显示记录下发后，才可作为原生目录键使用；不创建可购买的免费商品。未解析的候选第二DWORD仍为0，不冒称原版含义。

客户端4126/149从+145传回选择；服务端验证本会话已公告的称号及持久候选，复用TitleManager/RewardManager原子领取，成功2160，重试2161同实例，改选拒绝。重登后的6000刷新会优先恢复新手待领取选择，不依赖常规称号升级开关。之前版本已自动发放的完成记录不再补发候选，避免重复获奖；测试新流程需未完成引导的角色。

独立数据库临时表测试覆盖确认前零武器、两件候选、奖励专用目录、重登恢复、重复/改选请求及必得奖励只发一次。真实客户端图标与选择交互仍需实测。

## 2026-09-20 分步回调不是已确认的网络请求

当前客户端只读提取见 [step-event-queue.asm](step-event-queue.asm)，文件头绑定原始PE的SHA256，提取前后源文件哈希不变。

- 96A1A0 将参数交给自身+14对象的4C52D0；4C52D0取该对象+1C容器调用45C6B0，后者为分段容器分配/定位槽、复制一个DWORD并增加计数。这个具体调用链是本地事件入队，不能将参数当作网络opcode。
- 96AB90在初始化后通过96A1A0入队10；96A1C0入队27；96A1E0/96A230把462480结果加20，限制在21–24后入队；96A280限制来源对象+58在0–6后入队。这里仅保留范围，不把0–6或21–24擅自命名为走、跳、攻击。
- 96A2B0仍是已确认网络出口：玩家状态4536E0()==2时，经A3CA00发送4124/0，否则发送3110/0。

本次仅跟进上述回调链及有界代码范围，没有覆盖全部虚函数、事件消费和其他发送封装，因此不作“所有教学步骤绝不上行”的全程序结论。UserGuide.xml的OperationTip操作提示也不能直接作为服务端步骤完成凭证。当前服务端的tutorial_complete只绑定整体完成，不替代各分步条件；不增加猜测的协议处理器。
