# 任务列表恢复


## 后续任务6040插入行为与当前实现

successor-notification.asm绑定当前PE，A48EE0先查本地模板并判空，随后分配123B、写state1/key并插入+78容器，没有现有任务查重。因而服务器不能在包含新任务的6020完整列表后再重复6040。当前实现只在新完成任务、匹配客户端目录及服务端后续键、目标状态1时使用6040，并从同次6020排除该目标；其他任务正常列出。已接状态2不重置，重复查询直接返回完整6020，不再通知。

6040按当前消费下界编码6B（+4 WORD任务键，前DWORD沿用服务器角色上下文）；6030编码14B，未知前缀采用现有应答策略，不声称原版结构全部恢复。TestTasksTLS实际通过（43.660s），真实双账号两局结算后收到奖励、6030/6040且无重复任务；再次查询及重登不重复奖励，任务状态完整。原生界面展示仍未执行。

## 称号奖励候选与领取4126

title-reward-selection.asm补齐956F10/957280/957670/85BA40。4125的64B块布局：+0称号BYTE；+1..7未命名；+8起7组，每组两个DWORD。956F10依次从面板+580/+584至+5B0/+5B4返回两值；957670循环7项，以第一值调用85BA40查询108B商品记录，再以该记录+5的物品ID显示图标。因此第一值为商品目录键，第二值仍保留未知，不猜作数量或有效期。

957280选择按钮后先清零149B，再将所选候选首DWORD写入+145，发送0x101E（4126）。其后只有房间状态4且本玩家对象有效时才额外发送6220/13全零包；这不是称号奖励主请求。4126不能直接复用普通169B购买或把客户端商品键当发奖授权，后续必须校验服务端未领取资格、候选集合并原子标记领取。

共享解析加入7组候选及4126精确149B形状，测试器显示选中商品键、未知值和资格约束。固定字节测试覆盖首尾条目、+145 DWORD及148/150/169等错误长度。仅完成结构与消费恢复，资格持久化、成功应答、GM称号奖励配置还未接通。

## 角色+123称号字段与4125长度勘误

title-consumers.asm记录当前PE证据。注册段B21F1A设置opcode=0x101D（4125），对应829B50；该处理器读取载荷首BYTE写profile+123，再以该值查询A73BC0称号目录，并使用“恭喜获得称号”文案。9CD960同样读取profile+123。由此确认该字段为称号等级，不能混为经验等级；6030将基础任务配置+0D提升至此字段的行为也与称号有关。

**旧战斗准备.js的“无载荷”描述错误。** 829B50虽只检查len非零，下游957670会将完整64B（16个DWORD）从载荷复制到面板+578。至少需要64B才满足已确认读取；这尚不证明原版确切包长，也不证明其余63字节可全部填零。服务器暂不构造称号成功包，测试器新增4125长度下界检查和称号字节诊断，保留其余原始数据。

真实config.spf2的roletitle.xml为TitleSetting根、17条TitleLevel项，属性Level/Title/Icon；与titlemission.xml训练关卡不同。新增ReadRoleTitles及桌面只读title_catalog接口，验证BYTE范围、重复等级、空名称/图标及错误根；图标路径仅为目录信息，不复制素材。真实资源读取测试核对首条等级0且读取前后SHA256一致。此目录不是获得条件，尚未接GM称号发放或称号奖励规则。

## 6030/6040当前处理器与发送安全边界

新增completion-notifications.asm，对当前gfld.dat（哈希见文件头）只读提取完整处理器及下游。6030/829300读取+12 WORD，6040/8293F0读取+4 WORD；均无入口长度检查。因此目前证明的是至少14B/6B可读取，不是原服务器完整包长。测试器保留所有未知字节，不能将最短结构称为完整响应。

6030调用A48D90：按任务键查已接列表设state3，再由A47D00查本地任务配置，直接读取返回对象+0D WORD，并按需写profile+123 BYTE，随后重建未完成列表。配置对象未做空值保护，服务器任意自定义任务ID不能直接下发此通知；profile+123也不是已经证明的经验等级。后续830A80调用提示控件虚函数刷新。

6030/6040的附加界面分支不是无条件切换房间：先要求17C8710非空、其+78 DWORD等于5、811460面板可见且+614非零，才按房间状态15/21/其他处理提示。80D780/80FD80主要隐藏提示控件、释放已存句柄并置-1，80F4E0写面板+614；不能仅凭调用存在就称为游戏状态跳转。仍需恢复完整UI上下文与profile+123语义。

共享ParseTaskNotification与测试器接入6030/6040/6060/6090，显示任务键、最小可读长度及状态副作用。短包全部拒绝，额外字节保留，零/未知任务键保留用于诊断而非授权发送；测试验证固定字节、输入隔离和实际诊断入口。未向游戏客户端盲发6030/6040，服务器现有6020完成列表和奖励链保持不变。

## 基础任务模板已定位（与训练关卡不同）

当前PE的A4B220读取BaseQuest.txt，A48AA0读取条件并生成进度显示。完整反汇编见basequest-config.asm。真实资源有50条基础任务；另有BaseDailyQuest.txt、BaseNewBieQuest.txt、BaseRoutineQuest.txt，它们结构不同，不能混用基础任务解析器。

BaseQuest每行41个TAB字段：0为WORD配置键；1..4名称、图标、说明和完成文案；5为总参赛次数要求（内存+17）；6最大连击数要求（+21）；7..35为29项统计要求（+25起DWORD数组）；36为WORD奖励原值（+3，尚未确认币种）；37为DWORD物品字段（+5，尚未接发奖）；38为WORD等级原值（+13）；39为WORD后续键（+15）；40为启用BYTE（+2）。不得仅凭奖励数值将36列命名为金币或经验。

A48AA0的总参赛进度在状态2时取profile+133/+141/+149/+157之和，减去任务快照对应四项；其余29项按profile+129起数组减快照。状态1显示0，状态3显示目标值；最大连击分支只在状态3显示目标，否则为0，不能凭此推导真实连击完成算法。字符串表确认前9项依次为总歼敌、生存大乱斗参与/胜利、生存团队参与/胜利、歼敌大乱斗参与/胜利、歼敌团队参与/胜利。

ReadBaseQuests严格验证41列、整数宽度、重复键和后续键引用；GM后端task_templates为只读目录接口。真实资源测试验证50条及首项1001要求10场、1006要求16次歼敌，读取前后SHA256一致；坏列、溢出、重复及错误引用测试通过。模板读取不代表任务进度、接取/领奖、GM编辑已经接通。

## 请求与状态变化（当前PE再次核对）

证据state-actions.asm绑定gfld.dat SHA256。6010处理器821C40先逐条清零123字节局部记录，再拷贝7字节的Unknown0、Key(+4 WORD)、State(+6 BYTE)，交A48FC0。因此它是简表，不是直接将7字节当123字节读取；服务器/测试器现有列表解析已覆盖6010。

6050有两个发送入口：542252之前明确写入玩家UID、4651D0返回的对象+34上下文、任务键；831400只写后6字节，前8字节未初始化。两者均发送14字节。6080在542442同样发送14字节，按钮分支要求状态2；6050按钮要求状态1。共享ParseTaskAction保留前8字节但明确禁止用它代替认证会话，+8上下文仍不猜测业务名称。

6060/821B90读取+12 WORD交A47690，设状态2并复制角色数据快照；6090/821B50交A47660恢复状态1。6030对应A48D90设状态3并可能提高profile+123字节到本地配置+13 WORD的值；不能据此直接把该字节当角色经验等级或金币。6040的当前处理器为8293F0，读取+4 WORD调用A48EE0创建状态1记录。以上是消费和状态事实，尚不等于任务领奖及奖励规则已恢复。

解析与测试器测试覆盖简表、多条记录、截断、14字节请求偏移和未知身份前缀。请求并未开放到游戏业务，接取/完成条件及数据库事务仍待实现。

## 当前资源中的不同任务目录

通过现有Go SPF2解析器只读核对config.spf2：quest.xml为10条问答任务，questquestion.xml有30条问题；titlemission.xml有3组、28条基础训练关卡，带ID、Name、MapID、Time及DependentMission。不能据文件名把它当成可佩戴称号表，也不能自动认定其ID等于60xx任务列表配置键。

新增ReadTrainingMissions，保留分组、任务ID（0合法）、地图、秒数和前置任务；拒绝缺字段、重复ID、非法时限、自己依赖自己或引用不存在任务。真实资源读取TestTaskCatalogueClientArchive通过（0.190s），核对首条0/10001/45及读取前后SHA256不变。未复制或修改原资源；目前为GM/测试器后续接入的目录读取基础，不是任务服务器业务已完成。

旧协议索引见同级外的战斗内.js；本次只读核对当前PE处理函数，SHA256及完整消费汇编见list-consumers.asm。

6020对应821BD0：非空且长度至少7后，以len/123遍历，从payload起每条123字节，无7字节头。7字节空响应只导致零次遍历，不是完整任务记录。旧报告的首DWORD为key说法不准确：A48FC0在+4读取uint16查A47D00，并检查本地配置启用字节才复制整条记录。该键尚不解释为房间号。

6041对应822290，先清容器，再以len/141遍历并交A49080；其中读取记录+12 uint16查已接收记录容器（不是已证明的配置查询）。6042对应822310，亦按141B复制，A48920使用+12 uint16索引。其余字段尤其状态、进度和奖励尚待追消费者；不能凭完整长度构造全零成功任务。

共享ParseTaskRecords严格拒绝残缺尾记录，保留原始字节和配置键；对6020单独识别7字节历史空响应，6041/6042允许零条。解析器比原生整数除法更严格，不把截断尾部当合法。测试器诊断最多显示8个键，完整HEX仍保留。

TestTaskRecordNativeLayout通过（0.130s），覆盖两条记录、键偏移、输入拷贝隔离、三种消息诊断、空列表及短尾边界。本轮未接6000族服务器业务、任务持久化或GM任务配置，不表示任务系统已完成。


## 每日与新手目录区分及只读管理接口

当前PE的6041→A4B950→A4B8C0→A4AAE0读取BC9654字符串 `\data\config\BaseDailyQuest.txt`；6042→4045E0→A4B830→A4A300读取BC9674字符串 `\data\config\BaseNewBieQuest.txt`。完整函数证据见extended-task-consumers.asm。两者均为141B网络记录，不能复用6020的123B布局。A49080的+20查询针对收到的记录容器，不足以证明任意任务键都有客户端模板。

实际只读解析当前config.spf2：每日42条、新手15条，每行27个TAB字段。加载器先顺序读取22列，再读取5列；0/1/4/7/10写入WORD；2/5/8及22–25为字符串，其余数值保持原始列含义。目录显示名取22列，全部27列原样保留，不根据数值猜测经验、金币、物品数量或重置规则。

桌面管理新增task_extended_templates，返回daily/newbie目录和同次读取的SHA256；即使管理环境选online，也只读本机客户端资源，不访问线上账号数据库。此接口用于后续GM任务配置，尚未接入每日/新手编辑页面或服务器发奖。已有BaseQuest导入不混入这些不兼容的记录。协议测试器将6041标为每日任务、6042标为新手任务，其余未知字段继续保留。

TestExtendedTaskTemplatesValidation及TestExtendedTaskNativeArchive实际通过；验证字段宽度、重复键、空表、列数错误、管理入口、42/15条目录和读取前后资源SHA256不变。TestTaskRecordNativeLayout通过，含分类、截断及输入拷贝隔离。


## 每日／新手操作及状态消费者

证据extended-task-actions.asm含完整原生发送器、状态消费者、记录getter及注册表赋值，当前PE哈希同文件头。每日按钮5425B0/5427C0/542750分别发送6051/6081/6311；新手545440/5454B0/5453D0分别发送6052/6082/6312。均发送19B：+0 WORD任务键，+2 BYTE目标状态（接取2、放弃1、领奖3），+3 DWORD来自4651D0的上下文原值。+7..18未见初始化，不是可靠操作ID、认证身份、价格或奖励。按钮分别要求当前记录状态1/2/4；状态4的完整达成算法仍需追踪。

A47AB0/A47920返回map value+4，按钮读取返回对象+0C任务键、+10状态，对应141B网络记录+12/+16。新增ParseExtendedTaskProgress仅命名这两个字段，保留其余原始字节。

注册表确认每日6031/6091/6061/6301分别对应8221A0/822200/822230/822260；新手6032/6092/6062/6302对应8220B0/822110/822140/822170。消费者读取通知+0 WORD及+2 BYTE，入口不检查长度，只能证明至少3B，不宣称完整原始包长。每日A47A00更新收到容器的状态；新手A480E0在状态3时删除已接收记录容器中的任务；状态1都会清七个间隔12字节的进度字段。这不是统一的“成功回包+10”规律，6311/6312与6301/6302的具体成功配对仍需完整业务链验证。

共享协议解析及测试器新增上述六请求、八通知的诊断；请求严格19B并校验已观察的状态组合，拒绝把客户端state4直接作为完成授权。测试覆盖全部请求/通知、错长、错状态、零键、未知ID、记录字段偏移、未初始化尾部保留和输入拷贝隔离。protocol/protocol-tester测试通过。尚未接收发奖业务，不向原客户端发送猜测的成功通知。


## 七组条件进度与模板匹配

extended-task-progress.asm补入当前PE完整函数542830、543110、545650、545F40及A47E70，均提取至ret。543110读取141B记录+53起七组12B槽；每槽首DWORD与模板+C8/CC/D0比较，第二DWORD（记录+57+12*i）为当前计数，第三DWORD仍未知。模板+D4/D6/D8为WORD要求值，对应文本配置4/7/10列；条件编号对应3/6/9列，描述对应2/5/8列。新手面板有同样匹配和显示分支。

记录+14 WORD等于2时，显示分支将当前计数>=要求值转为1，否则0；其他情况显示当前计数与要求值。命名DisplayMode只说明已确认的显示用途，不推断完整任务类型。状态4控制领奖按钮，但本次路径没有证明客户端负责状态4判定，因此不能把此显示比较当成服务器完整完成授权。

共享ExtendedTaskProgress新增DisplayMode和七条件槽，第三DWORD及剩余原字节保留；目录接口新增三组条件描述/编号/要求。条件编号0确实出现在新手“走”训练、要求1，不能直接将编号0统一丢弃为空条件。测试验证首末槽与边界哨兵、零编号有效条件及真实42/15目录，protocol/desktop/protocol-tester均通过，真实资源测试使用OPENKFO_CLIENT_ARCHIVE执行。尚未实现各条件事件来源、周期和发奖业务。


## 20530附加记录与当前客户端循环缺陷

extended-task-update.asm记录当前PE的A2BEE0、A483B0、A481B0、A47530及注册表。AF31FA明确注册opcode0x5032（20530），每记录22B、+12 WORD任务键；2000..3000交每日容器，>3000交新手容器；记录保存到各管理器+58的附加列表，供A47E70读取。这不足以将其命名为条件计数或奖励表。

A2BEF7将局部循环索引[ebp-8]置0；A2BF15用该索引比较len/22；但A2BF00..06增加的是[ebp+0C]载荷长度，没有增加索引，而A2BF66..6C持续推进记录指针22B。至少一条记录时，静态控制流表明会继续越过载荷读取；未向真实游戏发送验证，不宣称已捕获运行崩溃。

服务器当前没有20530发送点。本轮仅增加ParseExtendedTaskAuxRecords及测试器离线诊断，严格检查22B边界、保留未知字段、列出任务键及原生越界风险；没有编码器或新增发送场景。测试验证双记录、尾部边界和输入拷贝隔离，通过protocol/protocol-tester。任务业务后续须采用另有证据的列表/状态路径；不得因新增诊断就向当前原生客户端重放该消息，也不修改原客户端文件。


## 新手接收容器与本地模板容器纠正

继续核对extended-task-consumers.asm：A4A300入口A4A331..3A清manager+20，A4A998..9A1将完整0x150模板按值插入manager+20；A48920末尾使用manager本体（没有+20）调用532840，复制141B。因此6042覆盖的是manager+0的接收记录，不是本地模板，不能将其描述为“覆盖配置对象”。A47920读取manager+0得到进度；UI545F40经A474D0取另一模板（变量[ebp-148]），奖励显示读取模板+11/+15等字段。之前对容器的混淆已修正，不能据此声称服务器修改奖励字段会改变本地模板显示。
