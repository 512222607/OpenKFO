# 房间管理未接入口核对

## 21370查询与21373策略名单接入

control-input.asm新增当前PE 751310、75BB00、762C10，均检查控件+56后才继续事件/交互路径，与7603D0→75A960的写入链对应。依据9290F3逐项比较正数MapID，服务器为已配置空名单发送文本`0,`：0不在已核对PVE目录中，强制遍历不匹配分支写0；不将0保存为实际地图或授权。单独Encode/ParseStageSelection处理该标记，通用StageProgress仍禁止地图0及混合标记。此为本服明确策略，非声称原服务器使用相同标记。

21370空载荷在大厅/房间安全阶段读取一致性玩家名单，与resolveWithAllowed已支持的模式及容量地图池相交，下发21373/404B。首DWORD沿本服保留0，不赋予未知业务含义；已核对消费者仅复制后从+4解析名单。未配置、版本/查询错误与请求错长发普通提示，不构造其他未经确认的相邻包；不发送会重复开启提示的21374。

TestStageGateTLS实际通过32.612s：两个临时认证账号查询分别得到真实授权名单/0标记，错长只提示，GM授权后重新查询得到名单，再开战成功。使用测试地图验证报文及隔离，不证明原生PVE画面已经可玩；原生名单清除的视觉及其他间接消费者仍待验收，自动通关/完整PVE业务未完成。服务器准入独立校验，UI状态不作为授权。

## RoomSet缺目录记录会提前返回

stage-records.asm中7F876D找不到21372目标地图记录时跳到7F897D，未经过7F8903的禁用路径。因此仅发送21373无可选标记不能覆盖此分支。服务器先为整个已配置PVE目录发送21372，再发21373筛选名单；未知四字段为零，条件地图使用本服无匹配标记0xffffffff。不能使用0代替：MapInfo有合法随机地图0，可能错误显示为前置地图。该选择不是原版协议规则恢复，条件标签的旧文本和原生画面尚未验证。

真实TLS回归TestStageGateTLS通过35.015s，覆盖先目录后名单及GM授权撤销后列表清除、开战拒绝、恢复授权再开战；未运行原生PVE客户端，不据此声明通关或怪物逻辑完成。

## 当前客户端两个MapType不是同一枚举

只读核对config.spf2：PVEEntryUI共50项，其MapID全部存在于96项MapInfo目录；同一地图在前者MapType为10或21，在后者均为110。首轮用两者类型相等的断言失败，随后保留该差异为当前客户端固定资源测试。不能把110统一减100或用两个类型互相替代，MapID才是本轮证据支持的关联键。

GM stage_requirements读取改为单次载入同一归档，返回原maps及附加pve_maps，指纹绑定同一份字节，关联缺地图时报错；原始类型、分组、难度和展示奖励均保留。这提供后续进度同步所需的完整目录，不代表通关业务或奖励已经接入。TestStage系列真实归档及解析测试通过，前后文件哈希不变。

## 21373列表键的PVE配置来源已确认

pve-entry-loader.asm保存当前PE完整A54390加载器及被引用字符串，绑定SHA256。A54413将LogicType写记录+0，A5443F将Group写+4，A5446C将SelectDifficulty写+8，A54499将MapID写+0C，A544C6将MapType写+10；后续+14为DisplayDifficulty，+18/+1C/+20为RewardItem1/2/3。结合settings-consumers.asm的9290F3比较候选记录+0C，21373文本项匹配的是实际MapID，不是Group或SelectDifficulty。默认选择写入的候选+8则是SelectDifficulty。

此证据将此前“配置布局相符”提高为明确的属性到偏移赋值链。它仍不证明哪些地图应初始解锁、如何通关或奖励概率，RewardItem仅为该配置的展示数据。完整难度列表不能从Group值或难度数值生成。

本轮在7F0000..810000与920000..940000范围检查首DWORD对应的三个面板偏移，已确认处理器复制点，未找到足以命名首字段的后续消费；这些范围也存在其他类的同偏移字段，不把搜索命中误作同一对象。不据局部扫描宣称全程序完全不读该字段。

## 21373列表与21374通知不能互换

本轮只读核对当前PE，新增difficulty-controls.asm，哈希与settings-consumers.asm一致。928840在929068将目标难度控件交7611C0(1)，后者经75AA30写控件+54；随后9290B7检查分割列表是否为空。仅有列表项时，9290F3比较候选记录+0C，匹配调用7603D0(1)，不匹配调用7603D0(0)，最终经75A960写控件+56。首次匹配还在929121将候选难度写入面板+584对应项。因此列表参与按钮状态与默认难度选择，不只是文本展示。

空列表直接跳到929151，跳过7603D0，不能推断发送空21373会将旧按钮全部锁定；+54和+56的完整控件语义仍需继续追实际点击/渲染消费者，暂不将这两个字节武断命名为可见/可用。服务器关卡准入必须独立检查，不能把客户端按钮状态当授权。

21374的806D40清理文本列表后，逐个匹配MapInfo并经806EBA..806EF6触发“关卡已开启”文案，没有本轮可见的跨包去重。故不能将它作为每次查询的静默全量同步；后续需区分完整状态与新增解锁通知，避免重复提示。21373/21374首DWORD仍只确认被复制，含义未确认，不填猜测值。测试器已加入这两类副作用诊断，尚未开放个人关卡进度业务。

## 21372首字段与地图选择链已确认

map-selection-key.asm记录A963B0从mapselect.xml的LobbyLevel/BattleMode/Map层读取Id，写44B记录+4；A56560把这条44B记录复制到地图配置扩展块+54。RoomSet选择函数7F89A0经7F8B47调用481580，把84B地图配置和44B扩展一起复制到面板+580，因此面板+5D8=+580+54+4，正是选中地图Id。7F872E用该值查询21372记录容器。

共享结构现为MapID（报文+0，目标地图）、Unknown[4]（+4..+16）、RequiredMapID（+20，条件文案地图）。两个地图ID不能混用。完整条件算法、通关证明和中间字段仍未恢复；不把仅有地图关系当作足够的通关授权。

## 21372末字段已确认是条件地图ID

完整A553D0加载器见map-loader-complete.asm（取代旧map-requirements.asm两段从指令中间起始的无效短反汇编）。A55530保存MapId，A555B4/A555BA将其写入配置结构首DWORD；A5594D从该结构构造存储记录。A56560复制地图配置首DWORD，输出地图列表；7F8854将列表pair+4与21372记录+20比较，匹配后取地图名称构造txtCondition。由此将共享StageRecord.Selection更名MapID，测试器标为“条件地图ID”，保留原字节布局。

该证据不证明记录Key含义，也不证明中间四个DWORD为成绩/次数。地图条件文本不能直接当作服务端已完成通关条件；尚未接个人解锁业务。原“Selection配置匹配值”描述是此次核实前的保守命名。

## 21371归类纠正：兑换面板，不是已证明的21370应答

当前PE进一步追A2BB90→7EB9B0→7ED340→7EBE00，后者访问picRequire/txtRequire、picHave/txtHave、btnRequire/btnHave并按面板+57C查询规则。7ECF30打开plChooseClothes，枚举68B库存，只收未装备且符合规则的物品。证据见exchange-21371.asm。21371的第二DWORD被整数除100交AnimateDestItem动画参数，不能命名为关卡经验或通关进度。

因此下方旧“21370初始化及响应消费”表仅是相邻包号消费者索引，不证明请求/应答配对。测试器取消21370期待21371的成功条件，并将21371诊断标为物品兑换面板。兑换扣费/发奖语义和该包的确切触发请求仍需继续核实；21372/21373/21374仍分别核对，不一并归为兑换协议。

## MapInfo准入目录补充核对

map-requirements.asm保存当前PE的A55FB0加载路径（/MapInfo、mapmgr.xml）、A553D0解析MapId/MapType/NeedTitleLevel及806E9A起21374的匹配与“关卡已开启！”文案。21374列表整数在806EB5与mapmgr容器pair+4比较，不能把PVEEntryUI的难度字段当作此处键。该证据确认配置来源和提示用途，尚不证明21371数值或21372四个中间DWORD的含义。

新增ReadStageRequirements只读解析具体MapConfig节点，返回map_id/map_type/name/title_level及同一次读取的配置包SHA256；RandomMap聚合节点不混入。实际读取96条非注释记录（早先97条统计不能继续作为有效条目数），其中ID0/类型110是“随机地图”目录项，合法保留但不能当作具体通关地图。真实读取/哈希不变及缺字段、重复、范围测试通过。stage_requirements桌面只读接口已接，尚未接游戏准入或个人通关应答。

当前gfld.dat哈希见settings-consumers.asm。本文静态核对，不代表服务器已实现。

## 3140不是已证明的主动移交

8062F0发送3140/9；+0来自面板+5B0的目标UID，+8通过481080读取本玩家对象+107C的byte。此字节并非已证明的“踢人/移交”操作枚举，不能把1擅自改成移交房主。现有服务端只支持已验证的0路径。

## 21370初始化及响应消费

7F8CC0在入口处发送21370空载荷，929320亦发送同号。注册表AF48E5、AF494F、AF49BA、AF4A25分别对应21371、21373、21372、21374，函数指针在各表项之前装载。

| 下行 | 当前处理器 | 消费限制 |
|---|---|---|
|21371|A2BB90 → 7ED340|必须8字节；后续字段待追|
|21372|A2A7D0 → 7F58A0|长度除24，逐条复制6个DWORD；具体业务待追|
|21373|A2A8C0 → 928840 / 7F5770|消费者复制0x65个DWORD，即404字节；还继续解析+4起内容|
|21374|A2BFB0 → 806D40|消费者复制404字节；后续解析未完整恢复|

21373/21374入口没有长度保护，不能回零长度或不足404字节的成功包。满足长度仍不证明字段正确，因此本轮不构造全零404字节假表。

新增测试器请求入口及下行诊断和长度边界测试。主动指定房主、锁位、观战仍需恢复真实请求，不根据连续包号猜3170等用途。

## 继续追踪：实际消费内容

928840访问plDifficulty、radEasy、radNormal、radHard、radVdif、btnSmallMap、szName；806D40使用关卡开启文案。因此21373/21374与关卡/难度选择进度相关，不是已证明的房主移交或锁位配置。还不能把CSV每个数字猜成地图ID或解锁等级。

928840、7F5770、806D40在复制404字节后，使用+4开始的文本调用661A70，分隔符地址B9C808为逗号。661A70只在找到分隔符时追加前面的子串；非空文本末项没有逗号会被漏掉。固定复制区内须有NUL，不能以包尾额外字节的结束符替代。

21371经7ED340将+4 DWORD先整数除100，再交AnimateDestItem参数。尚不能将该字段直接称为金币或经验。

panel-text.asm保存7ED340和661A70。测试器增加终止符、尾逗号及实际分割项数诊断，自动测试覆盖终止符落在404字节之外、完整列表和末项遗漏。

## 地图键比较与安全编码

9290DF→4E6D90→4C1E30将列表项转为整数，9290F3与候选记录+0C比较；候选记录+08选择四个难度控件。当前config.spf2/pveuiconfig.xml的Map节点有LogicType、Group、SelectDifficulty、MapID、MapType，例如组1难度1..4对应8110..8113。这与消费者布局相符；首DWORD仍不命名，记录映射仍需结合A54系列配置读取器进一步核实。

新增protocol.StageProgress编解码供服务端接入及测试器共同使用，保持首DWORD原值、固定404B、CSV尾逗号与NUL，超过399文本字节拒绝。正整数范围、唯一键限制是服务端安全策略，不声称原生atoi会拒绝所有这些输入。只编码调用方提供的数据，不自动全解锁、不据此发奖励。测试器显示安全解析成功的地图键，异常仍保留HEX。

## 21372记录索引及配置匹配值

stage-records.asm保存当前A2A7D0→41CD20写入及7F7EA0界面读取函数。每24字节记录以+0 DWORD为容器键，41CD20返回pair+4的数据区，六个DWORD原样拷入。重复键覆盖旧记录。7F872E按面板+5D8当前选择查记录；7F8854读取pair+18（即报文记录+20），与客户端配置记录+4匹配。不能把容器的map术语误当作已经确认的游戏地图ID。

协议结构暂定Key、Unknown[4]、Selection；+4..+16四个DWORD保留未知，Selection只表示此处配置匹配值，不命名为胜场/关卡等级。解析保留重复记录供诊断；服务端编码拒绝重复键，避免数据被客户端静默覆盖。测试器逐条显示前8条及重复键提示，完整内容保留HEX。独立边界及固定字节样例测试通过。

21370仍没有服务器进度业务应答：还需核实字段的实际配置含义及21373/21374首DWORD，再接真实进度持久化。不是回一串全零即可完成关卡系统。

## 关卡展示与奖励配置边界

当前pveuiconfig.xml的50条记录同时提供DisplayDifficulty和RewardItem1/2/3，空字段表示没有该展示项。ReadStageMaps及测试器现保留这四项；数字ID仅供匹配客户端目录，不携带图片、物品资源或原版概率，不自动用于服务器发奖。例如同组不同难度的RewardItem1不同，不能把首条奖励铺到全部关卡。

另核对mapmgr.xml根MapInfo有97条MapConfig，包含MapId/MapType/Name/NeedTitleLevel等；与PVEEntryUI不是相同表。RoomSet控件txtCondition在模式9/10分支里查询21372对应记录，并从mapmgr配置名称构造条件文案。尚未恢复+5D8赋值及所有记录字段，不能据此把21372宣称为经验/金币/通关成绩表。


## 2026-09-22 单人闯关与辅助状态查询

- mode 10 (FosterMode) 与 mode 21 均通过单人准备人数门槛；仍必须通过地图开关、网络准备、对应已保存的关卡计划和奖励验证。未对49张地图直接无条件放行。
- 924B80发送21342空载荷；AF4679/AF46CF将21343绑定到A27670。A27670要求至少8字节，读取两个DWORD数量，校验长度至少8+12*n+8*m，再交A54F20替换两组缓存。证据保存在stage-state-21343.asm。
- 本服尚未恢复两组记录的完整业务语义，因此21343仅返回两组空记录（8字节），不伪造通关历史。截图中的16字节全零也可满足接收器下限，但多出的8字节不被空列表消费。
- 21370仍使用21372目录与21373地图名单；不采用截图中的21371/404配对。
- 回归覆盖：mode10/21单人到达网络检查，普通个人/团队对战仍拒绝单人开局；21342大厅/房间查询与错长输入；已有关卡计划验证测试继续通过。原生关卡画面仍待实际验收。
