# 模式10生成数量限制

依据实际加载的编译 include，SHA256：
`0a083607cab1456c0976038f1e78658a208607355c015060a4492259cde5280f`。
`libs/event.lua` 267–411 行原型（提取列表索引51）的 Lua 5.1 指令，PC从1开始：

| PC | 实际计算 |
| --- | --- |
| 113–120 | `table.maxn(monsters) + monsters_cur_dead < max_monster_count` |
| 122–130 | `table.maxn(self.monsters) + monsters_cur_dead < self.group.max` |
| 132–139 | 全局可生成数 = 全局上限 − 活怪 − 尸体 |
| 140–146 | 组可生成数 = 组上限 − 本组活怪；这里没有减尸体 |
| 147–151 | 取全局和组可生成数的较小值 |
| 224–234 | 子组上限 − `get_gs_monster_count(self.monsters, self.group.subs[i])` |
| 235–247 | 再与该子组剩余未生成数取较小值 |
| 248–252 | 再与本次批次可生成数取较小值 |

服务端已核验计划每组只有一个子组，所以可将该组活动实体中血量大于零的数量用于子组活怪限制。未知模板血量也计入活怪，不能把初始零值当作死亡释放名额。已移除的实体不占容量；已登记零血量但尚未移除的尸体占全局容量，不占子组活怪名额。这里的血量仍是合法收包累计，不是服务端独立模拟。

`game/foster_spawns.go` 接入的是全局容量以及组/子组活怪数量的必要限制。**尚未实现完整批次门控**：原生外层用“组活怪 + 全局尸体”判断一次，而同批后续生成使用不减尸体的组预算；逐包重新套用外层条件可能拒绝同一合法批次的后续怪物。因此不把简单逐包尸体检查冒充原生逻辑。触发区域和批次门控继续保留为准入缺口，模式10尚未开放。

回归覆盖子组满额、组满额、其他组独立计数、零血量尸体释放子组但占用全局、已移除实体、未知血量、错误组号、零限制和校验不消耗生成顺序。真实归档8110的23个生成条目仍通过既有路由回归。
