# 8155 计分同步

当前客户端原生证据见 [scoreboard-8155.asm](evidence/scoreboard-8155.asm)。本次直接只读解码当前 gfld.dat，文件哈希写在证据首行。

9884D0 清零334字节缓冲区，复制7字节汇总，逐槽写8条36字节记录，调用A3FBB0发送8155。82C1D0要求至少334字节且处于战斗状态，读取同一布局。服务器暂只接受已证实发送器的精确334字节。

| 偏移 | 内容 |
| --- | --- |
| 0–38 | 8071公共信封，子命令8155 |
| 39–45 | 7字节汇总，具体字段名未完全恢复 |
| 46+36×槽位 | 8条玩家记录，各36字节，首8字节为UID |
| 记录+8、+12 | DWORD计数，暂保留偏移名称 |
| 记录+16、+18 | WORD字段 |
| 记录+20、+24、+28 | DWORD字段 |
| 记录+32 | WORD字段，接收器比较其增长后可能触发播报 |
| 记录+34、+35 | 发送器清零保留 |

不能把记录起点写成39：中间还有7字节汇总。此包没有已证实的房间/战斗序号尾字段，不得把最后一条玩家记录改写成上下文。

服务器策略：仅当前房主发送全表；逐槽检查UID与当前房间一致，空槽36字节全零；沿用信封UID校验、战斗状态限制与每子命令序号去重，然后原样广播给其他成员。非房主消息忽略且不因此断线。房主限制是服务器授权策略，不代表已证明原生实体控制标志就是房主标志。

这是客户端计分同步，不参与服务器金币、经验或奖励计算。由于缺少已确认的战斗标识，不能声称已经防住相同成员阵容跨战斗的所有延迟重放。

自动验证覆盖：原样广播、房间外隔离、非房主忽略、冒名信封、错误槽位UID、缺少成员、空槽携带数据、333/335字节、重复包以及结算阶段忽略。测试器诊断显示全部8槽与原始偏移；原包重放保留槽位UID，要求当前阵容一致。尚未证明原生界面计分及全部房型兼容。
