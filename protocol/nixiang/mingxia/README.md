# 名侠训练字段核对

## 真实TLS自动验证

TestTrainingTLS已通过（17.768s）：临时账号完整登录/P2P，查询→开始→重复开始→过早领取→两小时预览→领取→重复领取拒绝→重新开始。读取训练等级2对应120/小时，两小时实际入账240且唯一领取回执，ping保持连接。规则使用连接级临时表隔离，测试后清理本次账号及关联数据。此测试不执行原生UI，不代表角色等级即时显示或宝箱链完成。

## 领奖事务

新增ClaimTraining存储方法，账号行锁下检查原生消息对应的服务器操作ID；成功回执重试返回当前资料而不再次加经验。只接受已开始且满一小时的训练，以服务器时间和保存配置计算经验，复用AdvanceLevel；角色经验上限检查、累计经验、清空started与training_claims回执同事务。另设(uid,started)唯一约束防同周期重复提交。21007客户端会再发21002开始下一周期，但此处尚未接网络回包。

TestTrainingClaimLocalDatabase使用独立debug库连接级临时表，实际通过12.564s：经验升级、同操作重试、空闲/过早/关闭/未来开始时间拒绝、领取回执唯一键失败后经验及开始状态回滚、封顶、下一周期与非法账号/等级。没有修改已有玩家数据或运行配置。普通persistence测试亦通过；它在无DSN时跳过数据库用例，不能替代上述独立验证。

后续已移除rank调用参数：ClaimTraining在事务内从training_ranks读取，缺记录明确按未评级0，非法大于8拒绝；TrainingRank查询核对账号存在，不依赖角色等级。名侠晋级/宝箱和协议应答仍需接通。GM页面继续标记尚未生效。

扩展TestTrainingClaimLocalDatabase独立临时表实际通过（13.790s），覆盖默认未评级、已存等级读取及非法等级拒绝，同时重验发奖/回滚。没有开启运行配置。

另外静态核对4300的9CD5B0：仅写profile+F5/F9，不写角色等级+120，证据追加training-native.asm。大厅训练升级不能仅发送4300就声称等级即时同步完成，也不能挪用会触发结算界面的4120；需继续恢复原生非战斗资料更新路径。

当前客户端只读反汇编见training-native.asm，首行包含原文件SHA256。本报告不把显示字段当作已实现的奖励业务。

7F4730与881230发送21006空载荷；相邻7F4750/881250发送21002空载荷。事件表B2AD45起以56字节为一项，标志+0、回调+8、控件名+24：1DF8318项回调7F4750、名称btnMXtrain；1DF8350项回调7F4730、名称btnMXaward1。因此21002为开始训练，21006为领取入口。下一项7F52B0是物品信息展示，不是领取；不能错将名称归给后一个回调。另一面板B16825起同样有881250/881230回调。

21001/21005/21007的56字节块由7F5540分成32字节状态与24字节奖励预览；7F5520/7F5500分别返回两块。7F4980消费如下：

|偏移|已确认消费|
|---|---|
|0 QWORD|玩家UID|
|8 DWORD|名侠等级，7F5660格式化，1–8有等级文字分支|
|12 DWORD|未解|
|16 DWORD|等级至少1时用于txtMXBox1的X %d显示，不能据此直接发物品|
|20 DWORD|分钟，按有符号整数除60计算整小时，再除24计算天数|
|24 DWORD|未解|
|28 DWORD|训练状态门|
|32 DWORD|未解|
|36 DWORD|txtMXExp1每小时经验预览系数|
|40 DWORD|上述经验预览上限|
|44/48/52 DWORD|未解|

7F4B9C取奖励块+4，乘整小时，与奖励块+8比较取较小值。原生使用32位有符号乘法，后续GM校验及服务器计算必须防溢出，不直接相信客户端预览。7F4FE3起领取按钮至少一小时才启用，仅对本人显示可操作。

A2AD80处理21007时只接受56字节，随后发送21002并刷新状态；还需核实领取失败应答、等级/宝箱字段和奖励事务语义。当前服务器仅有查询与开始训练，本轮没有开启领取发奖。

新增共享ParseTrainingStatus和测试器诊断，保留未知DWORD；固定HEX用例验证64位UID、精确偏移和三种消息入口，0/32/55/57字节拒绝。TestTrainingNativeLayout实际通过（0.133s）。

## 配置持久层

## GM配置入口

training_get/training_save已通过既有desktop及HTTPS鉴权入口接入；请求携带training.revision与rules，服务器验证完整0–8级配置，保留本地/线上环境选择。GM新增名侠奖励页，读取失败不能保存，关闭开关不删除已填数值，保存冲突提示重新读取。当前仅配置持久化，游戏领奖事务尚未接通，页面明确标注尚未生效。

Go管理传输与HTTP鉴权测试通过；Flutter页面测试2项通过，覆盖读取失败、非法输入、revision及关闭状态下数值保存。静态检查修正条件块格式后通过。

新增TrainingSettings，保存0–8级每小时经验和上限，默认关闭且无虚构原版数值。版本检查、更新和审计同事务；关闭保留数值。奖励计算用uint64避免乘法溢出，满整小时计算并封顶。TestTrainingRewardPolicy通过；TestTrainingSettingsLocalDatabase独立debug库临时表实际通过（3.806s），覆盖旧版本拒绝、审计失败回滚、关闭保留配置及损坏数据拒绝。未接GM/领取发奖，未替换运行服务。
