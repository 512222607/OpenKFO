# 本地五项验收（2026-09-18）

## 18:27 实机复测发现的遗漏及修复

之前资料测试只覆盖2420→2421，未覆盖原生资料页随后的21000，因此不能将此前结果理解成资料页完整通过。实机 localtest2 查询 localtest1 时，2421/853 已完成原生写入，随后21000/8（目标UID10001）被“仅能查本人”的旧校验拒绝并断线。

现修正21000为按目标UID只读查询，21001写回目标UID；不存在目标返回通知，21002启动训练仍只使用已认证本人。查询使用账号LEFT JOIN训练状态，不创建训练记录。回归测试覆盖他人资料及训练串联、查询后继续请求、不存在目标、短包、禁止通过21002传入他人UID。下方为本次修复前的历史验收记录，不能代替修复后的原生界面复测。

范围：真实本地协议连接及独立测试库。使用 localtest8、localtest9、localtest10；没有启动或控制真实游戏画面，没有修改线上配置。测试器通过既有认证、P2P绑定、进房和结算流程，没有增加服务端测试旁路。

| 项目 | 本轮结果 | 尚未证明的部分 |
| --- | --- | --- |
| 自己/他人资料 | 2420→2421目标UID、长度与装备条数正确，两个账号均7件已装备 | 原生人物、装备模型及资料页画面 |
| 凌云神腕冰冻 | 对照 nixiang，发送类型14、持续3000毫秒的8150，另一账号收到完全相同内容 | 没有操作真实游戏的X技能，未证明自然3秒解冻或800伤害；按用户要求暂留 |
| 连续胜/负/平结算 | 连续3局：双方分别取得胜/负/平；胜10经验20金币，负和平5经验10金币；累计经验、金币增量一致；重复报告未再发奖；双方3550回房后可再次开局 | 客户端结果页显示与动画 |
| 购买/装备/持久化 | 一次100点券购买仅新增1个实例；扣款正确；2080槽0归一到槽8；卸下、再装备、重登后的库存及余额一致；最后恢复原武器 | 游戏商城与仓库界面的最终显示 |
| 房间异常 | 密码错误3080/31、满员3080/32；非房主踢房主不生效，连接保持；杀掉房主的测试连接组件后，另一成员保留原房且收到3160移交 | 原生错误弹窗及掉线后的画面 |

## 购买测试前置数据

初次自动购买阶段未通过，因为测试账号点券为0，不能声称当时完成购买。早期用例还误把物品类型25当成商城分类；测试代码已改为读取1540全商品后按记录类型选取。

随后通过**本地**GM接口给 localtest10 增加100测试点券，审计ID `protocol-acceptance-20260918-localtest10-100`，再执行购买专项。买入商品键1879301192（武器模板253000）、库存实例1048583；点券最终为0。新购物品保留在测试账号仓库，原装备已恢复。GM底层返回文案含“线上”，但本次显式指定 local 和本地私有配置，连接实现限定独立测试库，不应据文案误判环境。

## 证据与可重复运行

- `dist/协议测试器/acceptance-test.json`：首次综合运行，保留已完成的资料、效果转发、3局结算、房间异常证据，以及购买被前置数据阻塞的记录。
- `dist/协议测试器/purchase-test.json`：补齐测试点券后购买专项通过记录。
- `dist/协议测试器/acceptance-summary.json`：合并上述证据，明确保留未验证的游戏画面和冰冻自然结束状态。
- `dist/协议测试器/logs`：逐包发送/接收日志，包含商品、库存、结算数据。
- `client/protocol-tester/AcceptanceSmoke.cs`：可重复运行的协议验收。设置 `OPENKFO_TEST_PASSWORD` 后，EXE参数 `--acceptance-test` 跑综合流程；`--purchase-test` 仅重跑购买装备。需要空闲测试账号及足够余额，不自动赠送测试货币。它会实际修改测试账号数据。

Go的 game、persistence、protocol-tester 测试已用 `-count=1` 通过。未设置独立测试DSN的MySQL单测会跳过；上表数据库持久化证据来自真实协议重登及本地GM只读核对，不能混写成全部MySQL单测通过。

## 参考

- [默认槽与友好失败](nixiang/all-weapons-20260907-235015/equip/slot-zero-failure-report.md)：type25的slot0可规范为槽8；2100是2字节失败应答。
- [BUFF协议](nixiang/buff-debuff/BuffProtocol.md)：3000毫秒、类型14；自然到期还依赖客户端时钟与实体更新门控。
- [房间处理器](nixiang/room-enter-recovery/room-handlers/report.md)：3080错误DWORD位于+14；3100/245、3550/12等结构。旧笔记与当前实现存在差别时，未盲目改动服务器流程。
