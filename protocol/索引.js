# 全谱字典 · 索引

> **《全谱字典》= 消息面（包）+ 状态面（链）**。一本逻辑上的字典，物理上按册分卷；本文件是唯一总目与检索入口。
> 改档纪律：只增不改；新内容按"功能簇"落册，落不下的拆 `-a` / `-b`；册内按 opcode 升序。

---

## 一、两句话定位

- **消息面**（下列各册）：这个包长什么样、谁消费、写哪个全局。
- **状态面**（`状态面.js`）：这个状态怎么被建起来——时序 / 单例 / 状态机 / 依赖 / 失败模式。

两者互补不重复：**消息面不写时机与创建者，状态面不写字段与长度门**；状态面提到包只写 opcode 号。

---

## 二、总目（分册 → 范围 → 体积）

| 分册 | 范围 | 体积 |
|---|---|---|
| `索引.js` | 本文件：总目 / 检索入口 / 维护规则 / 使用说明 / 进度统计 / 工作指南 | ~7KB |
| `会话.js` | opcode 1020-1070（登录 / 账号 / 鉴权结果） | 9KB |
| `角色.js` | opcode 1101-1250（角色记录 / 档案 / 选角） | 19KB |
| `装备.js` | opcode 1450、2030-2591（物品 / 装备 / 换装 / 仓库） | 23KB |
| `房间.js` | opcode 3090-3550（建房 / 进房 / 成员 / 退房） | 14KB |
| `战斗准备.js` | opcode 4050-4300（开战 / 装载 / 就绪） | 25KB |
| `战斗内.js` | opcode 5001-6317（战斗事件 / 战报 / 战斗内配置） | 22KB |
| `社交.js` | opcode 7070-9161（社交 / 大厅目录 / 其他战斗） | 16KB |
| `系列20k.js` | opcode 19140-20572（活动 / 运营族） | 46KB |
| `系列21k.js` | opcode 21001-21483（培养 / 任务 / 商城族） | **64KB ⚠软上限** |
| `派发.js` | opcode 40000 及派发层阶段路由白名单（跨册通则） | 3KB |
| `上行发包-a.js` | 客户端→服务器 上行包与 sub_A3C950 发包点枚举（上） | **60KB ⚠软上限** |
| `上行发包-b.js` | 同上（下） | 55KB |
| `增补.js` | 离线全链实测增补 + 全静态战役反编译增补（跨册统一定谳） | 14KB |
| `规范摘要.js` | **规范性摘要**：消费链矩阵 / 字段图 / 各附录——**与分册冲突时以本册为准** | 61KB |
| `状态面.js` | 卷 T 时序 / 卷 L 生命周期 / 卷 S 状态机 / 卷 D 依赖 / 卷 F 失败模式 / 卷 P 传输层(SDP2P) / 卷 R 资源与 UI·地图 PVE / 卷 H hook 安全域 | ~19KB |

---

## 三、检索双入口

### 入口 1：按功能查（我要做什么 → 看哪册）

| 我要… | 册 |
|---|---|
| 登录 / 账号 / 鉴权 | `会话` |
| 角色 / 档案 / 选角 / 换线 | `角色` |
| 物品 / 装备 / 换装 / 仓库 / 商城 | `装备` |
| 建房 / 进房 / 成员 / 退房 / 房间设置 | `房间` |
| 开战 / 装载 / 就绪 | `战斗准备` |
| 战斗事件 / 战报 / 战斗内配置 | `战斗内` |
| 社交 / 大厅目录 / 频道 | `社交` |
| 活动 / 运营 | `系列20k` |
| 任务 / 培养 / 商城扩展 | `系列21k` |
| 上行包是从哪发的 | `上行发包-a/b` |
| 派发阶段 / 路由白名单 | `派发` |
| 离线实测与静态定谳的统一结论 | `增补` |
| 时序 / 单例 / 依赖 / 失败反查 | `状态面` |
| 实现 / 排查该按哪套口径 | `规范摘要` |

### 入口 2：按 opcode 号查（我手上有个号 → 看哪册）

| opcode 段 | 册 |
|---|---|
| 1000-1499 | `会话` / `角色` / `装备` |
| 2000-2999 | `装备` |
| 3000-3999 | `房间` |
| 4000-4999 | `战斗准备` |
| 5000-6999 | `战斗内`（6000 段部分见 `派发`） |
| 7000-9999 | `社交` |
| 19140-20572 | `系列20k` |
| 21001-21483 | `系列21k` |
| 40000+ | `派发` |
| 上行（不分明） | `上行发包-a/b` |

---

## 四、维护规则（防裹脚布）

1. **稳定键切割**：册 = 功能簇，不用数字序号；册内按 opcode 升序。新增系统直接加册，**不重编号**。
2. **软上限 60KB / 硬上限 100KB**：到软上限计划拆（按子簇或 opcode 半段拆 `-a` / `-b`），到硬上限必须拆。**拆分只改本表一行，其余册不动。**
3. **只收已定谳**：推演一律进册末"待定谳附录"，定谳后转正，超期未证直接删。
4. **跨册引用只给指针**：写"册名 + opcode 号"，不复制字段表。
5. **当前待办**：`系列21k.js`（64KB）、`规范摘要.js`（61KB）、`上行发包-a.js`（60KB）
   已达软上限，下次增补前拆分。
6. **★卫生规则（最重要）**：分册本体**只放已校对的最终结论**，不写时间戳、版本沿革、变更
   说明；一切变更 / 勘误 / 纠错一律记 `..\全谱日志.js`（一条 ≤3 行，格式见其档头）。
   —— 目的：避免某个 AI 的未校对产出被别的 AI 当成真理。

---

## 五、与 `规范摘要.js` 的关系

`规范摘要.js`（原 `工作区\协议规范.js`，R532 并入本目录）是**规范性摘要**（消费链矩阵 + 附录），本字典是**证据与总表**。两者分工记录已久，并存不改；本索引不替代它。

---

## 〇、使用说明

- 本字典以运行时 dump 的 293 条 opcode→handler 为骨架，逐个反编译 handler 填充。**每行 14 列**：`opcode` / `handler`(VA) / `RVA` / `方向` / `状态` / `阶段放行` / `字段布局` / `触发条件` / `读全局` / `写全局` / `关键子调用` / `关联包` / `冲突` / `说明`。
  - `方向`（上行/下行）：在 recv 表(msgmap_dump_run21b.log)内=下行，否则=上行 → 由提取器自动判。
  - `读全局` / `写全局` / `关键子调用`：由 S7 提取器从反编译 .c 自动抠出（dword_17C8xxx 引用 + 调用目标）。
  - `阶段放行`：已由 §十一 派发层白名单逆出并一次性回填（见 `out_C/S7_stage_fill.py`；来源 `dispatch_probe_run20e.log` + `msgmap_dump_run21b.log`）。
- **handler 形参约定（已由全量证据坐实，勿再按 `this`=缓冲理解）**：
  - `__stdcall  sub_X(a1, a2)` → a1=包缓冲，a2=包长。
  - `__thiscall sub_X(this, a2, a3)` → **a2=包缓冲，a3=包长，`this` 是 handler 所属对象**。
    90 个三参 handler 的统计：a2 判非空 43 次 / 被解引用 18 次；a3 与常量比 23 次；
    而 `this` 从不判空、从不与常量比（其 11 次解引用是访问对象成员）。
- **`写全局` 列的「无」系列取值**（空=未分析，「无」=分析过且确实不写，两者含义不同）：
  - `无` —— 解析所得子树（深度≤3）内零全局赋值，且子树中不含解析不了的虚调。
  - `无*` —— 同上，但子树含**未解析虚调**（虚方法体静态不可见，可能存在看不到的写）。
  - `无(仅懒加载单例)` / `无*(仅懒加载单例)` —— 子树里唯一的全局赋值是 `if(G==0){G=new(N);}`
    这类实例缓存，属分配缓存而非协议状态变更。
  - 依据：深度 1→4 逐级复算，「实写」行数恒为 0，且无一行是提取器漏填（见 `out_C/_fill_wg.py`）。
- **`字段布局` 列的两个非偏移取值**：
  - `无载荷(handler未读缓冲)` —— handler 全程未引用包缓冲形参，载荷被忽略、该包仅作触发信号。
  - `0x0:blob(0xN)` —— 包长守卫 `len==N` 且被调中恰有一次 N 字节整块拷贝，两者尺寸吻合，整包按不透明块落库。
- **地址列说明**：
  - **handler 列** = 函数完整虚拟地址(VA)，8 位完整格式（如 `0x00823420`）。本进程 NO ASLR，frida/IDA 可直接用此地址跳转。
  - **RVA 列** = 相对虚拟地址（RVA = VA - 0x400000），8 位完整格式（如 `0x00423420`）。IDA 反汇编窗口显示的就是 RVA。
- **上行包（客户端→服务器）不在 recv 表里**，需从 sub_A3C950 发包点逆向。例如 2080（切武器请求）是上行包，recv 表里没有。
- 派发层在 `sub_98FD60`（派发点 `0x0098FDB6`），有"阶段路由白名单"——某些 opcode 在特定阶段（大厅/房间/战斗）被拦截。白名单完整枚举已完成（见 §十一 / `S7_stage_fill.py`）。

---


## 十二、上行包（客户端→服务器，✅ 已完成）

> 上行包不在 recv 表里，从 `sub_A3C950`（发包函数）的全部 270 处调用点逆向，覆盖 146 个纯上行 opcode。
> 详见本文件末尾「§十二 续」完整表格（opcode/handler/字段布局/触发条件/读全局/写全局/关键子调用/关联包/冲突/说明 14 列）。

**已知关键上行包**：

| opcode | 触发场景   | 说明                                |
| ------ | ------ | --------------------------------- |
| 2080   | 点武器    | 切武器请求                             |
| 3010   | 建房     | 建房请求                              |
| 3020   | 进房     | 进房请求（83B）                         |
| 3320   | 1151回执 | sub_823420 末尾调 sub_A3C950(3320,0) |
| 3550   | 房间状态上行 | 客户端发房间状态给服务器                      |

---


## 十三、逆向进度统计

| 分组                      | 总数      | ✅已逆向   | 🟡部分    | ⬜待逆向  |
| ----------------------- | ------- | ------ | ------- | ----- |
| 登录/账号                   | 13      | 13     | 0       | 0     |
| 角色/角色记录                 | 23      | 23     | 0       | 0     |
| 装备/物品                   | 31      | 31     | 0       | 0     |
| 房间                      | 11      | 11     | 0       | 0     |
| 战斗/准备                   | 31      | 31     | 0       | 0     |
| 战斗内(5xxx/6xxx)          | 43      | 43     | 0       | 0     |
| 社交/其他战斗(7xxx/8xxx/9xxx) | 32      | 32     | 0       | 0     |
| 20xxx系列                 | 41      | 41     | 0       | 0     |
| 21xxx系列                 | 57      | 57     | 0       | 0     |
| 其他                      | 1       | 0      | 1       | 0     |
| **合计**                  | **283** | **282** | **1**  | **0** |

**完成率**：282/283 = 99.6%（✅）+ 0.4%（🟡）= 100.0%（A15/A16 对账勘正：20xxx/21xxx 全带字段级深读，与 r438 终表 VA 全一致；旧口径"53.7%"与"全🟡"作废）

---


## 十四、逆向工作指南

### 14.1 优先级

1. **装备包簇（§三）**：直接关系武器切换问题，最高优先级
2. **房间包簇（§四）**：关系开始键、花名册、退房再进房问题
3. **派发层白名单（§十一）**：解释为何房间内 2591 被拦截
4. **上行包枚举（§十二）**：补全客户端能发的所有包
5. 其余按需逆向

### 14.2 反编译方法

- 工具：`out_C/C_decomp_any.py "<gfxz.dat>" <out.c> <depth> <ea>`
- IDA 9.4：`D:\phaser-project\reverse-handoff\tools\IDA Professional 9.4\idalib\python`
- 对每个 handler 反编译后，提取（对应写入主表列）：
  1. 字段偏移+类型（从 buf+offset 读取）→ **字段布局**
  2. 读/写了哪些全局状态（dword_17C8xxx 等）→ **读全局** / **写全局**
  3. 调用了哪些子函数（尤其 sub_9CDB60 挂载 / sub_9D0F70 重建 / sub_A3C950 发包 / sub_81CB00 建房间avatar）→ **关键子调用**
  4. 发了什么回执包（sub_A3C950 调用）→ **关联包**
  5. 是否有阶段检查（if stage==xxx）→ 辅助填 **阶段放行**
  6. 方向（上行/下行）：在 recv 表(msgmap_dump_run21b.log)内=下行，否则=上行 → **方向**

### 14.3 填写规范

- 每逆向完一个 handler，更新本字典对应行的"状态"列为 ✅，并填充 方向/阶段放行/字段布局/触发条件/读全局/写全局/关键子调用/关联包/冲突/说明。
- 若发现该 opcode 属于某功能簇（如装备簇），在对应章节的关联图中补全。
- 若发现歧义（静态/运行时冲突，如 2591/3550），在"冲突"列标注、并在"说明"列写清。

---
