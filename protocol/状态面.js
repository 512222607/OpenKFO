# 全谱字典 · 状态面

> **本册是《全谱字典》的状态面分册**，总目与检索入口见 `索引.js`。
> 范围：卷 T 时序 / 卷 L 生命周期 / 卷 S 状态机 / 卷 D 依赖 / 卷 F 失败模式 / 卷 P 传输层 / 卷 R 资源与 UI / 卷 H hook 安全域。
> 纪律：只收已定谳，推演进"待定谳附录"；提到包**只写 opcode 号**，字段一律回消息面各册。

---

## 卷 T 时序

### T1 登录 → 大厅（已定谳）
```
1002 →（stage3 初始化 → 4）→ 1012 → 9145B0[stage5 + guard2=1]
  → 1130 灌窗体 → 选角 → 910790[guard2=0, guard1=1]
  → 910510[guard1=0, 连 GS] → 2030[双守卫过] → stage6
```
**kk_local 现役路径**：1012 → 连 GS 发 1010 → 1131/1020/1120 → 1151
→ 客户端自发 **3320**（选角）→ **3330 + 1201** → 客户端自断重连发 **2010** → 2030 → 大厅。

### T2 建房 → 战斗 → 退房（已定谳）
```
3010 → 3100 + 3160（建房）→ 3550（确认）→ 4030（准备）
  → 4050/4080（开战）→ 4160 → 4170 + 4180（装载）
  → 8040 → 8070（就绪）→ 战斗（8071 流）→ 3110 → 3115（退房回大厅）
```

### T3 每局换图（已定谳）
```
4030 → 3115 退房 →(1.2s)→ 3100 + 3160 重建（重抽地图）→(1.0s)→ 4050/4080 开战
```
依据：战斗地图权威 = 房间对象 +24/+28，建房时拷入锁死；**房间态重发 3100 被客户端无视**。

### T4 退房链（已定谳）
```
3110（上行 0B，房间态也发）→ 3115（下行 0B = sub_82D920：17C86D0=0 复位
  + 21428 重置 + sub_82CF60 + SetStage(6) 回大厅）
```
★服务端必须对房间态 3110 应答 3115，否则客户端弹"断开连接"。

### T5 换装链（已定谳）
```
2080（上行 16B 切武器）→ 1120（全量 68B×N）+ 2090（20B→17CA268）
  + 1151（360B+68B×N 重建）；2300（上行 4B 卸下 → sub_9CDA80）；2430（兵器库）
```

### T6 训练链（已定谳）
```
21000（上行 8B uid）→ 21001（下行 56B = 32B 训练信息 + 24B 奖励位 → 17C8B7C；
  +28=active 置位 → 自发 21002）→ 21005（56B）；21007 亦触发 21002；
  21010（0B 重推请求，0x925960/面板事件自发）
```
kk_local 已实装：21000→21001、21002→21005（engine.py）。

### T7 大厅拉链（已定谳，字节级锚 0x925960）
```
进大厅（925A60→925960）一次全发：2122+5286(空) → 6000(4B uid) → 6001/6002/6003(空)
  → 20531(空) → 21000(8B uid) → 21010(空) → 21340(空) → 9083(sub_85D930)
面板事件重发：9262D0/805850 再拉 6001/6002/6003
```
常量锚：0x84A=2122、0x14A6=5286、0x1770~0x1773=6000~6003、0x5033=20531、
0x5208=21000、0x5212=21010、0x535C=21340。此链解释全部高频 0B 上行（≈2/run）。

### T8 战斗 → 结算 → 回房（B 线定谳；结算数据源=【推演】待 B4-D1）
```
战斗（8071 流）→ 车轮局：8394（每轮胜负）×N → 8397（终局：95DF40 终局 UI
  + 987E40 收尾）；常规局：4120（A_NOTIFY_RESULT，清准备门）
  → 结算画面（数据=本地：统计块 4536C0 +0/+4/+16/+20、8155 比分、5411/5412 链）
  → 3110 → 3115（主清扫）→ stage6 回大厅
```
依据：已注册 214xx 19 码均非结算包（t280 零错配背书）；4120/8397=局末信号。
动态对账清单 = `产出\s_b_19_settlement_family.out.txt` §四。死亡/复活细分链见
`产出\s_b_48_death_chain.out.txt`（复活符 680001，PVE 每图 3 次）。〔B19/B41/B48〕

---

## 卷 L 生命周期（单例 / 全局）

| 对象 | 创建者 | 创建时机 | 被谁写 | 缺失后果 |
|---|---|---|---|---|
| RoleProperty 管理器 `dword_17C86F0` | sub_A40060 | **stage3** | — | sub_A74030(this=0) → **AV@0x43329a** |
| CUIActivityComp `dword_17C8B88` | sub_402DD0 ← sub_A40060 | **stage3** | — | **AV@0xa8897d** |
| 配置对象 `dword_17C86F8` | sub_A3C170 解析 config.xml | 启动 | **1131**（44B 整块覆盖） | LS 地址异常 |
| 派发器链 | sub_A274B0 / sub_A270E0 / sub_A27110 | 启动 | — | 293 条 opcode 全表失效 |

**★核心定谳**：强驱路径跳过 **stage3** = 全部单例缺失的总根；正解是补 stage3 时序让引擎自跑初始化，不是逐个补单例。

### stage × 单例创建全表（A34 落位）

每帧状态机 `sub_990250`（switch[stage]）：case0=17C87DC=0+A3EEA0→SetStage(1)；
case1=A3FC20→成功置 17C8720=1 否则 SetStage(2)；case2 空转；**case3=call sub_A40060
（总装载器）**→成功 17C8720=1；case4/5/6=44A8D0+A0C190（UI tick）；case7 空转（包驱动）；
case9=98E260(17C8710,dt)（战斗 tick）。

| 全局（A40060 装载序） | 对象 | 表文件 | 缺失后果 |
|---|---|---|---|
| `dword_17C868C` | sub_77ACB0 返回对象 | — | 连接前置 |
| `dword_17C86F0` | RoleProperty 管理器(0x40B) | RoleProperty.xml（A74350） | **AV@0x43329a**（进度 5%） |
| `dword_17C86F4` | EmoteFaceModel(0xC0) | EmoteModel.xml（A8DC60） | 弹窗+装载中止（10%） |
| `dword_17C8B1C` | 剧情任务管理器(0xFC) | BaseQuest.txt（A4B220） | 剧情 tab 空（60%） |
| `dword_17C8B88` | CUIActivityComp(0x24B) | sub_402DD0 | **AV@0xa8897d** |
| （同族续） | item.txt→17C8794 / rolemodel.xml / ShootObj.xml / 标准表 | T-293/T-294 | 对应系统瘫 |

> 全局/单例 33 行全表（含懒建族与读者面）见 `产出\s_a_40_vol_L_globals.out.txt`（跨册指针，不复制）。

---

## 卷 S 状态机（三套并列 · B38 完整化）

### S1 客户端 stage 机（sub_98FDE0：thiscall，ecx=stageMgr，栈=新 stage）
```
0 启动 → 2 资源预载 → 3 原生初始化（sub_A40060 装全部单例，见卷 L）
  → 4 单例就位 → 5 角色选择窗体（9145B0：guard2=1）→ 6 大厅（2030 双守卫过）
  → 7 房间（3100→SetStage(7)）→ 9 战斗（8070 链内切换，与 986370 Lua
  "OnBattleStart" 同段）
逆行：4081 开战失败→7；3115 退房→6；1220/40000→拆会话重登（回 0/2/3 链）。
写点：9145B0@0x91462d(5)、823160(6)、824490(7)、8070 链(9)、3115(6)。
```

### S2 房间 phase 机（服务端视角，kk_local_ab engine.py）
```
EMPTY → ROOM（3010→3100+3160 建成）→ [4030 准备] → BATTLE（4050/4080 开战，
  4080+8050+4180+8070 四连）→ [3110 退房] → EMPTY
随机房换图 = 退房→重建（3115→3100+3160 重抽）→再开战（T3 演序）。
```

### S3 战斗 phase 机（客户端）
```
装载：4080 后 4160→4170+4180（+6316/21440 辅助）
就绪：8040（上行 14B）→ 8070（下行 12B，dword_17BAB78=1）
战斗：8071 流（8121 伤害/8120 动作/8126·8150 buff/8155 计分/8450 换武器…）
局末：车轮=8394(每轮)→8397(终场)；常规=4120（清准备门）
收尾：987E40 → 结算（本地数据）→ 3110/3115 回大厅
异常：4081（清门+回 7）；1220/40000（拆会话/全清）。
```

---

## 卷 D 依赖（缺 X → 现象 Y）

| 缺失 / 未做 | 现象 |
|---|---|
| 跳过 stage3（强驱进大厅） | 单例全线缺失 → 进厅即 AV |
| 未走选角确认 `sub_910790` | guard2 恒 1 → 2030 守卫不过 → 进不了大厅 |
| 房间态 3110 不应答 3115 | 客户端弹"断开连接" |
| P2P 未绑定 | 3010 被拒 |
| 客户端包内缺 mapselect.xml | 6317 解析树为空 → **6317 白给**（与建房列表无关，见 增补 Z） |
| 外置 mapmgr.xml 未放 | 建房地图列表占位 / 联机之王单图（真源=A55FB0，窗体+0x578） |
| kk_local 不回 6000 族 | 任务面板四 tab 全空（模板表全本地，只缺进度喂） |
| kk_local 不回 21420/21380 | 商城/时装面板打不开（入口包门） |
| kk_local 不回 21011 | 签到面板空白 |
| kk_local 不回 5000/5002 的镜像 | 聊天互发无回显（单发不报错） |
| 外置 Data\Config 误放错版文件 | 对应系统行为被覆盖（外置=优先位，操作纪律见 卷 R） |

### 跨连接残留清单与清理点（B49 落位）

| 全局/容器 | 建 | 清 |
|---|---|---|
| `dword_17C8714` GS 会话 | 登录链 | **1220（=0+63E780）**、1030、40000、2070；A3C950 自环在其==0 时整体短路 |
| `dword_17C8708` 房间 | 3100 | 40000→81D5C0 |
| `dword_17C8710` 战斗 | 4080 链 | 40000 / 3115（82CF60） |
| `dword_17C86CC` 选择缓冲 | 1158 | 21425、4081 |
| `dword_17C86D0` 准备门 | 4050 | 3115、4081、21425、4120 |
| `dword_17C86D4` ready 显示 | 3105/3350 +144 | 随房间对象 |
| `dword_17C8694` 频道 | 2030 | 消费 2250/3070 |
| `dword_17BAB74/78` 局末标志 | 战斗 | 4120 尾部置 0 |
| 房间+304/308 聚焦成员 | 81BBE0 | 随房间对象 |
| 40 槽名册 | 3105/3160/3090 | 3115→82CF60/退房链 |
| P2P lease（SDP2P） | UDP 1001 | 1012/租约超时（卷 P） |

三大清理时机：3115=主清扫；1220=拆会话（822D50：17C8714=0+17C86A4=1，按
4164E0 状态走 910C00 重登/9107E0 回选角）；40000=故障全清。老线"读条 AV 三条件"
（3105 缺员/名册残留/GSState 跨连接残留）全部落本表——静态判据：进战前
818C70(40 槽表) 返回非 0 即表脏。kk_local 纪律：3115 必应答、40000 不该触发、
重连=1010 bootstrap 全链重放按新会话喂。〔B49〕

---

## 卷 F 失败模式（现象 → 候选链 → 判据）

| 现象 | 候选链 | 第一判据 |
|---|---|---|
| 进大厅即崩 | stage3 单例链（卷 L） | 查 `dword_17C86F0` / `17C8B88` 是否为 0 |
| 卡在 stage5 进不了大厅 | 选角确认链 guard2 | 查 guard2 是否归零（sub_910790） |
| 建房后卡死（mode=8 挑战） | ★与地图资源无关（10001=合法图"称号任务试练场"，资源齐全）；mode8 无 mapselect 白名单/无进度数据路径 → 等待挂起 | 对照 s_b_21；PVE 正解=Mode10/21+91xx（卷 R） |
| 打人不掉血 | host 槽 / 本地数值链 | 查 4080 +11 host 槽是否填对 |
| 地图列表占位 / 单图 | mapmgr 链 | 查外置 mapmgr.xml 与 A55FB0（窗体+0x578），与 6317/mapselect 无关 |
| 换装"图标变、模型不变" | 显示容器 ↔ 战斗实体双结构 | 1151 自环 3320 与 spawn 竞态 |
| 大厅大量 0B 上行无应答 | 大厅拉链 0x925960 | 正常自发非故障（T7）；kk_local 全吞安全 |
| 聊天发不出 | 5002 本地频控 | 1s 内重发被拦（红字提示，非断线） |
| 任务面板 tab 全空 | 60xx 喂养链 | kk_local 是否回 6000→6020/6041/6042（模板表本地） |
| 训练面板不动 | 21000 应答链 | engine.py 21000→21001 在位与否 |
| 商城/时装面板打不开 | 入口包门 | 21420/21380 应答族（面板可见性锁第一包） |
| 签到面板空白 | 21011 | 646B 8 卡是否下发 |
| 升级无特效 | 21326 | 71B 是否下发 |
| 点充值/官网无反应 | ShellExecute 外连 | 离线预期行为，非故障 |
| 选服列表空 | 1012 内 GSList | u16 计数 + 0x110/条格式 |
| LS 连接被拒/立即断 | 1131 覆盖面 | 44B 是否打歪 17C86F8+0x88 Ip/Port |
| 长会话 ~10min 掉线 | 600s 心跳兜底 | 心跳 ack 是否持续（C-99） |
| 私聊连发被吞 | 5002 频控 | rx 5002 恒 0xD7 判据 |
| 8071 尺寸对不上 case | 71B=8126|8440、55B=8157|9500 族 | 抓 payload 首 DWORD（0x1FBE/0x20F8/0x1FDD/0x251C~E）〔B16〕 |
| 战斗比分不动 | mode16 门 | 4C11C0(room,16)（计分/透传/击杀上报三件事同门）〔B17〕 |
| 连杀不播报 | 98AE80 映射 | score 3→事件123/4→124/≥5→19 + 44E110 在线门〔B17〕 |
| KOF 包无反应 | RTTI 门 | 非 CKOFMode 模式 8394/8397 整包静默丢〔B18〕 |
| 房间背景黑/旧图 | 静默降级 | A60D70 miss→81A300 早退 / 77D010 null→守卫跳过（零崩溃路径）〔B31〕 |
| 建房被拒"P2P未连" | SDP2P 门禁 | 0x9181b2（getter 0x462480==-1）→查 UDP 1001 链（卷 P）〔B28〕 |
| UDP 断了会退游戏吗 | 不会 | 判死刑在 TCP（1220/600s 兜底）；UDP 只承载 SDP2P 通道〔B35〕 |
| 模型不显示 | 挂树链 | 779390 挂树→5CA4C0 脏队列→7770C0(+0x28 场景指针门)；老线 C1 五证首选〔B44〕 |
| 垃圾图标路径 | 数据卫生 | item.txt +68 图标列 verbatim 缺失→字符串池错位读（864FB0）；数据侧修〔B34〕 |

---

## 卷 P 传输层（SDP2P / UDP · B28/B35/B39 落位）

### P1 帧头（24B 小端，version 恒 1）
version u16 / ident u16 / session u32 / seq u32 / source u32 / dest u32 /
reserved u16 / flags u8 / extra u8；门 24≤len≤32768。

### P2 包表
| 包 | 向 | body | 语义 |
|---|---|---|---|
| 1001 | C→S | 141B（首 2B 大端文本长 n≤20，实测 0；全长 165B 实证） | SDP2P 登录（分配 player/session） |
| 1002 | S→C | >III(0,player,session)+4B ip+u16 port | 登录应答 |
| 1013 | C→S | 4B | keepalive（续租 60s） |
| 1014 | S→C | 4B 零 | keepalive 应答 |
| 1012 | C→S | 4B | 退出（清租约，无应答） |

一 adapter 会话一 P2P 端点（重复 1001 拒绝）。1156=TCP GS 装备槽包，非 UDP。
kk_local 实装=wire.py sdp_header/sdp_reply + service.py SdpProtocol（客户端实测互证）。

### P3 门禁与失败
- 建房 P2P 门：sub_917450 尾 **0x9181b2**，getter 0x462480 返回 -1 → 弹
  "还没有连接P2PServer" 放弃建房；离线由 UDP 1001 正常应答自然过门
  （老线旁路钩 hook_run102/105/107 在新线全部不需要）。
- gfxz sendto 唯一调用区 0x63E130（63Exxx 连接管理族）；收包=WSAAsyncSelect 异步。
- 客户端 UDP 重试 ~83s 耗尽→进程结束（计时器待动态定位）；租约 60s 服务端可调。

### P4 与卷 T 的关系（一句话）
TCP（卷 T）=会话/房间/战斗的权威链，判死刑也在这里（1220 拆 17C8714 / 600s
心跳兜底）；UDP（卷 P）=SDP2P 通道，只承载 P2P 登录/续租与（真网的）战斗
转发，离线单机里它断了不杀会话。

---

## 卷 R 资源与 UI（A45/A46/A47 + B29/B30/B31/B40/B45 落位）

### 地图与 PVE 资源域（B 线落位）

- **地图权威表 = mapmgr.xml 96 条 MapConfig**（config.spf2 包内；全表=
  `产出\s_b_29_mapmgr_parse.txt`）。worldpath=Data\Map\<dir> 96/96 全存在；
  场景配置 xmlfile=maps\*.xml 全在包内（盘上 Data\Config\Maps 整个不存在=
  包内读，A5FDE0 通道）。MapType 三位开关：个位=新手/十位=精英赛/百位=迁敌赛。
- **装载点 = 3100→sub_81A300**（A60D70 全库唯一调用者；三调用=824490/81ABA0/
  8253A0）→"DATA\Map\room\%s"+MapConfig(+120 音轨/+144 出生点 shape)——建房期
  一次装死 ⇒ 每局换图只能退房重建（与 T3 互证）。
- **失败模式 = 静默降级**：A60D70 miss→81A300 早退；world 缺文件→77D010 null
  守卫跳过；零崩溃路径。81ABA0 内硬编码默认装 2 号房场景。
- **白名单 = mapselect.xml（仅 Level4 有货）**：Mode 0/1/2/3/5/6/10/15/16/21；
  **Mode10=ActMode 挑战 15 图、Mode21=9170**（PVE 正解入口）；mode8 不在任何
  白名单（10001 勘误：=合法图"称号任务试练场" ji/chrwb，卡死与地图无关）。
- **PVE/ActMode 关卡段**：81xx 六族×四难度 + 91xx 十九关（bossrush/年兽/僵尸
  古寺塔防族/天界之战/单挑BOSS）；场景配置=maps\act_*.xml（Camera/掉落点/
  可破坏物/宝石点）；波次与数值=pve.xml TitleMission（全本地）+ spf2 script\
  Lua AI；复活限制=pvemapitemrestrict.xml（680001 复活符每图 3 次）。
- **战斗资源命名与动表**见 `产出\s_b_45_res_semantics.out.txt`（新增资源：
  新图=mapmgr(+mapselect)；新 PVE=act_*.xml+mapmgr+pve.xml；全在 config.spf2，
  改表=重打包链）。星级=mapmgr Difficulty（1~10）无独立表。

---

- **文件系统机制**：spf2 包虚拟层（sdfile.dll）+ 外置优先覆盖位。Data\config.spf2
  为主体；外置命中则不读包。判"缺了会崩"必须查包内，外置缺失≠缺。
- **sui**：170 件明文 object 树（Data\UI\File\）；名字=各窗体 ctor 硬编码（8 实锚）；
  控件查找 sub_75FBF0 判空回退（缺件=静默缺失不崩）；**duilib 自绘无 Win32 句柄
  ⇒ 模拟点击不可行**。控件类型 25 类（CXStatic 3981/CXPicture 3560/CXPanel 1702/
  CXButton 1662/CXRadioButton 1532/CXAnimation 1489/…全表见 s_a_45）。
- **UI→(全局→)协议映射**：28 行两层表见 `产出\s_a_24` / 24 行三层版见 `产出\s_a_42`。
- **音频**：DirectSoundCreate8 唯一入口 0x645940；资源 Data\audio 851 件
  （2deffect/3deffect/music），文件名由 Audio.xml 配置驱动；缺失=静音。
- **Data 目录三档**：包内兜底为主（RoleProperty/item/mapmgr/BaseQuest 等）；
  外置优先位=Data 根 config.xml + Data\Config（parameter/standardspeed）；
  红线件=gpkupdate.ini（勿动）。

## 卷 H hook 安全域（A35/A36/A44/A48/A49 落位）

- **线程**：主线程（引擎/UI/协议派发全在）+ 网络泵（CreateThread 0x63F10D）+
  CRT 辅助（0x66C536）+ Concurrency 调度器（A40060 内）+ Lua 线程（_beginthreadex
  0x58C490 + createThread）。**XUI/sui 构造链必须主线程**；发送路径可跨线程。
- **时钟**：SetTimer/timeSetEvent 导入零命中（全靠帧内差值）；GetTickCount×64 /
  QPC×11 / timeGetTime×3（网络层专用 0x77F000/0x9056E0/0x905790）。改时间钩优先
  timeGetTime 三点；高频时钟点禁 attach。唯一长超时 = 600s 心跳兜底（C-99）。
- **分配**：纯 CRT new/malloc，无自管池 ⇒ frida Memory.alloc 安全；cave 页
  Memory.protect(rwx) 常规可用。禁区：/GS cookie（dword_C7B480）、SEH 链（fs:[0]）、
  Concurrency 内部。
- **异常**：SEH 全库铺开（编译器默认形态）；stage 机吞异常=卡死不崩；到 filter →
  SDError.dll（TempDump+POST）→ 死路#12 链；磁盘零改动+gpkit 接管 ⇒ 现役不触发。
  GPK 补充实锚：自校验对象=宿主二进制本体（死路#12：补丁版 861B47CD 触发、原版
  SEH 吸收存活）；GPKitClt.dll EntryPoint RVA=**0x1a6f72**（壳自展开器，.text raw=0）；
  检测项清单与接管结论=仅 ord1 必需（现役），处置正解=不处置（s_b_32/s_b_50）。
- **退出**：7 处（ExitProcess 0x672B2E/0x7EA230、TerminateProcess 0x6667F5/0x67235C、
  UHEF×4）；全部记录+放行（C-115：拦 noreturn 必卡死）；退出观测用 QuitLog.txt。
- **外连最小集**：socket 3 包装（0x63DE10/0x643300/0x63E2F0）+ GPK 更新链
  （0xB9B47C）+ SDError + sdologin HTTP + IE 内嵌；现役栈已全覆盖（s_a_29）。
- **特征码**：A74030(16B)/98FDE0(15B)/91462E(8B) 唯一命中；433290 需尾掩码；
  469A60=模板族改锚点+链行走。扫描器=s_a_51_sigscan.py（防呆三件套内建）。

---

## 待定谳附录

（当前为空。推演结论一律进此；定谳后转正进对应卷，超期未证直接删除。）
